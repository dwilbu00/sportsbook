# Sportsbook Value Finder — Improvement Work Log

> **Purpose:** durable progress tracker for the audit-driven improvement work, so
> work can resume after a context compaction. Update this file as tiers complete.
> **Full plan (rationale + every finding):** `~/.claude/plans/joyful-drifting-cocke.md`
> **Started:** 2026-07-16 · **Last updated:** 2026-07-17 (P3 — structural cleanups + full analysis.py split)

## ▶ Resume here (current state)
- **Done:** P0 (code side), all of P1, all of P2, and **all of P3** (full `analysis.py` split + correctness + hygiene). **154 tests passing**, all green. P3 was adversarially reviewed (4-dimension workflow, ~262k tokens) with **zero confirmed findings**.
- **Working tree:** everything is uncommitted (no commits made per user instruction). `calibration/basketball_nba.json` regenerated on the new data-driven pool (2026/2025); the prior P1 version is saved as `calibration/basketball_nba.json.p1bak` (untracked — safe to delete once happy, or `cp` it back to revert). See Git state at bottom.
- **Next up:** the audit-driven code work (P0–P3) is complete. Only the 👤 **user-only P0 key rotation/history purge** remains, plus the explicitly **deferred P3 residuals** (see P3 section: `backtest.py`/`app.py` god-module splits, app fetch/analyze pure-function extraction, single-bet missing-price *display* number).

## How to resume / verify
- **Run all tests:** `python -m unittest discover` (currently **154 passing**). CLI tools now self-configure UTF-8 stdout (`cli_encoding.configure_stdio`), so the `PYTHONIOENCODING=utf-8 PYTHONUTF8=1` prefix is no longer required to run them on Windows (still harmless).
- **Re-fit calibration (Windows needs UTF-8 to avoid a console crash):**
  - NBA: `PYTHONIOENCODING=utf-8 PYTHONUTF8=1 python refit_calibration.py --sport nba --prior-season 2025`
  - MLB: `PYTHONIOENCODING=utf-8 PYTHONUTF8=1 python refit_calibration.py --sport mlb --season 2024 --prior-season 2023`
  - `save_calibration` MERGES: it preserves non-props blocks (starter_adjustment, expected_runs_challenger, lineup_adjustment, prob_shrink). Safe to re-run.
  - Restore a calibration file if a re-fit goes wrong: `git checkout calibration/<file>.json`.
- **Deps:** pure stdlib + `requests`, `streamlit` only. No numpy/pandas/scipy.

## Status legend
✅ done & tested · 🚧 in progress · ⬜ not started · 👤 requires user action (out of band)

---

## P0 — Critical
- ✅ **P0.1 Committed live API key** — code hygiene done: `config.json` git-ignored + `git rm --cached`, `config.json.example` template added, `load_config`/`save_api_key` fall back to template + read `ODDS_API_KEY` from secrets/env, misleading help text fixed. Files: `config.json.example`, `.gitignore`, `app.py`.
  - 👤 **STILL REQUIRED (cannot be done for you):** (1) rotate/revoke the key at the-odds-api.com; (2) purge it from git history (`git filter-repo`/BFG) — it remains in history until then.

## P1 — High (money-losing / integrity)
- ✅ **P1.1 Props flagged +EV while −EV** — added `analysis._prop_is_value(edge, threshold, expected_roi)`: requires edge ≥ threshold AND `expected_roi > 0` at the executable price. Wired into candidate + logged flag. Files: `analysis.py`.
- ✅ **P1.2 SGP correlation-inflated payout** — added `analysis._parlay_value_joint(best_joint, indep, has_sgp)`: same-game parlays priced against the INDEPENDENT joint (cancels the correlation benefit the book removes); `has_sgp` computed before the gate; naive figure kept as `expected_roi_pct_naive` for display. Files: `analysis.py`.
  - Known limitation: parlay *selection* still scores with the copula joint, so an SGP can be selected then rejected at the gate (a size may yield no value parlay). Gate correctly prevents surfacing −EV SGPs; a full re-rank is a P2-scale follow-up.
- ✅ **P1.3 Look-ahead leakage in prop-calibration sweep** — `_resolve_opp_pa_windowed`→`_resolve_opp_pa_asof` (window=None = season-to-date as-of, matching runtime); `def_adj` application always uses as-of, never the full-season aggregate, and skips when no prior games. Files: `backtest.py`.
- ✅ **P1.4 Winner's-curse calibration selection** — `_evaluate_calibration_methods` split into reusable `_score_calibration_methods`; `refit_calibration._best_per_prop` now requires a non-empirical method to beat baseline A by `MIN_CALIB_BRIER_GAIN` (0.002) on the holdout AND confirm in two expanding chronological folds, else falls back to empirical A. Persists `baseline_brier`, `cv_brier`, `confirmed`. Files: `backtest.py`, `refit_calibration.py`.
- ✅ **Calibration files regenerated** (the actual leakage cleanup):
  - `calibration/basketball_nba.json` — 3 props, all confirmed=True, leakage-safe as-of defense.
  - `calibration/baseball_mlb.json` — H2 gate demoted pitcher_strikeouts (B→A) & pitcher_outs (C→A); pitcher_earned_runs=C confirmed; pool 18→70 players; ensemble blocks preserved.
- ✅ **P1.5 Stale odds silently priced as live** — `_read_cache_expired` returns age; `get_event_odds` serves stale only on genuine quota/rate-limit (`_is_quota_error` distinguishes 401-invalid-key from 401-quota), bounded by `STALE_CACHE_MAX_AGE` (6h), flags payload `_stale_cache`; app warns. Files: `odds_client.py`, `app.py`.
- ✅ **P1.6 Failed/empty ESPN responses cached 30 days** — added `NEGATIVE_TTL_HOURS` (15 min) short negative cache in `cached_athlete_id`/`cached_gamelog`. Files: `espn_cache.py`.
- ✅ **P1.7 No pre-flight credit guard** — hard-stop before the paid fan-out when `actual_cost > remaining - credit_reserve` (config.json, default 0). Files: `app.py`.
- ✅ **P1.8 Durable queue silently non-durable** — surfaced the "Local cache" durability warning in the sidebar (was only on the Model Guide page). Files: `app.py`.
- ✅ **P1.9 Money-path untested + no CI** — `test_money_math.py`, `test_calibration_refit.py`, `test_reliability.py`; `.github/workflows/tests.yml` runs `python -m unittest discover`.

### Bugs fixed along the way (pre-existing, not in original plan)
- ✅ `backtest.py` used `defaultdict` without importing it → `run_player_props_backtest` crashed on every call (re-fit tooling was broken). Added `from collections import defaultdict`.

---

## P2 — Medium (🚧 batches 1–3 done)
- ✅ **P2a Edge baseline inconsistent across markets** — added `analysis._devig_fair`; ML/totals/spreads now measure edge against the de-vigged fair prob AND gate on `_prop_is_value` (positive EV at executable price). Falls back to raw implied on one-sided markets. Files: `analysis.py`. Tests: `DevigFairBaselineTests`.
- ✅ **P2b Retry connection errors + forward-tracker attempt bug** — `_get_with_retry` wraps `requests.get` in try/except `RequestException` and retries transient faults; `forward_tracker.capture_closing_odds` no longer marks an event "attempted" on retryable-later failures (transient network faults with no HTTP response, or 401/429), so paid closing snapshots aren't dropped forever. Files: `odds_client.py`, `forward_tracker.py`. Tests: `RetryOnNetworkErrorTests`.
- ✅ **P2c XSRF/CORS disabled on forwardable port** — removed `--server.enableCORS false --server.enableXsrfProtection false` from `.devcontainer/devcontainer.json`.
- ✅ **P2d Deployed Platt (a,b) never scored on held-out data** — the deployed params are (correctly) refit on all observations; the `holdout_*` metrics are a cross-validated score of the *procedure*, not of those exact params. Made the provenance explicit in the persisted fit (`holdout_metric_scope="fold_cross_validation"`, `deploy_fit_scope="all_observations"`) + a clarifying comment. No behavioral change (both folds must still beat raw). Files: `recalibration.py` (`fit_platt_chronological`, `refit_sport`, `seed_from_book_line_cache`).
- ✅ **P2e Platt seed warmup feature mismatch** — `seed_from_book_line_cache` now counts only current-season prior games via `count_current_season_games(prior_dates, sport_key, now=obs_date)`, matching runtime warmup blending (was `len(prior_games)`). Files: `recalibration.py`. Tests: `SeedWarmupSeasonCountTests`.
- ✅ **P2f Committed recalibration_*.json inert** — deleted `calibration/recalibration_baseball_mlb.json` + `recalibration_basketball_nba.json` (unvalidated seeds, already filtered out by `load_recalibration`). Deployed state now matches the README (recalibration activates only once validated fits accumulate). `git rm` staged; reversible via history.
- ✅ **P2g Duplicate prediction rows grow unbounded; full-blob rewrite per append** — `log_prediction_rows` now upserts by `prediction_identity`: newest forecast supersedes stale unresolved dupes, while graded outcomes + captured closing prices are folded forward (never clobbered); an already-resolved forecast is never overwritten by a re-log. Added `compact_prediction_log()` (collapses historical dupes; wired into `maintain_sport`, writes only when dupes exist). Files: `recalibration.py`. Tests: `test_prediction_log.py` (`UpsertDeduplicationTests`, `CompactionTests`).
- ✅ **P2h Cross-process closing double-spend** — the process-local `_capture_lock` prevents same-process double-spend; cross-replica double-spend (read-then-write, no atomic claim) is real but harmless on Streamlit Community Cloud's single-replica deployment. Documented the single-replica assumption + the exact ETag claim-then-fetch change needed before scaling. Files: `forward_tracker.py` (`capture_closing_odds` docstring).
- ✅ **P2i Broad/bare excepts swallow ESPN failures** — the two highest-impact data-ingestion swallow points now surface faults to stderr (fail-closed behavior preserved): `search_athlete` (keeps the site→web fallback chain, warns on each) and `get_athlete_gamelog` (narrowed to `RequestException`/`ValueError` + warn). Added `espn_client._warn`. Files: `espn_client.py`. Tests: `EspnFailureVisibilityTests`.
  - Note: `recalibration.py`/`app.py` best-effort excepts intentionally return safe defaults (`0`/`[]`) and are left as-is; they degrade gracefully by design.
- ✅ **P2j DNP/empty stat cells → 0.0** — generalized the reliability filter's "low-minutes" drop to a sport-aware **low-participation** drop (`prop_filter.py`): `_participation(game, sport_key)` returns minutes (NBA/NHL) or plate-appearances/at-bats (MLB, reading both runtime `_pa`/`_ab` and backtest `PA`/`AB`); `PARTICIPATION_FLOOR`/`PARTICIPATION_FRACTION` per sport. MLB uses **floor-only (fraction 0.0)** → drops only true DNP (0-PA) games, not 1-PA cameos. Deliberately did NOT change `_parse_stat_row` (empty→0.0) — too broad a ripple. NBA behavior byte-identical; NFL/pitchers unfiltered (no PA/AB signal). Files: `prop_filter.py`. Tests: `test_prop_filter.py`.
  - **Backtest re-validation** (MLB `batter_hits`, 2024, star pool): first cut (median-relative threshold 2.0) regressed MAE 1.118→1.143 by over-dropping 1-PA games and breaking streaks → **retuned to floor-only**. Final: MAE/RMSE **identical** (1.118/1.498), Hit% 53.79%→54.01%, only 6 fewer obs. Neutral on the (star-heavy) measurable pool by design — the fix's real benefit is for part-time players with genuine DNPs, who aren't in the default backtest pool (see survivorship item). Re-run: `PYTHONIOENCODING=utf-8 PYTHONUTF8=1 python backtest.py --mode props --sport mlb --season 2024 --props batter_hits --games-per-player 40 --variants all`.

- ✅ **P2k Survivorship bias in the NBA calibration pool** — replaced the hand-picked 18-superstar `DEFAULT_STARTERS["nba"]` refit fallback with a data-driven, usage-representative pool (mirrors MLB's `_mlb_player_pool`). `espn_client.list_season_athletes` paginates ESPN's free `statistics/byathlete` endpoint (columns mapped BY NAME; **fail-closed-as-a-whole** — any incomplete pagination returns `[]` so refit aborts loudly rather than fitting a scoring-biased page-1 subset). `refit_calibration._nba_player_pool` takes the top-N NBA players by TOTAL minutes (≥ `min_games`), and `espn_cache.seed_athlete_id` pins exact ESPN ids so the broad name list resolves to the right players (skips `search_athlete`'s lossy first-match). CLI: `--nba-max-players` (150), `--nba-min-games` (15). Files: `espn_client.py`, `espn_cache.py`, `refit_calibration.py`, `backtest.py` (comment). Tests: `test_calibration_pool.py` (14).
  - **Adversarial review** (10-agent workflow) confirmed + fixed 3 findings: pagination not truly fail-closed (page-1-only is scoring-biased and still overfills the pool → now returns `[]` on any incomplete/errored fetch); pre-sorted test fixture (sort not actually exercised); dedup test never verified which id was pinned. 3 other findings correctly refuted by the verify pass.
  - **Deployment** (`--sport nba --season 2026 --prior-season 2025`): pool **18→150** players, n_obs **980→8935**; P2j now engages on NBA (334 DNP/low-min drops across 125 players vs 35/15 on the star pool). Brier is honest-representative: player_points 0.247→**0.2433** (C, confirmed, better outright), player_assists **0.2448** (C, confirmed), player_rebounds **0.2484** (A empirical fallback — the P1 star numbers 0.2423/0.2353 were survivorship-optimistic on 980 star obs). Prior file backed up to `basketball_nba.json.p1bak`.
  - Re-run: `PYTHONIOENCODING=utf-8 PYTHONUTF8=1 python refit_calibration.py --sport nba --season 2026 --prior-season 2025`.

- ✅ **P2l Savant team-key mismatch silently disabling the MLB ensemble** (`mlb_starters.py`) — the expected-runs challenger keyed offense/bullpen dicts by the team string Savant returns (`player_name` under `group_by=team`) but the consumer looked them up by the StatsAPI `abbreviation`. A **live probe confirmed the namespaces diverge for Oakland** (StatsAPI `OAK` vs Savant `ATH`, the Athletics rebrand) — so every Oakland game silently got `expected_runs.complete=False`, 29/30 teams matched, Oakland failed with zero visibility. Fix in `get_expected_runs_team_factors`: `_canonical_team_key` normalizes Savant keys into the season's StatsAPI-abbr namespace via `_SAVANT_TO_STATSAPI_ABBR = {"ATH":"OAK"}` — **team-index-aware so it can't mis-remap a future season** where the sources agree; a fail-visible `_warn` (new helper) fires on any residual coverage gap (unmapped Savant key = likely new rename, or a StatsAPI team with no Savant data). Cache key bumped `v1`→`v2` so stale raw-key caches aren't served. Consumer unchanged. Re-probe verified: `OAK` now resolves, 30/30 covered, no warning. Files: `mlb_starters.py`. Tests: `test_p2_finish.py` + hermetic fix to `test_modeling.py`.
- ✅ **P2m Athlete-name collisions surfacing the wrong player** (`espn_client.py`, core money path) — the live projection path `get_player_stat_history`→`search_athlete` took the **first of up to 5 name matches with no team filter**, so a common name ("Will Smith" — **live probe: 8 cross-sport + 2 MLB matches**) could resolve to a different-team / different-sport / retired athlete whose stats then got projected onto a bet. Fix: `search_athlete(..., team_ids=None)` now prefers a candidate on one of the **matchup's two ESPN team ids** (via nested `_pick`), falling back to the first result when no hint/no match (**never worse than before**); `get_player_stat_history(..., team_ids=None)` forwards it; `app.py` + `main.py` derive the game's two team ids (`find_team(espn_teams, home/away)`) and thread them. Team resolution works for MLB via the web-search fallback's `teamRelationships` (verified live: `search_athlete` returned the right `team_id`); team preference is confined to sport/league-matched candidates so a cross-sport team-id collision can't surface the wrong athlete. Files: `espn_client.py`, `app.py`, `main.py`. Tests: `test_p2_finish.py`.
  - **Adversarial review** (7-agent workflow): 1 confirmed finding (web-fallback branch — the real MLB path — had no success-path test) → fixed by adding web-fallback tests on the real payload shape + a small hardening (don't team-prefer across a cross-sport list when no sport/league match). 2 findings correctly refuted (a pre-existing uncached-`None` re-fetch nit; a duplicate of the same test-gap).
  - **Documented residuals (not fixed — lower value / rare):** (a) **cross-event namesake** — app.py keeps global `(player,prop)` fetch dedup, so two *different* players with the same odds-API name in two *different* games on one slate still collapse to one lookup; (b) **grading path** — `cached_athlete_id` (recalibration / book_line outcome resolution) is still name-only, but a wrong athlete there resolves *consistently* wrong (grading, not surfaced bets), and the refit pool already pins exact ids via P2k's `seed_athlete_id`.

## P3 — Structural / lower priority (✅ DONE — full split + correctness + hygiene)
- ✅ **God-module split** — `analysis.py` **2806 → 938 lines**, split into four focused modules behind a backward-compatible re-export facade (a pure, behavior-preserving move, green after every sub-step): new leaf `stats.py` (normal CDF/PPF, weighted stats, recency), `pricing_common.py` (odds/EV math, calibration glue, shared venue/defense multipliers + the calibration caches + `_starter_adjustment`), `parlay.py` (Gaussian copula + parlay search), `props.py` (`analyze_player_props_value` + prop knobs/multipliers). `analysis.py` keeps margin/ML/totals/spreads + formatters + `make_bet_checklist_entry` + `_apply_starter_logit` + `_mlb_expected_runs_projection`. Layering is acyclic (`stats` ← `calibration_loader`; `stats/calibration_loader/odds_client` ← `pricing_common` ← `props`,`parlay`,`analysis`). `test_module_split.py` locks the contract: clean fresh-interpreter import of each module (cycle guard), facade completeness for all 8 importers, `_norm_cdf` single-object dedup, shared-cache-object identity. **DEFERRED (explicitly out of scope this pass):** `backtest.py` (3282) and `app.py` (2394) splits, and app's fetch/analyze pipeline pure-function extraction.
- ✅ **Latent duplication drift** — `_norm_cdf` canonicalized in `stats.py`; `calibration_loader` + `analysis` now import it (identity-verified single object). Dead `analysis._opponent_strength_multiplier` (0 callers) deleted; `test_multipliers.py` characterizes `backtest.opp_strength_mult(p, 0.5) == 0.5 + clamp(p)` to lock the documented equivalence to the removed formula (the live parameterized version stays in `backtest`).
- ✅ **Copula non-PSD shrink now visible** — `parlay._make_psd_cholesky` returns `(L, applied_shrink)`; `_gaussian_copula_joint_prob`/`_copula_joint_hit_prob` take `return_shrink=` (default still returns a scalar — test_money_math unaffected); `generate_parlays` surfaces `correlation_shrink` + `correlation_degraded` on the output dict, so a silent collapse-to-independence is no longer indistinguishable from a legitimately uncorrelated parlay.
- ✅ **Unbounded multiplier bounded** — the inline unbounded `output_def_mult` (could go negative or huge on sparse `opp_pa` / calibrated `strength > 1`) extracted to `props._output_defense_multiplier(...)` clamped to `[0.5, 1.5]` (mirrors `_opponent_defense_multiplier`). With `matchup_mult ∈ [0.7,1.4]` and `lineup_mult ∈ [0.8,1.2]`, `combined_mult` is now bounded `~[0.28, 2.52] (> 0)`, so `effective_line = line / combined_mult` stays finite.
- ✅ **Value flag on missing price** — the real EV bug fixed: `generate_parlays` now **excludes** value/`safe_value` combos containing a leg with `odds_price is None` (previously priced at a fabricated −110 / 1.91 that could clear the EV gate on a price the book never offered). Single-bet totals/spreads got additive `over_price_missing`/`under_price_missing`/`price_missing` display flags. **Residual (documented, cosmetic):** the single-bet *display* still shows a 0.50-devig placeholder edge for a fully-missing-price side — but the `_prop_is_value`/`_expected_roi` gate already prevents such a bet from ever being surfaced as value, so this is display-only; making the shown number `None` would ripple into ~6 f-string sites for no money impact.
- ✅ **Misc hygiene** — UI cache text "10 minutes" → "60 minutes (1 hour)" (actual TTL is 3600 s); `bet_selection:*` session keys now `st.session_state.pop()`'d instead of `= False` (the old reset defeated Streamlit's widget GC → unbounded growth) with the "Clear selected bets" button converted to an `on_click` callback so the pop is safe; dead `@st.cache_data` wrappers (`fetch_event_odds_cached`, `fetch_team_schedule_cached`, `fetch_player_history_cached`) **and** the dead `app.py` `build_team_stats` that called one of them deleted; naive `datetime.utcnow()` provenance writes → `datetime.now(timezone.utc)` (byte-identical `...Z` output) in `nfl_epa.py`, `backtest.py`, `calibration_loader.py`, `historical_odds.py`.
- ✅ **Windows console UnicodeEncodeError** — new `cli_encoding.configure_stdio()` reconfigures stdout/stderr to UTF-8 (`errors="backslashreplace"`), wired into the `__main__` block of all 13 CLI tools. Proven under a simulated `cp1252` console (raw print raised `UnicodeEncodeError`; after `configure_stdio` it does not). Retires the `PYTHONIOENCODING=utf-8 PYTHONUTF8=1` prefix for running the CLIs.
- **Adversarial review:** 4-dimension workflow (split parity/facade, correctness, import-cycle/cache identity, hygiene) → adversarial verify. **0 confirmed findings** (all four dimensions clean).

---

## New files added
- `config.json.example`, `test_money_math.py`, `test_calibration_refit.py`, `test_reliability.py`, `test_prediction_log.py`, `test_prop_filter.py`, `test_calibration_pool.py` (P2k), `test_p2_finish.py` (P2l/P2m), `.github/workflows/tests.yml`, this file.
- **P3 split modules:** `stats.py`, `pricing_common.py`, `props.py`, `parlay.py` (extracted from `analysis.py`); `cli_encoding.py` (UTF-8 CLI stdout).
- **P3 tests:** `test_module_split.py` (facade/cycle/cache-identity contract locks), `test_multipliers.py` (opp_strength characterization), `test_p3_correctness.py` (clamp / copula shrink / missing-price parlay).

## Files deleted
- `calibration/recalibration_baseball_mlb.json`, `calibration/recalibration_basketball_nba.json` (P2f — inert unvalidated seeds).

## Git state
- Working on branch `main`. Changes are in the working tree, **not committed** (no commit made unless the user asks). `config.json` staged-deleted (untracked going forward).
