# Python
__pycache__/
*.pyc
*.pyo

# Editor swap files
.*.swp
.*.swo

# Streamlit secrets (only the example is committed)
.streamlit/secrets.toml

# Local runtime config holding the API key (only config.json.example is committed)
config.json

# Runtime caches and logs (populated by the app at run time, not source)
cache/

# Local odds-warehouse fallback (durable copy lives in Azure Blob at run time)
warehouse/

calibration/archive/

# Local-only sql scripts
clear_tables.sql

# Web resources for Claude
.webresources

# Overfit experiment registry (runtime append-only log)
experiment_registry.jsonl

# Cloned reference app (separate git repo, mined for ideas; not part of this repo)
.baseballpredictions/

# Derived clean-odds backtest store (rebuilt by build_clean_odds_store.py from the
# committed api20XX source stores + the warehouse; regenerable, so not committed).
historical_odds/*__apiclean.json

.feature_cache
historical_odds/*.json
cache.zip
warehouse_mirror_data/
backtest_cache/
