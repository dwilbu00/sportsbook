# Copy this to secrets.toml for local testing, or add in Streamlit Cloud dashboard
ODDS_API_KEY = "your_api_key_here"

# Optional durable shared prediction log. Use a private Azure Block Blob SAS URL
# with read, create, and write permissions. Keep the SAS token secret.
# PREDICTION_LOG_BLOB_URL = "https://ACCOUNT.blob.core.windows.net/CONTAINER/prediction_log.jsonl?SAS_TOKEN"
