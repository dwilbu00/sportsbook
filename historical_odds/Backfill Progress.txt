# CLOSE (default snapshot)
python pull_multibook_odds.py --sport mlb --season 2024 --max-credits 500000 --dry-run
python pull_multibook_odds.py --sport mlb --season 2025 --max-credits 500000 --dry-run
python pull_multibook_odds.py --sport mlb --season 2026 --max-credits 500000 --dry-run
# EARLY (fixed ~9am ET morning line)
python pull_multibook_odds.py --sport mlb --season 2024 --snapshot early --max-credits 500000 --dry-run
python pull_multibook_odds.py --sport mlb --season 2025 --snapshot early --max-credits 500000 --dry-run
python pull_multibook_odds.py --sport mlb --season 2026 --snapshot early --max-credits 500000 --dry-run


Deep base recal — PROPS, 2024-2026 (you run; I review the --diff before promote)
Step 1 — full synthetic sweep (methods + knobs: park/reliability/opp-def/half-life/shrinkage; the full 576-variant grid = the deep version). Stages a candidate; live untouched:


python refit_calibration.py --sport mlb --seasons 2024,2025,2026
⚠ This is the heavy one — the full grid is single-threaded (GIL) and the 3-season pool hit ~10GB RAM last time; expect a long run. Your 24-core box has the RAM; it's wall-clock that's the cost. (If it's painful, --focused-grid is ~37 variants / ~15× faster for iterating — but you wanted the full/deep version, so no flag = full.)

Step 2 — re-select methods at REAL book lines (composes onto Step 1's candidate; this is where the DH recovery + the new 1.5 buckets pay off):


python refit_calibration.py --sport mlb --real-lines --seasons 2024,2025,2026 --xstats-strength 0.75
(--xstats-strength 0.75 matches the shipped batter_hits xBA-D weight so the D bucket serves what it's fit under.)

Step 3 — review (paste this; I read it before anything goes live):


python refit_calibration.py --sport mlb --diff
I'll check: the method changes per prop, whether RBI/TB now adopt a distinct 1.5-line method (the whole point of the bucket + DH work), and — critically — that value_gate.cv_floor is preserved in the candidate (the props refit only rewrites the props block; save_calibration preserves other blocks, so the earned_runs CV gate should carry through — but I want to see it, not assume it).

Step 4 — promote (only after we're happy with the diff):


python refit_calibration.py --sport mlb --promote
Step 5 — fresh Platt seed on the NEW base (clean slate so no stale fit carries over; auto-resolves the batter_hits keep/drop question):


rm calibration/recalibration_baseball_mlb.json
python recalibration.py --sport mlb --seed --seasons 2024,2025,2026
(Season filter + merge are already in; reversible via git if anything looks off.)

Step 6 — validate the new base: a props-odds 2026 OOS run to confirm accuracy held, and re-check the earned_runs CV≥1.3 band still prints (the edge should be stable — CV is from the raw series — but we confirm on the new base).

Two notes:

This refits props only. Team markets (prob_shrink/blend/expected_runs) are a separate backtest --mode odds --write-calibration refit — say the word if you want those redone on 2024-2026 too, but that's an independent run.
Nothing here touches live until Step 4, and I gate the promote on reviewing Step 3.
Kick off Step 1 whenever you've got the machine time — it's the long pole. Paste the sweep summary + the --diff when they're done and I'll take it from there.