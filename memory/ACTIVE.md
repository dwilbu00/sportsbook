---
name: active
description: In-flight work only — what's being worked on now, linking to the domain files. Move an item to its domain when done; do not let this file accrete history.
metadata:
  type: project
---

# ACTIVE — in-flight work

One line per open item. When an item ships, delete it here and fold the durable outcome into its `[[domain]]` file (result-supersedes-idea). Last reconciled **2026-09-08** (pruned the completed MLB precise-backfill + Steps-1-4 sagas into the domain files; NFL is now the focus).

> **▶▶ RESUME (2026-09-08):** **NFL is the ACTIVE sport.** MLB is parked/efficient — every candidate edge dissolved; lone survivor `batter_strikeouts UNDER 1.5` (+1.26%, thin). NFL clean-slate data foundation on **nflverse** (`nflreadpy` offline→mirror; dep-free runtime). **Phases 1-3 DONE + edge sweep COMPLETE — NFL is efficient-for-us on every front** (flat-side, situational, props all null/artifact; **Phase-3 QB-aware model** = a REAL model improvement but NOT a betting edge: base team-EPA loses to the close everywhere, +QB-as-of layer beats base and reaches ~breakeven on QB-change games (+1-3% ATS) but t≈0.3, fails per-season replication (2024 red), no CLV early — the market prices the QB; the model gets us TO its level, not past it). **DECISION POINT for Doug:** (a) ship the QB layer into the LIVE NFL model as a prediction/calibration improvement (candidate-staging), (b) forward-track the thin QB-change ATS lead at token stakes, (c) accept efficient + consolidate. All work on `origin/main`. Coherence RL live gate = PAUSE; cv_floor monitor = re-check/pause.

## Ongoing monitors (accumulate + watch)
- **⛔ Coherence run-line — PAUSED (dead on clean data).** Devigged bands + clean corpus → in-band −3.06%, stable −4.47%, replication FAIL; the +11% was a raw-band/dirty-capture artifact. Live `coherence_flags` cv_max=1.0 gate bets a −EV population → do not run. Full tombstone → [[edges-and-backtests]].
- **`batter_strikeouts UNDER 1.5` — the one clean-data MLB survivor** (+1.26% t=1.69, replicates every season; inverted public over-bias). Bet flat at DK/FD only when the UNDER is ≥ ~−110; small stakes, do NOT scale. → [[edges-and-backtests]].
- **⚠ cv_floor (earned_runs E + CV≥1.3) — SUSPECT, re-check or pause.** Never re-validated on the clean corpus; every sibling edge died on clean data. Re-run before any stake. → [[edges-and-backtests]].

## ▶▶ NFL BUILD (the current focus)
- **DATA ARCHITECTURE (decided):** canonical = **nflverse** (free, CC-BY). Ingest OFFLINE via **`nflreadpy`** (`nfl_data_py` is deprecated; polars, `load_*`) → **mirror parquets** in `warehouse_mirror_data/` (LFS-shared). Runtime (app + backtests) never imports nflreadpy/polars — reads the mirror, or self-maintains **dep-free fetch-with-TTL** (no Azure for NFL — nflverse IS the maintained public source; no scheduler needed, mirror is an LFS stub on Streamlit → CSV fallback). REUSE `nfl_epa.py` (team EPA, leakage-safe as-of) + `game_results.py` (ESPN live grading).
- **SPINE = nflverse `game_id`** (`{season}_{wk}_{AWAY}_{HOME}`) — one key across schedules/pbp/player-week/NGS (zero fuzzy matching) + `espn` id crosswalk. games.csv = `raw.githubusercontent.com/nflverse/nfldata/master/data/games.csv` (all seasons, `game_type∈{REG,WC,DIV,CON,SB}` — no PRE → preseason can't leak).
- **✅ PHASE 1 DONE (d86b50b):** `nfl_schedule.py` (spine, mirror-first/CSV-TTL; `resolve_events()` fail-closed join = static 32-team map `nfl_epa.NAME_TO_ABBR` + `season_for_date` + date tiebreak; 6 integrity checks) + `nfl_ingest.py` (nflreadpy → nfl_game__ parquet). **AUDIT: 837/837 odds events → one game_id, 0 collisions, 0 unresolved.** Odds mirror: 2023-2026, 3 windows, ML/spread/total, full team names.
- **✅ PHASE 2 DONE (c14f68f):** `nfl_market_scan.py` flat-side scan (ML/spread/total sides, devigged bands, per-season). **No clean flat-side edge** — pooled sides negative (fav-longshot bias visible but priced); candidates weak (n<180, t<2, no mechanism). One note-don't-trust: moderate favorites 0.60-0.65 devigged (+10-14%, t≤2.16). ⇒ NFL edge (if any) is MODEL or PROPS or a specific bias, not raw structure.
- **✅ PHASE 2c — PROPS scan DONE (746d3b7, 2026-09-08) = efficient/artifact.** `nfl_props_scan.py`: flat-side per-market grade (prop `player`→player_week via normalized name + spine (season,week)) + **line-conditional cut** (`--by-line`, per market×side×line-bucket — where MLB's edge hid) + **`--drill prop:SIDE:bucket`** deep-dive (price-band ROI + **survivorship probe**). RESULT: huge recreational OVER-bias (rush_yds/receptions OVER −8 to −12%, t≈−8) but **vig-eaten**; rushing UNDERs breakeven-closest. Line-conditional surfaced ONE apparent candidate `player_receptions OVER 0.5` (+14.76% t=2.60, +ROI all 3 seasons) — **KILLED by the drill: all 31 dropped bets in the cell are SCRATCHES** (rostered that season, inactive that week) = phantom OVER losses the name-join silently excludes; grading them losses → **−3.03%**. Pure DNP-survivorship artifact (same class as dead MLB edges). ⚠ REUSABLE LESSON: low-line OVER props are inflated by dropped scratches, low-line UNDERs deflated — a pretty price-band curve ≠ clean corpus. (FIX-if-ever-used: grade rostered-but-inactive players as 0 via the roster layer, don't drop.)
- **▶ PHASE 2b — ANCHORING-BIAS edge test (NEW LEAD, 2026-09-08).** Paper "Anchoring Bias in the NFL Gambling Market" (Patterson/Shank/Fodor, SSRN 2025, N=5,088, 2003-2023): bettors anchor to **pre-season Super Bowl odds** all season (over-bet the preseason favorite: +7-8.5% home bets when big preseason fav, −23% in Week 1 when preseason longshot). **The over-betting is INVERSELY related to WEEK-1 ATS profitability → fade the preseason-anchored favorite ATS in Week 1.** Profit relationship is **Week-1-ONLY** (bias persists all season but no season-long profit); sportsbooks also anchor (closing lines carry preseason odds). TEST: build a small **preseason-SB-futures layer** (PFR — paper's source; or Odds-API futures) → Week-1 fade-the-preseason-favorite ATS backtest off the spine. ⚠ Week-1 only = ~16 games/yr → our 3-4 seasons ≈ 50-64 games (too thin to prove; lean on the paper's 20-yr prior + forward-track). Best NFL lead we have — market-structure, literature-backed. Ref: `.referencematerial/Anchoring Bias in the NFL Gambling Market.pdf`.
- **✅ PHASE 3 DONE (2026-09-08) — QB-aware model built + OOS-tested = real model lift, NO betting edge.** Commits 1174965 / 2a897fa / 7329130. Steps:
  - **✅ pbp mirror dep-free:** `nfl_epa.load_plays` now reads the pbp MIRROR parquet first (via `nfl_data.pbp`, pandas-only, same game_id spine) — kills the ~19MB/season live download; falls back to trimmed-JSON→live. `test_nfl_epa.py` = leakage guard (as-of counts exactly plays strictly before cutoff) + abbr parity (pbp+spine) + mirror-is-source; **ALL PASS**. 2024 = 35,082 REG plays, top net-EPA BAL/DET/PHI/BUF/GB (correct).
  - **✅ QB as-of layer (`nfl_qb_asof.py`):** leakage-safe per-passer dropback-EPA/play (as-of, shrunk <250 db, prior season folded in), game-actual starter (public pre-game = CEILING test; live path would use depth+injury), QB-delta = starter-vs-team-baseline-QB (≈0 usual, negative on backup). ⚠ BUG FIXED: passer_player_id NaN is truthy in Python → guard explicitly. Validated: 2024 top QBs Lamar/Goff/Allen; nails Rattler-for-Carr, Rush-for-Dak, Dobbs-for-Purdy; 279-281 QB-change team-games/3yr.
  - **✅ OOS margin test (`backtest_nfl_epa --oos-qb`):** leave-one-season-out. +QB lowers OOS margin RMSE ALL 3 folds (13.494→13.431 wtd; −0.164 on QB-change games), w_qb stable ~12-15. Real OOS predictive lift.
  - **✅ beat-the-close (`nfl_qb_edge.py`):** OOS model vs DK closing spread/ML, QB-change slice isolated. Base team-EPA LOSES everywhere (−6 to −13% ATS, every season = efficient market); +QB beats base and flips QB-change games to ~breakeven (+1.3/1.6/2.8% ATS at thr 1/2/3) — BUT t≈0.2-0.4, NOT +every season (2024 red, pooled + carried by 2025), early_12h no better (not a CLV story). ⇒ market prices the QB; the model reaches its level on QB-change games, doesn't beat it.
  - **OPEN DECISION (Doug):** (a) ship QB layer LIVE (candidate-staging via `backtest_nfl_epa --save` for the EPA weight; QB weight needs a save path — `_fit2` in backtest) as a prediction/calibration win; (b) forward-track QB-change ATS at token stakes; (c) consolidate. Not auto-shipped — awaiting call.
- ~~**▶ PHASE 3 — NFL TEAM MODEL + honest refit** (superseded by ✅ above; original priorities kept below for the not-yet-done items)~~ Priorities:
  - **(MUST) QB as-of layer — the biggest modeling gap.** `nfl_epa` is TEAM-level; NFL is QB-dominated and the model has no notion of who's starting (backup/injury/mid-season change → team rating wrong). Build a starting-QB as-of adjustment (depth charts + injuries + player-week QB EPA) — the analog of MLB `pitcher_asof`; most likely source of a REAL model edge.
  - **(MUST) Fix the calibration BUG:** `calibration/americanfootball_nfl.json` has `player_pass_yds` ≡ `player_rush_yds` (byte-identical residual_mu/sigma/ecdf — placeholder). Props section is a toy; refit from a real player-week layer.
  - **(MUST) Mirror the pbp:** `nfl_epa` fetches ~19MB/season live at runtime (no parquet) — breaks the dep-free pattern. Add `--pbp` to `nfl_ingest` → `nfl_pbp__…{season}.parquet`; make `nfl_epa.load_plays` mirror-first. (Widen the trim: down/ydstogo/yardline_100/air_yards/wp/weather/roof for later situational splits.)
  - **(MUST) OOS refit:** `backtest_nfl_epa` fits IN-SAMPLE (weight 32.293, corr 0.38, no split) → walk-forward/chronological split. And there's **no NFL `--mode odds` path** to fit prob_shrink/blend honestly — build it or generalize MLB's `--sport nfl`. Grade vs `nfl_schedule.team_scores_index` (the spine). Refit prob_shrink/blend at CLOSING like MLB Steps 2-3.
  - **(MUST) Integrity/leakage tests** (no Azure parity net for NFL): **abbreviation-parity** (LA vs LAR drift → silent Rams drops!) = `nfl_epa.TEAM_ABBR_TO_NAME` must equal distinct teams in games.csv AND pbp; a **leakage** unit test (team_epa as_of excludes game_date≥as_of); **spine-completeness** (285/season); promote the join audit to CI (0 collisions, 0 unresolved); ingest-time `--validate` + data-version stamp.
  - **(NICE) future-proof layers** (all keyed to game_id, per-season parquet, offline via nflreadpy): player-week stats, rosters/depth/injuries (avoid grading inactive players), **game-context (rest/short-week/bye/travel/primetime — FREE from the spine, historically the most robust NFL angles)**, weather/roof (in pbp), NGS aggregates (CPOE/time-to-throw/separation — raw tracking NOT public/needed), situational/down-distance EPA splits (PROE, red-zone, 3rd-down). Also: in-process cache TTL for the live season; decide REG-only vs playoff-inclusive EPA.
- **✅ FULL DATA MODEL BUILT (2026-09-08) — "everything modeled datawise" (Doug's directive), edges next.** `nfl_ingest.py` now ingests ALL layers via nflreadpy → mirror parquet (per-season): **pbp** (widened: down/distance/field/air_yards/epa/wp/cpoe/success/weather/roof + play-ids), **player_week** (props + player model), **rosters/depth/injuries** (QB-as-of + inactive guards), **NGS** passing/rushing/receiving, **team_week**. Generic per-season runner off the exact nflverse dictionary schemas. FREE **`--validate`** (dep-free): abbreviation-parity (LA-vs-LAR silent-drop guard), no-PRE, game-counts, game_id-in-spine, player_id-non-null — spine already PASSES. `nfl_data.py` = mirror-first LFS-stub-safe readers (pbp/player_week/rosters/depth/injuries/team_week/ngs) + **`game_context()`** derived from the spine alone (rest/short-week/off-bye/rest-edge/division/primetime — leakage-free, 0 extra data; verified 855 games: 293 div / 183 primetime / 58 short-week / 127 off-bye). As-of aggregation stays the consumer's job (nfl_epa pattern).
  - **ON DOUG'S BOX (materialize the data):** `pip install nflreadpy` → `python nfl_ingest.py --all --seasons 2023,2024,2025,2026` → `python nfl_ingest.py --validate --seasons 2023,2024,2025,2026` (expect ALL PASS) → re-commit the new `nfl_*__americanfootball_nfl__*.parquet` (LFS). Then `python nfl_data.py --layers` shows row counts.
- **▶ AFTER DATA MATERIALIZED — predictive modeling + edge ID (Doug's plan):** (1) migrate `nfl_epa.load_plays` to read the pbp mirror (dep-free/0-network) + add the as-of leakage unit test; (2) build the QB-as-of layer (depth+injuries+player-week QB EPA → starting-QB rating); (3) OOS-refit `backtest_nfl_epa` (in-sample today) + build/generalize an NFL `--mode odds` path for prob_shrink/blend; (4) situational/market-structure + props edge scans on the full data model (game_context + player_week), honest devigged/per-season bar.
- **HOW TO RUN / RESUME:** spine audit `python nfl_schedule.py --audit --seasons 2023,2024,2025,2026` (837/837); market scan `python nfl_market_scan.py --seasons 2023,2024,2025 --snapshot closing`; anchoring `python nfl_anchoring.py --season 2026 --week 1 --min-gap 0.04` (Phase-2b, PARKED — Week-1-only, thin; 2026 SB odds fetched, in `calibration/nfl_preseason_sb_odds.json`).
- **FILES:** NEW `nfl_schedule.py` (spine+join) / `nfl_ingest.py` (all-layer ingest) / `nfl_data.py` (readers+context) / `nfl_market_scan.py` / `nfl_anchoring.py`. REUSED `nfl_epa.py` / `backtest_nfl_epa.py` / `game_results.py` / `analysis._predict_margin` / `r2_sharp.fair_two_way`. Same devigged/clean-data/per-season honesty bar as MLB.
- **REFERENCE MATERIALS (added 2026-09-08, `.referencematerial/`):** `nflverse dictionaries/` (field glossaries: pbp/player_stats/schedules/rosters/depth_charts/injuries/nextgenstats/participation/snap_counts — the layer-build reference), `nflverse_readme.md` (nflreadpy API), `nflfastR EPA and WP methodology.html` (keep nfl_epa faithful), `Anchoring Bias in the NFL Gambling Market.pdf` (Phase-2b lead). Betting-theory refs (Clarke devig→fair_two_way, Kelly→shipped, Logic-of-Sports-Betting→CLV thesis) already applied.

## Open review findings
- **[OPEN, medium] warehouse_mirror live-season staleness** (`warehouse_mirror.py`): `ensure()` fast-path returns instantly when all files `_valid` but never re-syncs the CURRENT season (mutable) → backtests can read stale current-year data. MITIGATION: `--refresh-mirror` when current-season freshness matters. FIX: exclude the live season from the `_is_valid` fast-path + force-refresh its per-season files. (Related fixes this session: `--mark-valid`, sport-key normalization, verify-no-files guard.)

## MLB — DONE + parked (tombstones; detail folded to domain files)
- **All MLB edges dissolved on the clean corpus (2026-09-05).** Steps 1-4 done: CLV (execute early_4h/calibrate closing), props recalibrated (batter_hits D+xBA(1.0)), team refit (well-calibrated, no edge), market-structure (coherence/under/f5 all DEAD). Lone survivor batter_K UNDER 1.5. Full results → [[edges-and-backtests]] + [[modeling-and-calibration]].
- **Precise 5M-credit backfill + warehouse reload + mirror = DONE + VERIFIED (2026-09-03).** Warehouse now holds ONLY uniform −12h/−4h/close windows (`source`=role): mlb 54,678 / nba 19,708 / nfl 4,164 snapshots, 0 legacy. Backtests + calibration read the mirror 0-DTU. Detail → [[data-and-architecture]].
- **Recal stale-fit bug fixed + operator rule** (purge stale bare SQL Platt keys after any method change) → [[modeling-and-calibration]]. OPEN hardening (deferred, needs sign-off): make `props._resolve_recal_cfg` not fall back to a bare key for a line_methods prop (a tested behavior) + seed-merge drop bare keys → [[wishlist]].

## [[modeling-and-calibration]] — parked calibration ideas
- **value_gate auto-tune** — fold a confirmation-gated `--gate-diag` selection into the offline refit as advisory + `--promote` (never live-auto). `refit_calibration` still never calls `save_value_gate`.
- **EV floor 4%→3% for DK/FD** — `value_gate.ev_floor` still 0.04 (chosen on optimistic consensus prices); A/B 4% vs 3% on real DK/FD fills via `--gate-diag`.
- **Pitcher props: seed Platt + exit synthetic-line fits** — grow pitcher REAL-line obs + enable online Platt (zero fits today) → closes the forward-Brier gap. (MLB — deferred while NFL is active.)
- **Online Platt Blob-mode hardening (low pri)** — seed-pristine invariant + per-key merge are SQL-only; mirror into the Blob branch before any non-SQL deploy.
