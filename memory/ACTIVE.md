---
name: active
description: In-flight work only — what's being worked on now, linking to the domain files. Move an item to its domain when done; do not let this file accrete history.
metadata:
  type: project
---

# ACTIVE — in-flight work

One line per open item. When an item ships, delete it here and fold the durable outcome into its `[[domain]]` file (result-supersedes-idea). Last reconciled 2026-08-28 (verified against code + owner-confirmed prod state).

## Ongoing monitors (not tasks — just accumulate + watch)
- **⛔ Coherence run-line — PAUSED (2026-09-05): edge did NOT survive honest re-validation.** On DEVIGGED-fair bands (the basis the live gate actually uses, fixed in 3ac3003) + the CLEAN precise closing corpus: pooled in-band ROI **−3.06%** (t=−1.35), stable-SP(<1.0) in-band **−4.47%** (t=−1.70), per-season replication **FAIL** (2024 −9.2% t=−2.66). The prior "+11%" was a RAW-implied-banding + old-biased-capture ARTIFACT. The live `coherence_flags` cv_max=1.0 gate bets exactly this (devigged in-band) population = negative EV → **PAUSE the live RL bets.** Supersedes the ★★ coherence claim in [[edges-and-backtests]] — do NOT rely on it. (Structural cross-market bet, not timing → early_4h won't resurrect it; test deprioritized.)
- **batter_strikeouts UNDER 1.5 — the one clean-data MLB survivor** (+1.26% t=1.69, replicates every season; inverted public over-bias on batter-K overs). Bet flat at DK/FD only when the UNDER is priced ≥ ~−110 (thin edge, price is everything). Small forward-track, do NOT scale. [[edges-and-backtests]]
- **⚠ cv_floor forward-monitor (earned_runs E + cv_floor 1.3) — SUSPECT, re-check or pause.** NOT re-validated on the clean corpus this session; given every other MLB edge dissolved on clean data, do not assume it survives — re-run before any further stake. [[edges-and-backtests]]

## [[edges-and-backtests]]
- **prop_roi RUN (2026-08-28) → over-bias confirmed but vig-eaten; 1 marginal cell.** Verdict in [[edges-and-backtests]]: only `batter_strikeouts UNDER 1.5` (+2.2%, t=1.93, replicates) crosses +EV; ER UNDER 2.5 breakeven; everything else vig-negative. OPEN owner decision: forward-track K-under-1.5 small (borderline, clears the coherence t≈1.66 precedent) or record + move on. Don't build a system on it.
- **Line-timing DONE (2026-08-31) + precise-window CLV follow-up DONE (2026-09-03) — results in [[edges-and-backtests]].** ML: bet EARLY (~2% CLV paired, `+++` 3/3) — now INDEPENDENTLY confirmed by the outcome-free `clv_window_study.py` on the clean corpus. RL/totals (was unanswerable offline) NOW ANSWERED: they move meaningfully (MLB RL 3.15%@12h prob; NBA/NFL sides&totals move ~1-1.8 pts as the NUMBER); vig is FLAT across windows (no penalty for early); signed drift ≈0 (CLV needs model edge, not timing alone). **Transaction rule: execute at early_4h; calibrate/validate vs closing.** OPEN follow-ups: (1) adopt the ML-early timing rule in the live workflow; (2) **coherence RL timing (Doug's plan 2026-08-31): NO code needed now — the auto-log already banks the EARLY side** (`recalibration.build_market_prediction_rows` runline_coherence rows carry `ts`=analysis time, `price`=early dog+1.5 price, `point`, `commence_time`, event identity). Let it accumulate; WHEN sample sufficient (~few hundred flagged games), do a ONE-TIME **targeted** Odds-API historical pull of CLOSING dog+1.5 for only those game_pks (small confirmed spend, not a full backfill) + a CLV grader → per-bet early-vs-close ΔROI on the REAL live edge. Streamlit has no scheduler, so this defers the paid close capture instead of cron-ing it. Parked directions still open: bet-disqualification (partly folded into datamine). Monte Carlo = no-go.
- **Upset datamine — RAN (2026-08-31): no new schedule/rest/travel/DH edge; one SUSPECT totals signal.** graded 2,534 games. dog_ml schedule ★s were false leads (t~0.5, just the tiny +0.95% base re-sliced → ★ criterion tightened to require pooled t≥1.5, commit ebcb9b4). dog_rl ★s = the known coherence dog+1.5 edge re-confirmed (+4.08% t=2.33), no factor concentrates it. **Umpire untestable** (each ump ~27 games/3yr < min_n → batch-2 = pool by leakage-safe ump over-rate tendency). **STRIKING but SUSPECT: `total_under` +5.19% t=2.81 3/3 (vs `total_over` −13.89%; sum ≈ −vig, i.e. the under IS the inverted over-bias, clearing vig because the over-bias is huge).** Likely a STALE-HIGH captured-total artifact (our team snapshot is hours pre-commence; not DK-internal so timing doesn't cancel; contradicts the well-powered "totals abstain" prior). **`under_timing` scenario BUILT (commit 64ec676) to settle it — RUN: `python scenario_backtest.py --scenario under_timing --seasons 2024,2025,2026`.** Buckets over/under ROI by the total snapshot's lead-to-first-pitch. **RAN (2026-08-31) — pushes AGAINST the artifact, NOT confirmed.** KEY: we have ZERO total captures inside 12h (all totals ≥12h pre-commence; no_total=1,498) → true close unobservable offline (same wall line_timing hit). Within observable range the under edge is STRONGEST at the CLOSER bucket (12-24h +9.85% t=4.99 [+++]) and nearly gone at the staler one (>=24h +1.13% t=0.49) — the OPPOSITE of the stale-high-line prediction, i.e. it looks like a real edge strengthening toward close. Pooled under +5.94% t=3.98; over −14.65% t=−9.82 (sum ≈ −vig → the under IS the inverted, confirmed DK over-lean). ⚠ Extraordinary for an efficient book + seems to contradict the team-market "totals abstain" prior — likely reconciliation: prior work tested MODEL totals bets + de-vigged fair values, never naive FLAT-side ROI (a real blind spot). NOT yet actionable. STRESS-TEST PASSED (2026-08-31, `--scenario under_stress`, commit d1d36de) — the +9.95%/t=5.04 (12-24h window, n=2,196, 57.8% under hit) is BROAD not concentrated: +ROI in ALL total-line buckets (8.5-10.8%), ALL favorite-strength buckets (7.5-11.9%), all 3 seasons (+9.5/+11.7/+7.4%). Only structure = SEASONAL/weather: strong Apr/May/Sep (+13-16%), fades summer, ~0 in Aug (−0.5%) → market under-adjusts totals for cool-weather run env. Selection defused: capture-timing is exogenous to outcome. ⇒ **CREDIBLE THIRD-EDGE CANDIDATE** (though a +10% flat-side edge is extraordinary → forward-confirm before sizing). NOTE: paid close-pull is NOT the right confirm (we'd execute at 12-24h, not close). CONFIRM via FORWARD-TRACK of the real bet: flat-unders 12-24h out, weighted to cool months (Apr-Jun+Sep, skip Aug), small stakes — cleanest = a daily under-flags tool + auto-log mirroring coherence_flags. PENDING Doug's go-ahead on the forward-track build. TEMP MECHANISM CHECK RAN (2026-08-31, weather_game.temp_f, coverage 2,193/2,196): NOT clean cold-weather — under edge is +ROI in 4/5 temp buckets (<55F +17.86% biggest, 75-85 warm +12.45%, cool +9.95%, mild +7.79%), ONLY >=85F hot is negative (−4.55%). So temp is a MODULATOR (cold amplifies, extreme heat kills — explains Aug dead-spot = the >=85F games), not the sole mechanism. Refined rule: bet unders 12-24h out, SKIP >=85F, lean into cold. ⚠ The very PERVASIVENESS (+ across line/fav/temp/month/season) is double-edged: reassuring vs overfit BUT a uniform +10% flat-under could equally be a systematic LINE-CAPTURE OFFSET (our total ~0.5 high across the board → unders win everywhere). 2026 softest (+7.43% vs 2025 +11.70%) = weak decay tell (DK catching up to the low-scoring regime?). **DECISIVE FREE CHECK PENDING (Doug's pick): compare captured DK total vs sharp Pinnacle total (in warehouse, analysis-only) — DK>Pin systematically = real DK over-shade (clean rule: under when DK>Pin); DK≈Pin = regime edge (likely decays); DK total looks off = capture artifact → fix pipeline.** Only forward-track/bet after this resolves real-vs-artifact. **DK-vs-Pin RAN (2026-09-01, `--scenario under_dkpin`, commit 2bb3d79) — DEFLATES the signal + exposed a Pinnacle data bug.** DK−Pin total gap mean +2.7 / median +3.5 runs (IMPOSSIBLE for real lines) → OUR PINNACLE TOTAL CAPTURE IS BROKEN (grabbing F5 ~4.5 / mis-parsed totals; inconsistent — 29% match DK, 69% garbage-low). DK is fine (its total dist is normal ~8.5). Key: in the CLEAN subset where DK==Pin (valid comparison, n=502) the under edge is only **+4.36% t=1.05 — NOT significant**; the big +13.38% lives in the contaminated DK>>Pin bucket. **CORRECTED (2026-09-01): that deflation was WRONG — the +4.36% "clean" bucket was itself contaminated.** ROOT CAUSE (warehouse probe): `db_store.team_market_lines` returns ALL `kind`s mixed (no filter); Pinnacle posts BOTH full-game totals (kind=team, avg 9.24) AND first-5 (kind=first_five, avg 4.51), DK posts full only (kind=team, 9.08). My raw under_* readers didn't filter kind → Pinnacle totals got F5-contaminated (the +2.7 gap was DK_full − Pin_F5, not a line offset). FIXED (commit 5676f69): under_timing/under_stress/line_timing_study now filter kind=='team'. CLEAN facts: DK_full (9.08) ≈ Pin_full (9.24) → **DK totals are NOT line-offset**; the +9.95% under (always on clean DK full totals) is **NOT a DK-line artifact** → back to REAL-in-sample/unconfirmed. NEW ANGLE (Doug's idea, built): **`f5_decomp`** = DK full − Pinnacle F5 = DK's implied innings-6-9 runs (normal ~4.6); grade over/under at DK full bucketed by that gap → does DK misprice game SHAPE (low gap → over)? RUN: `python scenario_backtest.py --scenario f5_decomp --seasons 2024,2025,2026` (+ re-run `under_stress` post-fix for clean fav-bucket). **f5_decomp RAN (2026-09-01) — Doug's hypothesis CONFIRMED + mechanism found.** Integrity clean: DK_full − Pin_FULL mean/median +0.00 (n=1,047) → DK totals EXACTLY match Pinnacle full → NOT a line artifact. MONOTONE dose-response of over/under at DK full total by gap (DK_full − Pin_F5, normal ~4.0): gap<3.5 → OVER +9.58%/UNDER −18.96%; gap 4.0-4.5 → UNDER +11.68% ★; gap 4.5-5.0 → **UNDER +21.14% t=4.05 [+++]**, OVER −30.56%. Exactly Doug's logic (low gap=DK implies few late runs=full too low→over; high gap→under). MECHANISM: DK over-prices late-inning scoring — full avg 9.08 vs F5-extrapolated ~8.1; the more late scoring DK prices in (high gap), the more reliably it stays under. Gap ADDS info beyond line height (under_stress was FLAT across line buckets; f5_decomp is monotone across gap). ROBUST side = **UNDER when gap≥4.5** (+21%, 3/3); OVER-low-gap side is thin (n=63, not replicating) — don't lean on it. Same ~10% pooled as flat-under (sharpens WHERE, not a 2nd edge). STILL 12-24h + forward-unconfirmed + extraordinary magnitude, but clean integrity + mechanism + monotone replication = most credible yet. TAUTOLOGY CHECK PASSED (2026-09-01, `_gap_x_line_under`): within a FIXED line (8-8.5), UNDER ROI rises +4.0%→+13.1%→+36.1% as gap goes 3.5-4.0→4.0-4.5→4.5-5.0; same in 9-9.5 (+3.4→+12.7) and <=7.5 → the gap is REAL shape info, NOT line-height. Fully validated offline (clean integrity + monotone + 3/3 + non-tautological + mechanism). **FLAGGER BUILT + shipped: `under_flags.py` (commit 1070a9c)** — daily DK-vs-Pinnacle-F5 flagger mirroring coherence_flags: `--live` (Odds API: DK totals + Pinnacle F5, ~2 cr) or warehouse (free); flags UNDER at gap 4.0-5.0 (STRONG 4.5-5.0) / OVER at gap <3.5 (speculative), bet at DK's FULL total, Pinnacle=reference. 7 tests. RUN daily: `python under_flags.py --live` (bet SMALL). ⚠ STILL forward-unconfirmed — this IS the confirmation instrument. FOLLOW-UP: auto-log flags into the forward tracker (app doesn't yet capture Pinnacle F5 → not auto-logged like coherence; for now bet+note manually, or wire app capture before next April's big window). Under signal = now a MONITOR. (General lesson reaffirmed: a −ROI market says ABSTAIN, not invert, unless the loss exceeds the round-trip vig — then bet the opposite, e.g. batter_K UNDER 1.5.)
  Datamine harness = `scenario_backtest.py --scenario upset_datamine` (commit a4bbb58; `--datamine-min-n`). Batch-2 factors if the under signal or umpire pooling pays off: park/weather run-env, day-after-night (needs tz), bullpen fatigue, lineup-vs-typical.
- **Sharpen coherence w/ stable-favorite-SP filter (Doug's pick 2026-08-31) — DONE + SHIPPED LIVE (commit a5aabad).** favorite-SP-CV_MAX=1.0 gate live in `coherence_flags` (CLI + in-app), fails closed, tested. Full detail in [[edges-and-backtests]] (★★). Now a MONITOR: the in-app `runline_coherence` forward log tracks the SHARPENED (stable-SP) edge — watch it grow; expected ~+11% but bet small (t-ceiling ~3.37 on 3 backtested seasons). Verify live once: `python coherence_flags.py --live` shows the gate line + skip counts. FOLLOW-UP still open: the free early/close RL capture for the timing question (see line-timing item above).

## ⏳ PRECISE HISTORICAL BACKFILL — TIME-CRITICAL (5M credits expire ~2026-09-21)
Odds API topped off to **5M credits on 2026-09-01** (monthly reset is independent of the invoice cycle); reverts to 20k/mo on ~Sep 21. ONE-TIME chance to build the clean historical corpus. **DESIGN LOCKED (2026-09-01):**
- **Method:** per-event `get_historical_event_odds(event_id, date=<exact game-relative ts>)` — NOT the bulk/flat-clock endpoint the old backfill used (that's WHY we only had sparse ~9am-ET snapshots + couldn't find early+close pairs; it was capture-method, not availability, and NOT an F5 issue). Per-event returns the ACTUAL snapshot ts + is permanently cached (`is_historical_event_cached` → idempotent/resumable/free re-runs). Cost = 10 × markets × regions per call.
- **Snapshots (market-smart offsets):** team+F5 at **−12h, −4h, close(−10min)**; props at **−4h, close** (props post same-day → skip −12h, no wasted calls). −4h = lineup-safe (probables/lineups up) + fills the sub-12h window we've never seen (resolves under/F5 timing).
- **Markets:** Tier1 team+F5 (h2h/spreads/totals + *_1st_5_innings); Tier2 props (batter hits/TB/RBI/K + pitcher K/ER/outs).
- **Books (store ONLY these 4):** DraftKings, FanDuel, bet365 (3 bet books), Pinnacle (sole reference). Drop all others. Regions ~us+eu (bet365 US=us; Pinnacle=eu).
- **Seasons:** 2024, 2025, 2026 (R). ~7,000 games. Nominal est ~3.5M credits (region-vs-`bookmakers`-param cost Q could ~2× it — DRY-RUN + one probe call to read real credits/call decides).
- **Safety:** newest-first, `--max-credits` cap, `--dry-run` prices before spend, resumable via cache, store tagged role(early/mid/close)+exact-ts+source="backfill_precise", never overwrite. **NOTHING fires without Doug's explicit "go, ~N credits".**
- **Split by MARKET GROUP × region-need** (not arbitrary us/eu): team+F5 → us+eu (Pinnacle); props → us only (Pinnacle has no MLB props — never pay eu for props). Probe also tests `bookmakers=` vs `regions=` billing (could ~halve team cost).
- **3-phase raw-first pipeline** (protects the one-time spend): Phase1 FETCH (credits; odds_client permanently caches raw to `cache/hist_event_odds/`), Phase2 COMPILE cached raw → parquet (FREE, re-runnable), Phase3 LOAD parquet → Azure (FREE). Only Phase1 races Sep-21; a later parse/schema bug re-runs Phase2/3 for $0. Parquets → dedicated `deploy/odds_backfill/parquet/` (separate from cache).
- **Effort:** build at HIGH (mechanical, spend-critical); assurance goes on `--dry-run`+probe AND a pre-fire multi-agent REVIEW of the tool (the ultracode-worthy step), NOT on ultracode-ing the build.
- **Cache hygiene DONE (commit 0bd299e):** odds_client cache now foldered by purpose (`cache/<type>/`), legacy files self-migrate — so the ~42k backfill JSONs land in `cache/hist_event_odds/` cleanly.
- BUILD: new focused `backfill_precise.py` (reuse odds_client historical primitives + warehouse.capture_event_odds), NOT a refactor of the multi-mode backfill_historical_odds.py.
- **PHASE 1 BUILT + PROBED + REVIEWED + FIRM-PRICED (2026-09-01) — READY TO FIRE (pending capped go-ahead).** `backfill_precise.py`: enumerate_games (mlb_game R/Final, detailed_state-guarded, newest-first), resolve_event_ids (warehouse odds_snapshot reuse → cached hist-events → cap-bounded 1cr/date harvest; **DH-aware** nearest-commence one-to-one), plan_and_run (**concurrent fetch**, --workers default 12; budget-sliced before dispatch so --max-credits bounds the WHOLE run), --dry-run/--probe/--limit-games. Offsets team+F5 [−12h,−4h,close], props [−4h,close].
  - **PROBE (2026-08-30, Doug ran ~180cr): Odds API bills a per-event call with `bookmakers=` as ONE region regardless of span → team = 60cr/call (NOT 120), and the single us,eu call STILL returns Pinnacle.** So team=180cr/game, props=140cr/game = **320/game**. bet365 DROPPED (absent from the full 33-book historical slate → no MLB depth); books now **DK/FD/Pinnacle** ([[preferences]]).
  - **FIRM FULL DRY-RUN: 6,916 games → ~2.21M credits** (harvest ~358; 5,902/6,916 event_ids already in warehouse). ~2.8M headroom in the 5M window (expires ~Sep-21).
  - **PRE-FIRE REVIEW DONE (workflow wf_fffe6b6c, 16 agents, 9 confirmed) → ALL FIXED (commit after probe):** HIGH ×2 doubleheader mis/never-capture (multi-valued warehouse map + nearest-commence one-to-one in both passes); MED cap now bounds harvest; MED enumerate excludes postponed/suspended/cancelled Final+partial rows; MED/LOW unmapped-games manifest (`odds_backfill/unmapped_games.csv`); LOW probe cache-key alignment. One finding (official_date fallback) verified false.
  - **SMOKE TESTS DONE (2026 ~50k + 2024 ~30k, Doug ran).** `--verify` (FREE cache audit) + `--skip-recent-days` (archive-lag guard, caches 404s permanently so never fetch not-yet-archived games) added. VERIFIED: game-relative capture EXACT (close median 0.24h, early_4h 4.07h, early_12h 12.07h, 100% within tol); books DK/FD 100%, Pinnacle lands in the us,eu call (+ bonus Pinnacle pitcher props). Harvest now ALSO parallel (was serial preamble).
  - **BUG FOUND+FIXED via 2024 verify (would've silently dropped ~11%):** ~11% of 2024 games were ALL-EMPTY (404 every offset) — NOT lag. Cause: warehouse map keyed by odds_snapshot.game_date (commence-UTC date) but looked up by mlb official (local) date → night-game series bound the PRIOR day's event_id (~24h off → expired → 404). FIX (commit after 2024 smoke): key warehouse map by MATCHUP, bind by nearest COMMENCE within 2h (self-corrects; 2024 resolution 2194→2424/2429; DHs/absent → harvest). Verified the 10 all-empty games now map to commence-matching ids or harvest.
  - **FIRM FULL ESTIMATE now ~2.14M credits** (6,667/6,916 warehouse-resolved; harvest ~176). ~2.86M headroom.
  - **FIX CONFIRMED (2024 re-run 2026-09-01):** 2024 all-empty dropped 10→**0** (467 fetched, 1 empty); 6 partials = early_12h Pinnacle-not-yet-open (fine). 5 games (0.2%) unmapped even after harvest (Sept-30 Mets@Braves DH, "Athletics" naming, etc.) → logged in `odds_backfill/unmapped_games.csv` for later hand-map.
  - **PHASE 2 BUILT (`--compile`, FREE, commit 2026-09-01):** cached raw → line-level parquet per (season,group) in `odds_backfill/parquet/`. Generic flattener (raw bookmakers[].markets[].outcomes[]) keeps F5 period markets + props; re-resolves ids free; cached-only reads (api_key=None); skips uncached as 'pending' → re-runnable as Phase 1 fills. Validated on 2024 (134k rows). Schema: game_pk/season/official_date/commence/home/away/event_id/role/group/requested_ts/snapshot_ts/lead_h/book/market/market_last_update/outcome/description/point/price/source.
  - **PHASE 2 VALIDATED:** `--validate` (FREE data-quality assertions: schema/prices/points/market-balance/dups/lead-times) added; 2024 parquet ALL PASS. Props balance is point-aware (players carry multiple lines e.g. hits 0.5&1.5; one-sided Over ok).
  - **PHASE 3 DESIGN LOCKED (Doug 2026-09-01) = REPLACE warehouse odds with precise corpus.** `warehouse_odds_reload.py` (backup→purge→load→verify). Decisions: **(B) TRUE PURGE-ALL** — Doug wants ONLY the rigid −12h/−4h/close windows; NO legacy mixed-timing live snapshots diluting them (accept a ~1wk recent-2026 hole from --skip-recent-days 7; live re-capture + later mop-up refill it). **Back up ALL sports** (NFL/NBA too) to parquet — Doug will later translate this pipeline to repopulate NBA/NFL. Purge via **TRUNCATE** (fast): truncate odds_line (bulk), then odds_snapshot (⚠ SQL Server won't TRUNCATE a table with an incoming FK → drop/recreate fk_odds_line_snapshot around it, or DELETE the smaller snapshot table). Reload MLB via `warehouse.capture_event_odds` (reuses best-price/consensus extraction + `_kind_for_markets` → team/first_five/props). ⚠ capture derives kind from the `markets` string & `_enumerate_lines` parses the payload per kind → must feed per-kind sub-payloads/markets (full vs *_1st_5_innings vs props) so F5 lines land as kind=first_five (verify parse_game_odds F5 handling at build).
  - **SOURCE = ROLE (Doug 2026-09-01, SUPERSEDES 'backfill_precise'):** load with `source ∈ {'early_12h','early_4h','closing'}` (internal role `close`→`'closing'`) so `source` IS the snapshot-window selector (his words: "otherwise we lose our filter for choosing which snapshot to use"). Current read scheme is binary (`source=='backfill_early'` vs else) → INSUFFICIENT for 3 windows. Phase-3 REMAPS the readers to select by exact role: `db_store.team_market_lines(only_early)` + `player_prop_lines(only_early/exclude_early)`, `warehouse.load_team_market_store(snapshot=...)` + `load_prop_lines`, `warehouse_mirror` source handling. Add explicit 3-way `early_12h`/`early_4h`/`closing`; `snapshot="close"`→'closing' (unambiguous alias ok). **DECIDED (Doug 2026-09-01): bare `snapshot="early"` must RAISE** — force the caller to name `early_12h` or `early_4h` explicitly (no silent default; robustness). Post-purge MLB holds ONLY these 3 source values (legacy 'live'/'backfill_early'/None gone) → clean remap.
  - **Warehouse scope (2026-09-01): odds_snapshot 32,422 (nfl 4,565 / mlb 14,100 / nba 13,757); odds_line 3,697,604.**
  - **PHASE 3 FULLY BUILT (2026-09-01) — `warehouse_odds_reload.py` + reader remap. All committed, dry-run/read-validated, tests green.**
    - `--backup` (all sports → parquet, odds_line streamed chunked; sets LATEST.txt), `--counts`, `--purge` (FK-aware: SQL Server truncates odds_line, drops fk_odds_line_snapshot, truncates odds_snapshot, re-adds FK; SQLite=DELETE; refuses w/o backup unless --force), `--load` (drives bp PLAN, per-book lines via `ingest_multibook_cache._per_book_lines`+`_as_full_game_markets` F5 shim, one snapshot/(event,kind,role), source=role, dry-run default), `--verify` (composition + closing team price parity vs parquet). All dry-run/read default; `--apply --yes` to write/destroy.
    - **Reader source-remap SHIPPED (commit after load):** `db_store.team_market_lines`/`player_prop_lines` new `snapshot_source` filter; `warehouse.load_team_market_store`/`load_prop_lines`/`load_prop_market_store` `_snapshot_source()` maps snapshot→source (`closing`/`early_4h`/`early_12h` exact; `close`=legacy nearest-commence, back-compat; bare **`early` RAISES**); `warehouse_mirror` delegates to Azure when snapshot_source set; `backtest --snapshot` choices=close/closing/early_12h/early_4h. Tests updated; all green (3 pre-existing test_p3 print/refit errors unrelated).
    - KIND MAP: team payload→ kind='team' (full h2h/spreads/totals) + kind='first_five' (F5 shim); props→kind='props'. props have NO early_12h (only −4h+close).
  - **MLB Phase 1 DONE + full corpus compiled/validated (team ALL PASS; props benign WARN — raw-API double-list, collapsed on load).**
  - **MULTI-SPORT (Doug 2026-09-01): NBA+NFL precise backfill running.** Reason: 5M credits die Sep-21 AND the truncate wipes ALL sports, so capture NBA/NFL historical NOW → one truncate repopulates all three. `backfill_precise` generalized: `--sport {mlb,nba,nfl}` (MLB=warehouse enum+F5; NBA/NFL=events-listing enum, team[h2h/spreads/totals]+props, no periods; `--date-from`(props floor 2023-05-03)/`--date-to`). NBA/NFL cost = team 90/game (3mkt×3off) + props 200/game = 290/game. Smokes clean (0 err); verify: team DK/FD/Pin 99-100%, props all 3 books 84-99% (NO FD-drop, unlike MLB batter props), lead times exact. **Doug FIRING full: nba (~1.13M, cap 1.2M) + nfl (~264k, cap 300k) = ~1.39M; ~2.84M remaining. Running (resume-safe, hours).** NBA 4046 games, NFL 1081 (incl preseason — low-value, filter at analysis time). enum via listing ~1.3k cr.
  - **compile/load/verify/validate ALL-SPORTS now** (commit 2026-09-01): `configure_sport()`+`enumerate_for_sport()` shared by CLI + reload; parquet named `{tag}_precise_{group}_{season}`; `warehouse_odds_reload --load/--verify --sport`; `--backup`/`--purge` all-sports (one truncate). NBA/NFL → kind team+props only (no first_five). 120 tests green.
  - **ALL 3 SPORTS FETCHED + COMPILED + VALIDATED (2026-09-02).** MLB ~4.5M / NBA ~3.27M / NFL ~534k parquet rows, all --validate PASS (benign props double-list WARNs; player_anytime_td is point-less — excluded from point check). NFL 2023 has more empties (oldest + preseason). Parquet in `odds_backfill/parquet/{tag}_precise_{group}_{season}`.
  - **PRE-RUN REVIEW DONE + ALL FIXED (workflow wf_c68116bf, 13 agents, 5 confirmed → fixed commit 2026-09-02):** HIGH: MLB event-id resolution read the just-purged odds_snapshot → would silently load an EMPTY MLB warehouse; FIXED by sourcing game_pk→event_id from the compiled parquet (purge-immune) + <50%-coverage abort under --apply (MLB 6908/6928 mapped). MED: backup pyarrow schema pinned (nullable-string chunk mismatch); snapshot_hour bucketed on requested `ts` not served snap (write-once uq collision → dropped window); verify() now FAILS on empty/incomplete reload (was false-OK) + checks parquet→wh coverage. LOW: backup gate validates manifest+parquets present. (2 rejected.)
  - **ASSURANCE (2026-09-02): 2nd ADVERSARIAL review (wf_93f37113, 10 agents, 6 raw→1 confirmed; other 5 = non-defects → the earlier 5 fixes hold) → fixed: verify() now asserts purge-completeness (0 legacy/non-role snapshots) + post-purge emptiness assert. Plus a SQLite END-TO-END REHEARSAL (real writes, throwaway db): purge emptied odds + LEFT wagers intact (protected-table safety proven), load wrote per-book source=role snapshots (0 err/0 collision), price parity 0-mismatch, verify correctly failed on partial/legacy. Only unrehearsable piece = the mssql FK-drop/recreate DDL (SQL-Server-only; inspection+reviewed).**
  - **EXECUTION IN PROGRESS (2026-09-02) — two live-run fixes:** (1) app SQL login is db_datawriter (no ALTER/DDL) → `TRUNCATE`/FK-drop fail ("Cannot find object ... or you do not have permissions"); purge now uses batched `DELETE TOP(100000)` (committed batches, needs only DELETE perm) — Doug ran `TRUNCATE TABLE dbo.odds_line` as ADMIN in Azure (child table, instant) then the tool purge cleared odds_snapshot. (2) serial load crawled (1 txn/snapshot × Azure latency) → parallelized `--load --workers 12` (committed). Load is idempotent (write-once uq) → safe to Ctrl-C + resume. Doug running mlb→nba→nfl loads now.
  - **✅ RELOAD DONE + VERIFIED (2026-09-03).** All 3 sports: `--verify` OK — **mlb 54,678 / nba 19,708 / nfl 4,164 snapshots, 0 legacy (purge complete), prices match parquet, windows balanced.** Warehouse now holds ONLY the uniform −12h/−4h/close windows (source=role). Backups in `odds_backfill/warehouse_backup/`. Verify is 20-DTU-safe (composition + per-event indexed spot-check; full JOINs time out on S1).
  - **MIRROR-FIRST BACKTEST READS BUILT (f161fc0):** db_store emits `source`; warehouse_mirror filters by source; load_team_market_store/load_prop_lines mirror-first → re-tests read parquet (0 DTU) windowed by source.
  - **✅ MIRROR GENERATED + WINDOWED READS CONFIRMED (2026-09-03).** `warehouse_mirror --sync --refresh` done for all 3 sports (per season×book, source-carrying parquet in `warehouse_mirror_data/`). Fixes en route: dropped team_market_lines server-side ORDER BY (timed out sync); `SQL_TIMEOUT` env (mirror --timeout default 600) for bulk reads on 20 DTU; readers filter snapshot `kind` (skip seek-and-discard, helps live too); guarded `_sync_facts` MLB-only (was mislabeling MLB facts as nba/nfl — junk deleted). VERIFIED: windowed reads (`closing`/`early_4h`/`early_12h`) serve from parquet 0-DTU, source-isolated, all 3 sports (mlb ~19k / nba ~7.9k / nfl ~1.7k closing team rows/season). **Backtests now fully on the mirror.**
  - **MIRROR-EVERYTHING + SNAPSHOT-ELIGIBLE (2026-09-04) — Doug: "all backtests and calibrations on the mirror parquets, eligible for snapshot windows."** DONE:
    - **Root fix (0ed5348):** unscoped (dates=None) `load_team_market_store`/`load_prop_lines` now enumerate the mirror's OWN season files (`warehouse_mirror.available_seasons` + `warehouse._unscoped_season_range`) instead of probing 2019..now into Azure → fully 0 DTU. Mirror off = legacy range. Fixes calibration odds leg + CLV + backtest unscoped reads at once.
    - **Calibration --snapshot (4f6aca5):** `book_line_calibration.SNAPSHOT` (module default 'close') threaded into the one odds read (harvest→load_prop_lines snapshot=); `--snapshot {close,closing,early_4h}` on `refit_calibration` + `recalibration --seed`, set once at CLI so ALL real-line paths inherit. ⇒ recalibrate/validate vs CLOSING; tune value_gate EV at early_4h. Read-only, stages candidates.
    - **Calibration actuals mirror-first (9a72d0c):** `warehouse_mirror.calib_gamelogs_bulk_full` (reduced bulk + game_date=official_date + completed=True — the only fields `_match_rows_to_gamelog` reads; opponent/park come from `_attach_gamecontext`, matching is game_pk-first). `book_line_calibration._bulk_gamelogs` mirror-first→Azure fallback. Verified 1,577 lines graded off the mirror, 0 DTU.
    - **Backtest --snapshot (4808e47):** `snapshot_source` threaded through r2_data loaders; `--snapshot {close,closing,early_4h,early_12h}` on r2/coherence/f5 (keyed into pickle cache per window). Default 'close' = legacy self-pick (byte-identical). ⇒ can test the coherence RL edge etc. at the early_4h execution window.
    - **Statcast mirror DONE (450ba7e):** opt-in `statcast__baseball_mlb__{season}.parquet` (`warehouse_mirror --statcast`); `savant_history.load_days` mirror-first → method-D/xBA + platoon refits 0 DTU. NOT in the default autobuild (heavy).
    - **Sweep facts + opp-defense on mirror DONE (f6ace94):** mirror sync extended (mlb_game +game_date/status/detailed_state, batter +team_id, new team_dim parquet); new readers `calib_gamelog`/`calib_gamelogs_bulk_espn` (full ESPN shape w/ opponent), `team_name_map`, `team_final_games`; `mlb_warehouse.get_calib_gamelog`/`get_calib_gamelogs_bulk`/`_team_name_map`/`_team_final_games` mirror-first → synthetic sweep AND `_mlb_warehouse_defense_lookup` (Doug's opp-defense "was JSON" → now computed from parquet) run 0 DTU. SAFE: new readers return None when a needed column is absent (old mirror) → Azure until re-sync.
    - **⚠ RE-SYNC REQUIRED to activate the new mirror schema:** `python warehouse_mirror.py --sync --refresh --seasons 2024,2025,2026` (adds game_date/status/detailed_state/team_id/team_dim) AND `python warehouse_mirror.py --statcast --seasons 2024,2025,2026` (statcast). Doug runs on his box (touches Azure once). Then re-commit the refreshed `warehouse_mirror_data/` parquets (LFS) so both machines get them.
    - **STILL Azure by design:** `_attach_gamecontext` StatsAPI (cached, fail-open, network not warehouse) + prediction-log backstop (small, not mirrored); `--weather-sweep` `game_weather_density_map` (opt-in diagnostic, not the default method sweep — P1.3 weather is live-only).
    - **Savant off-season fix (9aef197):** `missing_days` skips off-season (Feb 15-Nov 10 window) → no more perpetual "missing" re-fetch of the ~4 gameless months.
    - ⚠ Run tests with `PYTHONIOENCODING=utf-8` on Windows (cp1252 chokes on ✓ in refit prints). Pre-existing unrelated failure: `test_distributional.test_deep_bucket_adopts_d` (fails at session start too).
  - **SWEEP/REFIT SPEEDUPS DONE (2026-09-04, all parity-verified BYTE-IDENTICAL to serial, all pushed):** `run_player_props_backtest` `--workers N` parallelizes the variant sweep over CONTIGUOUS player chunks (order-preserving merge) (3794c81); `_best_per_prop` post-sweep reduce parallelized over (prop × variant-CHUNK) — the per-prop 7-way tail-stalled on batter_hits alone (b61cb58, 618cddc); combined-sweep ranking now OPT-IN `--combined-sweep` (default OFF, was ~5min/season diagnostic-only) (8f4f042); in-place `\r` progress bars for sweep + reduce (49b7e8e/9690676); noisy output quieted (fetch summary, worker stdout redirect, safe-mode 576-variant dump off in sweep) (9690676); mirror memoization of small dim parquets + bulk gamelogs (da3a646, 420306f). Threaded `--workers` through refit_calibration→refit_sport→both calls + _best_per_prop. Perf on Doug's 12c/24t: sweep 225 players×576 variants ~55-70s/season @ workers=24; reduce parallel. Sweet spot workers≈16-20 (load-balance past physical cores). **Grid = 4×3×3×4×2×2=576 (half_life {None,5,10,15} — marginal table hides None; effect is flat/inert). Reduce caps at min(workers,n_props) for per-prop but now variant-level.**
  - **✅ STEP 2 DONE (2026-09-04) — MLB props recalibrated on CLOSING, promoted + Platt-reseeded. Full result → [[edges-and-backtests]] / [[modeling-and-calibration]].** AB bug (4a51226) fixed → D competes fairly. Re-fit `--real-lines --snapshot closing --xstats-strength 1.0`: **batter_hits → method D+xBA(1.0) per-bucket** [line<=0.5:D, line<=1.5:D] (real-line Brier 0.2362, closing ROI −3.6% = best of all methods; bracketed 0.5→0.2400 / 0.75→0.2381 / 1.0→0.2377, monotone, 1.0=ceiling); batter_strikeouts A→C; pitcher_strikeouts E→C; kept batter_total_bases E, batter_rbis A, pitcher_outs A, pitcher_earned_runs E. Platt re-seed (closing): only **batter_strikeouts** (a0.674) + **batter_total_bases** (a0.311) fit; batter_hits/pitcher_K/others serve RAW (D+xBA well-calibrated → Platt can't beat raw, exactly as designed). **All promoted + committed + pushed** (calibration 81806ed era; reversible via archive).
  - **⚠ RECAL STALE-FIT BUG (caught+fixed this session) — [[data-and-architecture]].** After batter_hits flipped E→D, the OLD bare `batter_hits` Platt (a0.56, n4292, E-era) lingered in the **durable SQL overlay**; `load_recalibration` merges SQL-only keys untouched + `props._resolve_recal_cfg` pre-migration fallback serves the bare fit when a line_methods prop has NO composite `@bucket` key → stale E-Platt on D raw. FIXED: purged SQL key (`db_store.load_recal`→pop→`save_recal` reconcile-delete); committed seed already clean. **MANDATORY post-promote step until hardened: after ANY prop method change, re-seed Platt AND purge stale bare SQL keys, verify with `recalibration.py --show`.** OPEN HARDENING (Doug decision): (a) seed-merge drop bare keys for line_methods props (safe); (b) resolver stop falling back to bare for line_methods props — BUT `test_recal_buckets.test_resolve_recal_pre_migration_fallback` asserts current behavior (intentional for SAME-method bucket migrations) → needs sign-off. → [[wishlist]].
  - **⚠ PROPS VERDICT (2026-09-04, props-odds `--snapshot early_4h`) — NO capturable probability edge in MLB props. → [[edges-and-backtests]].** Model Brier > market Brier for EVERY prop; optimal model⇄market blend w≈0.00 (model adds nothing); model systematically OVERCONFIDENT (batter_hits 90-95% bin: pred 92.7 / actual 57.4); daily top-10 EV portfolio OOS (2025-08→2026-08) → **−100% (bankrupt), RAW and CALIB.** Confirms Step-1 CLV "props broadly vig-eaten" — the props edge (if any) is **CLV-only, not probability**; a realized-outcome backtest structurally can't credit it. Only thin/high-variance +pockets: batter_K CV≥1.0&EV≥8% (+2.2%/5490), pitcher_ER CV≥1.3 (+4.6%/700) — DON'T build a system. D+xBA is the best CALIBRATION; the ceiling is just below the market. NOTE on rigor: `--mode props-odds` is fit==serve (NO auto train/test — flatters model, still loses); dist-diag/roi-diag/real-lines DO auto chronological-split (that's the `n_train/n_test`, `cv` numbers). ⇒ props = accept + monitor CLV; bet effort goes to TEAM markets.
  - **▶ STEP 3 — TEAM calibration (ACTIVE, mapped):** NOT a Platt/real-lines flow. `prob_shrink` = Brier-optimal model-prob-vs-OUTCOME shrink (market-agnostic/window-agnostic — snapshot doesn't change it); `market_blend` + `expected_runs_challenger` shares = model-vs-de-vigged-MARKET (snapshot/closing-RELEVANT); `expected_runs_additive` (separate: `backtest_starters.py --additive-save`). ONE command fits team (⚠ needs `--limit 10000` — default is 200 most-recent games!): `$env:ODI_MLB_WAREHOUSE_OFFENSE="1"; python backtest.py --mode odds --engine live --write-calibration --sport mlb --seasons 2024,2025,2026 --source warehouse --snapshot closing --fit-shares --fit-blend --min-shrink-n 200 --limit 10000` (stages candidate) → validate `--serve-mode` + `--team-gate-sweep` (same `--limit 10000`) → `refit_calibration --promote`. Coherence RL offset = live-recomputed constant (`coherence_flags.compute_offset`), NOT persisted/promoted. No team value_gate. `stage_team_market_reset` exists but is tests-only (not CLI-wired). Backtest.py odds-mode `--snapshot {close,closing,early_12h,early_4h}` exists. **RUN PREREQS — now AUTO (afa117a):** the odds backtest self-materializes the team-offense cache (`mlb_starters.ensure_offense_cache`, 0-DTU from statcast mirror, 24h TTL) AND skips the per-game pitcher_asof debut-fills (`pitcher_asof.SKIP_ONDEMAND_FILL` set after prewarm — byte-identical, was ~254 recurring Azure round-trips/run = the 'flew last time, sits now' tax). Matchup features cache to `.feature_cache` on first run. **`ODI_MLB_WAREHOUSE_OFFENSE=1` still REQUIRED** (offense reads/auto-refreshes only when on; off → Savant network). pitcher_asof_daily verified current in Azure (25k+ SP rows/season); needs no `--build`.
  - **STEP 4 — market-structure edges (MLB nearly closed out):** (1) **Coherence run-line — DEAD on honest data (2026-09-05).** Band-scale fix (3ac3003, devigged-fair) + clean corpus → pooled −3.06%, stable-in-band −4.47%, replication FAIL. The +11% was a raw-band/biased-capture artifact. Live gate PAUSED (see monitors above). early_4h timing test deprioritized (structural bet, not timing). (2) **under/f5_decomp — ALSO DEAD on clean data (2026-09-05).** f5_decomp: UNDER pooled −2.56%, OVER −5.96%, the old +21% gap-4.5-5.0 cell now −2.08%, dose-response no longer monotone. under_stress: pooled −2.56%, weather/month structure EVAPORATED (cold <55F now −2.57% not +17.86%; Apr/May/Sep now ~+1% NS; all 3 seasons negative). The whole MLB under family was old-capture contamination.
    - **✅ ONE MLB SURVIVOR (all-scenario sweep, clean data): `batter_strikeouts UNDER 1.5` — n=9,179 ROI=+1.26% t=+1.69, +ROI EVERY season.** The lone cell to clear the artifact-killing purge. Mechanism = inverted over-bias (public over-bets batter-K OVERS → flat UNDER clears vig; matches the standing "invert only when loss > round-trip vig" lesson). THIN + marginal (t=1.69, +1.3%) → **price-sensitive (need DK/FD UNDER ≥ ~−110), small forward-track, DO NOT scale.** This supersedes the old cv_floor/K-under claim (was +2.2% t=1.93 on dirty data → +1.26% t=1.69 clean).
    - **PATTERN (2026-09-05): on the CLEAN precise corpus + honest (devigged/OOS) methodology, EVERY MLB edge dissolved** — props (no probability edge, early_4h), team markets (no raw-Brier edge), coherence RL (artifact). The durable win = the validation harness + precise 3-sport corpus, which now correctly says "no free edge here." Better found cheaply than bled.
  - **▶▶ NFL BUILD (ACTIVE 2026-09-05) — clean-slate data foundation, then Steps 2-4.**
    - **DATA ARCHITECTURE (decided):** canonical source = **nflverse** (free, CC-BY). Ingest OFFLINE via **`nflreadpy`** (NOT `nfl_data_py` — deprecated; polars, `load_*` API) → **mirror parquets** in `warehouse_mirror_data/` (LFS-shared like MLB). Runtime (live app + backtests) NEVER imports nflreadpy/polars — reads the mirror, or self-maintains dep-free. NO Azure warehouse for NFL (nflverse IS the maintained public source) → **live maintenance = fetch-with-TTL** (nfl_schedule refreshes games.csv every 12h; nfl_epa already does pbp) — no scheduler needed (Streamlit has none; mirror is an LFS stub there → CSV fallback path). REUSE: `nfl_epa.py` (team EPA from pbp, leakage-safe as-of — unchanged), `game_results.py` (ESPN live grading). Stale May-2026 `americanfootball_nfl.json` calib to be rebuilt on clean data.
    - **THE SPINE = nflverse `game_id`** (`{season}_{wk}_{AWAY}_{HOME}`) — one key across schedules/pbp/player-week/NGS (zero fuzzy matching between stat layers) + an `espn` id crosswalk for the ESPN grader. games.csv = `raw.githubusercontent.com/nflverse/nfldata/master/data/games.csv` (all seasons, `game_type∈{REG,WC,DIV,CON,SB}` — **no PRE, preseason can't leak**).
    - **✅ PHASE 1 DONE (d86b50b): spine + integrity-checked odds join.** `nfl_schedule.py` (mirror-parquet-first, CSV-TTL fallback; `resolve_events()` = fail-closed join via static 32-team map `nfl_epa.NAME_TO_ABBR` + `season_for_date` + date tiebreak for reg-vs-playoff rematch; 6 integrity checks). `nfl_ingest.py` (nflreadpy → nfl_game__ parquet). **AUDIT: 837/837 captured NFL odds events → exactly one game_id, 0 collisions, 0 unresolved, all seasons.** NFL join is trivially clean vs MLB (no DH, 1 game/matchup/week). Odds mirror confirmed: 4 seasons, 3 windows (early_12h/early_4h/closing), ML/spread/total, full team names.
    - **ON DOUG'S BOX:** `pip install nflreadpy` → `python nfl_ingest.py --schedules --seasons 2023,2024,2025,2026` (materializes the fast mirror parquet; optional — CSV path already works). Then re-commit `nfl_game__*.parquet` (LFS).
    - **NEXT — PHASE 2: flat-side market-structure scanner** (sport-generic: fav/dog ML, home/road, over/under, spread-cover, by devigged band + season, graded off the clean spine). MLB's ONLY survivor was a market-structure cell → scan structure FIRST. Then Phase 3 team model (refit nfl_epa EPA→margin + prob_shrink/blend on clean corpus, validate honestly), Phase 4 props (player-week + NGS aggregates via nflreadpy — NGS *aggregates* free; raw RFID tracking NOT public/needed). Hold same devigged/clean-data skepticism — assume every edge is an artifact until it clears.
  - **▶ PIVOT DECISION (Doug 2026-09-05): finish MLB (f5_decomp check) → then NFL.** NFL/NBA are the active in-season sports; MLB parked until it matters (Doug doesn't bet the MLB opener anyway). NOT a restart — precise backfill already captured NBA+NFL, reload loaded them, mirror has them, scenario/calibration harness is `--sport`-generic (+ sport-key-normalized, 49ffc14). NFL has documented public/recreational biases MLB's sharp market may lack → point the SAME rigor at NFL. Redo Steps 2-4 for `americanfootball_nfl` (props recalibrate → team calibrate → market-structure edges) with the same devigged/clean-data skepticism.
    - **⚠ MIRROR OPERATOR STEP (new, learned 2026-09-05): after any `warehouse_mirror --sync`, run `python warehouse_mirror.py --verify --sport mlb --seasons 2024,2025,2026` ONCE.** `sync`/`_write` drops the `_valid` markers (data changed → must re-verify); `scenario_backtest`/coherence/f5 use `warehouse_mirror.ensure` which fast-paths ONLY when every needed file is `_valid`, else runs the full Azure parity `verify()` (slow on 20 DTU, prints only at the END → looks hung with no output). Calibration paths (`load_prop_lines`/`load_team_market_store`) read parquet directly (no verify) → unaffected. One `--verify` marks files `_valid` → all ensure-based backtests then fast-path (0 Azure). Watch for any `FAIL -> demoted` (a failing slice re-verifies every run).
  - **RE-TEST STEP 1 DONE:** ✅ CLV early-vs-close (`clv_window_study.py`, 2026-09-03) — flat vig, ML-early confirmed, RL/totals answered, **execute early_4h / calibrate vs closing** (full result [[edges-and-backtests]]).
  - **OPERATOR NOTES for the next session:** Doug runs backtests on his own 12-core/24-thread box + pastes output (he does NOT want the assistant running them). Mirror is Git-LFS shared; materialize on any box with `git config lfs.fetchexclude "" && git lfs pull`. Everything (backtests + calibration) reads the mirror 0-DTU and works WITHOUT SQL creds (warehouse loaders serve the mirror when `_mirror_on()` even if `_sql()` off; harvest uses the mirror when enabled). game-context team-factors avoid Savant via `ODI_MLB_WAREHOUSE_OFFENSE=1` (odds backtest now auto-refreshes the offense cache from the mirror, 0 DTU — manual `mlb_starters.precompute_offense_cache` no longer needed). Run tests with `PYTHONIOENCODING=utf-8`. Calibration is candidate-staged (`--diff` review, `--promote` archives previous = reversible). **PUSHED/DEPLOYED @ afa117a** (Step-2 props D+xBA(1.0) promoted; recal SQL purge; odds-backtest Azure-tax fix). PARKED ([[wishlist]]): live-app DTU hotspots (13); FanDuel MLB batter-prop capture; multi-sport durable STATS warehouse (nflreadpy/nba_api); Statcast reads still hit Azure `statcast_pitch` except where the opt-in statcast mirror is built.

### backfill_precise.py — PHASE-1 BUILD SPEC (fetch+dry-run+probe; deterministic post-compact)
- **Reuse:** `odds_client.get_historical_events / get_historical_event_odds / is_historical_event_cached`; `backfill_historical_odds.load_config` (api key) + `_names_match`/`_find_sampled` (game→event name match); warehouse write is Phase 3 (later).
- **Enumerate games:** warehouse `mlb_game` (game_pk, commence_time UTC, home/away team_ids→names via `mlb_team`) for `--seasons` (2024,2025,2026), game_type='R', final. Map each to Odds-API `event_id` via `get_historical_events(sport, date)` matched by (date, home, away). Reuse existing warehouse `odds_snapshot.event_id` mappings where present to save enumeration calls.
- **Offsets (game-relative, from commence):** team+F5 = [−12h, −4h, −10min(close)]; props = [−4h, −10min]. Tag role: early_12h / early_4h / close + store the ACTUAL returned snapshot ts.
- **Per (game, offset, market-group) call:** `get_historical_event_odds(event_id, date=iso_ts, regions, markets, bookmakers=[draftkings,fanduel,bet365,pinnacle])`. Team+F5 markets = h2h,spreads,totals,h2h_1st_5_innings,spreads_1st_5_innings,totals_1st_5_innings @ regions us,eu. Props markets = batter_hits,batter_total_bases,batter_rbis,batter_strikeouts,pitcher_strikeouts,pitcher_earned_runs,pitcher_outs @ region us. Skip if `is_historical_event_cached` (free). Store ONLY the 4 books.
- **Flags:** `--seasons`, `--tier {team,props,all}`, `--offsets`, `--max-credits` (hard cap), `--dry-run` (count calls + estimate at 10×m×r, NO api), `--probe` (ONE real call → read actual credits/call from headers, also test `bookmakers=` vs `regions=` billing), `--books`. NEWEST-FIRST ordering. Never overwrite.
- **Phase 1 = fetch→cache only** (raw auto-preserved in `cache/hist_event_odds/`). Phase 2 (compile→`odds_backfill/parquet/`) + Phase 3 (load→Azure) are FREE + built after, unhurried (survive Sep-21).
- **Gate:** build (high effort) → dry-run+probe → pre-fire multi-agent REVIEW of the tool → Doug approves exact credits → fire. NO SPEND before that.

## Review findings (session audit 2026-09-01, workflow weezv0bnw — 4 confirmed)
- **[FIXED, deeaaeb] under_dkpin kind contamination** — was missing `kind=='team'` filter → Pinnacle F5 leaked → the impossible +3.5 gap. Fixed; f5_decomp already superseded it.
- **[OPEN, medium, LIVE money] coherence_flags band-scale mismatch** (`coherence_flags.py:115`): live `_fav_band_ok` bands on DEVIGGED fair prob, but `scenario_coherence_stable_sp` (which set the live `cv_max=1.0` via the +11.43% lift) banded on RAW american-implied prob. Boundary games near 0.60/0.70 classify oppositely (~4.5% hold → raw>fair). coherence_backtest uses fair (matches live). FIX: align `scenario_coherence_stable_sp` (+ dog_runline) to devigged fair, RE-RUN to confirm the CV lift holds on the population the live gate actually bets. Low blast radius (boundary only, edge is 3/3) but it's live money → do before scaling stakes. Will also be re-validated wholesale once the precise backfill lands true closing lines.
- **[OPEN, medium] warehouse_mirror live-season staleness** (`warehouse_mirror.py:503`): `ensure()` fast-path returns instantly when all files are `_valid`, but never re-syncs the CURRENT season (mutable) → backtests can read stale 2026 data (understated 2026 n). MITIGATION NOW: run backtests with `--refresh-mirror` when current-season freshness matters. FIX: exclude the live (max/current-year) season from the `_is_valid` fast-path + force-refresh its per-season files in sync (touches ensure + sync `_skip` + the single-file mlb_game). Session results unaffected in conclusion (edges 3/3; 2024/25 immutable+correct; only 2026 possibly incomplete).

## [[modeling-and-calibration]]
- **value_gate auto-tune** — fold a confirmation-gated `--gate-diag` selection into the offline refit as advisory + `--promote` (never live-auto; threshold argmax overfits vs smooth Platt). `refit_calibration` still never calls `save_value_gate`.
- **EV floor 4%→3% for DK/FD** — `value_gate.ev_floor` is still 0.04 (chosen on optimistic consensus prices); A/B 0.04 vs 0.03 on DK/FanDuel fills via `--gate-diag`.
- **Pitcher props: seed Platt + exit synthetic-line fits** — grow pitcher REAL-line obs so they leave synthetic-line fits, and seed/enable online Platt for pitcher props (zero fits today) → closes the forward-Brier gap.
- **Online Platt Blob-mode hardening (low pri)** — seed-pristine invariant + per-key merge are SQL-only; mirror per-key merge into the Blob branch + make save guard mode-agnostic before any non-SQL deploy.
