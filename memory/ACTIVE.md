---
name: active
description: In-flight work only — what's being worked on now, linking to the domain files. Move an item to its domain when done; do not let this file accrete history.
metadata:
  type: project
---

# ACTIVE — in-flight work

> **▶▶ ASTRA AUDIT REMEDIATION (2026-09-21) — F01–F28 ALL ADDRESSED (F28 P3 deferred by design; F23 staging-SQL smoke-test T23 outstanding). ⟹ CURRENT PUSH STATE (authoritative; mid-narrative markers below are historical): origin/main tip `374134b` = F01–F27 + F28 P1 + all chore/memory commits; UNPUSHED but committed = F23 `8e85702` + F28 P2 `086e10f` (awaiting Doug's batch-push OK — he names the push).** External audit in `audits/Astra_Audit_09-2026/audit/2026-09-19/` (28 findings F01-F28 + 56 tasks T01-T56; contracts in test_audit_contracts.py/test_supplemental_contracts.py/check_ui.py — note their ROOT=parents[2] path math is off given the nested folder, so contracts were ported into our own suites, not run in place). Baseline `abb50e6`. Working top-to-bottom: reproduce → minimal fix → port a regression test → targeted suite green → 1 commit/finding. **DONE + PUSHED origin/main → `5f13563` (Doug authorized 2026-09-21): F01-F17.** F01 exec-offer(price+book) `fb83e9c`; F02 stale-exec-price trim `001e332`; F03 failed-read reconcile guard `2f1df68`; F04 parlay legs-first+replace `e03fdbc`; F05 parlays→bankroll `6976525`; F06 void stake-edit refund `da05bec`; F07 MLB leg commence/date `8f20119`; F08 SGP min-legs `a3bbf1c`; F09 SGP+→SGP-only (Doug's call) `101b5ad`; F10 game-diverse frontier `9c7f43e`; F11 promo-min vs 0 bankroll `0d64edf`; F12 impossible-SGP reject `9c8a86a`; F13 NFL shrink-K race `0094262`; F14 prob-floor default 0 (HR/TD reachable) `428162f`; F15 american-odds validation `3bcf38b`; F16 stable bonus_id `658a4d8`; F17 NFL model past ESPN gate `5f13563`. **F18-F21 DONE + PUSHED origin/main → `6111164` (2026-09-21):** F18 std/safe probability parity `1685c8c`; F19 integer-line push semantics `25d5502`; F20 lightweight access gate (st.secrets['app_password'], constant-time compare, gates before boot maintenance) `361cc0a`; F21 credential redaction in UI/logs/telemetry (`odds_client.redact_secrets`) `6111164`. **★ F20 ACTION FOR DOUG: set `app_password` in Streamlit Cloud secrets** (until then app shows an UNPROTECTED warning + stays open; once set, requires sign-in). **F22-F25 DONE + F23 documented (2026-09-21, committed NOT pushed since 6111164):** F22 wager batch-edit non-destructive-first + accurate partial reporting `fca582f`; F24 don't cache NFL layer absence `79b5ab2`; F23 mutate() lost-update concurrency: CONTRACT DOCUMENTED `77ce25a`, then **IMPLEMENTED 2026-09-21 `8e85702` (committed NOT pushed)** — optimistic compare-and-set: each UPDATE's WHERE also requires the row still hold the EXACT values we read (`_cas_where` over the live spec cols); 0-row result → `_LostUpdate` → the retry loop re-reads fresh + re-applies the mutator on the winner's state (0.1s·attempt backoff); UNCHANGED rows never trip (CAS value = the exact value just read); kill-switch `ODI_DISABLE_MUTATE_CAS=1` → identity-only. Row-count based so backend-independent, validated on SQLite — SQL Server DEADLOCK-under-load STILL to smoke-test on staging (T23; kill-switch = escape hatch). 3 F23 tests + 125 money-write tests green; F25 distinguish Platt-fit outcomes (no-signal 'low_slope_weak_signal' ≠ 'optimizer_failure'; serving unchanged; `platt_fit_reason`) `4e97e0e`. **F26-F27 DONE (2026-09-21, committed NOT pushed since 62e838f):** F26 test hygiene `820a90c` (gamelog_store dispatch → assert caching property not absolute count [prior-season spill legit]; pitcher_gamelog join → patch BULK get_calib_gamelogs_bulk keyed by mlb_id [stale fixture, not regression]; prediction_log seed already green; test_nfl_signs _have_pbp → nfl_data.pbp mirror-only so a clean checkout SKIPS not downloads); F27 docs `4772c6d` (README capability-map table + copula-is-research-not-live + warehouse/nflverse grading). CI job-separation (T26) + auto-rendered guide (T27) deferred. **NEXT: F28 ONLY** — prediction provenance/replay, LARGE/architectural, touches the IRREPLACEABLE prediction corpus schema → needs a DESIGN DECISION from Doug before building (scope: full immutable context+hashes+replay vs minimal provenance stamp vs design-doc-first). **[F28 DESIGN-DOC written 2026-09-21 `1bb6f7e` — `notes/AUDIT_F28_PROVENANCE_DESIGN_2026-09-21.md`; design-doc-first per Doug; NO code change pending his answers to 4 open decisions (schema=columns-vs-table; artifact-hash; scope=P1-only vs all; owner DDL ok?). Reuses calibration_loader.serving_fingerprint + feature_store.FEATURE_SCHEMA_VERSION; additive nullable columns on prediction_log (old rows=unknown provenance, kept); phased P1 stamp+NFL week=99 fix / P2 replay / P3 partitioned reports.]** **[F28 P1 BUILT 2026-09-21 `106507c` — Doug approved all 4 design Qs (columns / add artifact-hash / P1-now / owner-DDL-ok).]** prediction_provenance.py (build_context: event-derived season/week [NOT wall-clock], as_of, calibration_hash=serving_fingerprint, model_schema=FEATURE_SCHEMA_VERSION+artifact-content-hash, method, reference_book_policy, context_fingerprint [excludes as_of]); 8 nullable cols on prediction_log (db_store Table + _PREDICTION_SPEC — legacy rows NULL=unknown provenance); log_prediction gains context=; props stamps at log site; NFL week=99 serve fix (_nfl_model_override derives season/week from event commence_time — behavior-preserving for upcoming games, replay-correct for past). **`sql/prediction_provenance.sql` APPLIED to Azure (Doug, 2026-09-21) — the 8 provenance columns now exist on the live prediction_log.** db_store `_live_columns` reflects schema ONCE PER PROCESS (cached), so the running Cloud process must REBOOT (or redeploy) to refresh the cache and begin stamping provenance; existing rows stay NULL (unknown provenance, kept). Forward-tolerance `2227c29` meant zero logging pause pre-DDL. P3 (validation-domain partitioned reports) deferred per design. test_prediction_provenance.py (6). **F28 P2 DONE 2026-09-21 `086e10f` (committed NOT pushed):** `prediction_replay.py` — `replay(row)` recomputes the RAW model prob from the row's recorded event_season/event_week + artifact hashes with NO wall-clock (this is what fixes the early-season NFL week=99 cohort). Order: PARITY gate first (rebuild current context; any calibration_hash/model_schema change → `artifacts_changed` — refuse to claim a replay across a serving-version bump); then recompute → compare to row['raw_prob'] (the deterministic model output; final_prob's online recal isn't a clean standalone, so raw is the parity target) → status `ok` matches T/F; abstain `unsupported` on missing event_week / non-NFL-model / serving-None (never a false OK); `no_provenance` for legacy rows. `replay_many`/`replay_log` sweep the live log READ-ONLY (status tally + divergence examples); CLI `python prediction_replay.py --sport … --limit N`. test_prediction_replay.py (9, deterministic — serving injected as fakes). P2 scope = NFL model (project() pure given season/week/line/artifact); MLB as-of replay = future extension. **★★ ASTRA AUDIT PASS COMPLETE: F01-F27 implemented+tested+PUSHED (origin/main), F23 documented-deferred (needs staging SQL Server — don't infer from SQLite), F28 P1 implemented (P2/P3 deferred by design). Each fix = 1 commit + regression test; deferred-fuller parts tagged T## per-commit. ▶ DOUG ACTION ITEMS: (1) ✅ `app_password` SET in Streamlit secrets 2026-09-21 [F20] — auth gate now active; (2) ✅ F28 design Qs answered + provenance built (P1+P2 done); (3) F23 optimistic-CAS BUILT — only the SQL-Server-DEADLOCK-under-load staging smoke-test remains (T23); (4) live-test the deployed batch (sign-in, bonus slice, My Bets/Parlays); (5) ✅ Cloud app REBOOTED 2026-09-21 → db_store `_live_columns` refreshed; provenance now STAMPS on predictions logged post-reboot (verified wiring: props.py:2337 build_context → context=_prov → single log_prediction; NFL rows carry method=="nfl_model" @ props.py:1945 → replay-able). Verify via `prediction_replay.py` sweep once a slate's been viewed post-reboot (older rows stay no_provenance).** — F26 test hygiene (repair 3 stale fixtures: test_gamelog_store dispatch [prior-season spill = 3 SQL calls now], test_pitcher_gamelog join [stricter MLB identity — CHECK if real regression before editing fixture], test_prediction_log seed [int→dated-row-dict]; + offline-ify test_nfl_signs's nflverse download; separate offline/integration jobs); F27 docs/capability registry (README says ESPN grading + copula parlays — now warehouse + independence); F28 prediction provenance/replay (LARGE/architectural — immutable context + hashes; needs design + owner input). Deferred T-parts noted per-commit. **[PUSHED origin/main → `6111164` = F01-F21; F22-F25 local.]** (F18 std/safe/alt probability parity, F19 integer-line push semantics, F20 access-control deploy-verify, F21 API-key redaction, F22 batch-wager-edit atomicity, F23 SQL lost-update, F24 layer-cache freshness, F25 low-slope calib policy, F26 test hygiene, F27 docs, F28 prediction provenance) then T29-T56. Cadence: keep grinding + flag forks inline; push in batches on Doug's OK. Deferred-fuller parts noted per-commit (T-suffixed). **★ Two forks settled: SGP+→SGP-only; F16→full stable-id.**

One line per open item. When an item ships, delete it here and fold the durable outcome into its `[[domain]]` file. Last reconciled **2026-09-08** (folded the completed NFL Phases 1-3 + margin-model ship + granularity-law + prediction-ceiling into [[modeling-and-calibration]] and [[data-and-architecture]]; ACTIVE slimmed for an independent-review handoff).

> **▶▶ CURRENT THREAD (2026-09-09 late) — MLB PARKED until spring 2027; PIVOTED to NFL PROPS.** Decision (Doug 2026-09-09): the opportunity-first arc is conclusively tapped for MLB (4 probes — TB/HH%-spike/RBI/pitcher-K — all negative; incumbents + recency weighting already capture the accessible per-player signal; MLB props efficient for us on PREDICTION too). Park MLB (season ending, he doesn't bet the tail). **Loose-end PARKED for spring:** live `pitcher_strikeouts` C is over-confident (cv 0.266 > 0.25 baseline, no recal overlay) → recalibrate via `refit --recalibrate` (+audit other un-recalibrated props); ~+0.0126 Brier, cheap. **NOW BUILDING: NFL PROPS.** Data foundation is solid (`nfl_data.player_week`: targets/carries/attempts/receptions/air_yards + `nfl_snap` offense_pct participation + NGS). Current `calibration/americanfootball_nfl.json` props = TOY (player_pass_yds/player_rush_yds/player_anytime_td; pass≡rush placeholder). NFL is RECREATIONAL (softer than sharp MLB) → possible EDGE, not just prediction. Template: opportunity×conversion (receptions=targets×catch-rate, rush_yds=carries×yds/carry). ⚠ apply the MLB lessons: PROBE-FIRST + honest gates (real-line, quote-time, cross-season replication, participation-safe grading). NFL TEAM model = reliable PREDICTION, NO edge (market wins ~13.2 vs 12.7). **▶ RECEPTIONS — prior NEGATIVE was a MODELING BUG, now CORRECTED to a PREDICTION WIN.** First POC (`d409edb`, `nfl_props_probe.py`): fixed-n binomial → Brier 0.304 (WORSE than coin-flip), wrongly concluded "efficient-for-us." **REBUILD (`nfl_receptions_process.py`, 6,736 obs 2023-25, process/3-layer thesis + count-variance fix) = WIN.** Pooled 2-fold OOS Brier, clean layer attribution: raw binomial(old) 0.3046 → incumbent A 0.2813 → **M2 raw NegBin 0.2638 (+0.0174 vs A — the GIANT lever)** → M3 decomp share×pace 0.2636 (+0.0002, ~nothing) → **M4 +aDOT-depth catch 0.2617 (+0.0195, replicated both folds + all 3 seasons)** → market 0.2441 (ceiling). **LAYER VERDICT: (L3) the fixed-n binomial was the bug — receptions inherit TARGET-COUNT overdispersion (4↔12/gm), a NegBin (var=μ+φμ²) fixes it, HALVING the gap to market (0.037→0.0176); (L1) target_share×team-pace decomposition earns ~nothing over raw recency targets — keep simple; (L2) aDOT-shrunk catch (deeper→lower catch) is small but real+replicated.** **⇒ NFL receptions is NOT efficient-for-us — our first model was just WRONG.** SHIP M2's NegBin + M4's depth (per governing principle: better prediction ships). Edge still absent at the close (fine — logger harvests it live). **★ TWO portable fixes for the other NFL props: overdispersed count model + a depth/quality conversion layer.** **▶ CONVERSION PUSHED DEEPER (Doug: "the stat you bet is never the best stat to project it — there's always something underneath"): M5f models catch rate PURELY from fundamentals (aDOT + team CPOE, ZERO own-catch history) = 0.2626, M5b (fund prior + light own shrink) = 0.2620, vs M4 own-anchored 0.2617 — a 3-way TIE within fold noise (M5f wins 2025).** Fitted catch ≈ 0.80 − 0.016·aDOT + 0.009·CPOE (deeper→lower, accurate QB→higher; signs stable both folds). **⇒ we CAN fully de-anchor from the outcome stat with no accuracy loss — the conversion layer's "underneath" = target depth + QB accuracy; the stat now survives ONLY in the NegBin φ (calibration, correct).** SHIP **M5b** (ties best Brier + most robust for thin-history players: rookies/role-changes/new-team, where own-catch-rate is noisiest). Honest: a TIE not a gain — fundamentals MATCH own-catch; payoff = robustness + cold-start + conceptual purity. Big lever stays the NegBin count fix (+0.017); depth+CPOE are the fine layer (+0.002). **▶ TEMPLATE CARRIED to RUSHING_YDS (`nfl_rushing_yds_process.py`, 3,192 obs 2023-25, COMPOUND yardage = carries×ypc): GENERALIZES.** Pooled 2-fold OOS Brier: incumbent-A 0.2908 → M1 naive Gauss (carries×ypc, const sd) 0.2701 (+0.0207 — core "process not outcome" win repeats) → M2 vol-scaled variance (Var≈a+b·car+c·car², the count-fix ANALOG for yardage) 0.2692 (only +0.0009 — MODEST vs receptions' NegBin +0.017, because the naive Gaussian's pooled sd isn't catastrophically wrong the way the fixed-n binomial was) → M3 decomp vol 0.2697 (nothing again) → **M4 +ypc self-shrink 0.2674 (+0.0234, best all 3 seasons — the biggest lever HERE; rush_yds noisier/game so shrinking ypc→league stabilizes the mean)** → market 0.2489 (gap 0.0185, ~half naive→sharp, same as receptions). **SHIP M4 (ypc self-shrink, Doug's pick).** **CONSISTENT CROSS-PROP FINDINGS: (1) opportunity model (volume×conversion dist) beats empirical-over-rate by ~+0.021 both props; (2) volume DECOMPOSITION (share×pace) earns ~nothing — DROP it everywhere; (3) the variance/count fix matters MOST where the naive model is most broken (huge for receptions' binomial, modest for yardage's Gaussian); (4) self-shrink conversion helps more the noisier the per-game stat.** Edge NOT a gate but flag: rush_yds |edge|≥10% bucket +4.04% price-robust realized−implied (n=671) — prior rush_yds edge = juiced-band artifact, revisit at price-band level in the live/edge pass only. **▶ RECEIVING_YDS done (`nfl_receiving_yds_process.py`, 7,028 obs, compound = receptions×YPR + aDOT quality): thesis validated RICHLY + a key caveat.** Pooled 2-fold: A 0.2898 → M1 naive Gauss 0.2726 (+0.0172, opportunity>hit-rate 3rd time) → M2 vol-var 0.2730 (**+0.0167, WORSE than M1 by 0.0004!**) → M3 +YPR self-shrink 0.2714 (+0.0183) → **M4 +aDOT-driven YPR 0.2705 (+0.0193, best, wins 2024+2025)** → market 0.2498 (gap 0.0207). **aDOT drives YARDAGE: YPR≈6.9+0.55·aDOT (rock-stable both folds) — deeper target→more yds/reception; the 'underneath' for a yardage market is DEPTH, and it pays.** SHIP M4. **★ KEY CAVEAT — the variance-vs-volume fix is NOT universal: receptions binomial +0.017 (naive var catastrophically wrong), rush_yds Gauss +0.001, receiving_yds Gauss −0.0004 (variance dominated by the fat-tail single big catch, not reception-count var → quadratic-in-volume misspecifies; const sd fine). ⇒ variance model is PER-PROP, decide const-vs-vol-scaled in the sweep, not blanket.** No edge (big-disagreement buckets negative — receiving_yds market efficient). **THREE PROPS SHIPPED (receptions M5b, rush_yds M4, recv_yds M4); consistent: opportunity>hit-rate ~+0.02 every time, gap-to-sharp ~half, decomposition useless, self-shrink helps, aDOT is real signal for both catch-rate AND yardage.** **▶ COUNT-PROPS BATCH done (`nfl_count_props_process.py`, 4 props): NegBin vs Poisson vs hit-rate — textbook overdispersion result.** rush_attempts: A 0.2887 → Pois 0.2829 → **NB 0.2676 (+0.021, φ=0.14)** → mkt 0.2474. pass_attempts: A 0.2961 → **Pois 0.3057 (WORSE than hit-rate — game-script overdispersion, Poisson var too tight/overconfident)** → **NB 0.2772 (+0.019)** → mkt 0.2512. completions: A 0.2909 → Pois 0.2995 (worse) → **NB 0.2815 (+0.009)** → mkt 0.2482. pass_tds: A 0.2768 → **Pois 0.2645 (+0.012)** → NB 0.2646 (≈Pois, φ≈0 — small 0-4 counts genuinely Poisson) → mkt 0.2380. **SHIP NegBin for rush_attempts/pass_attempts/completions, Poisson(φ=0) for pass_tds — the data's φ picks the model.** **⇒ SEVEN straight props: distributional/opportunity model beats hit-rate every time (+0.009..+0.021); the variance fix scales with actual overdispersion (huge where naive is most broken, nil where the count is genuinely Poisson). gap-to-sharp 0.018-0.033.** **▶ PASS_YDS done (`nfl_pass_yds_process.py`, 1,163 obs, starting-QB only, compound attempts×YPA): 8th prop, opportunity wins again.** Pooled 2-fold: A 0.2938 → M1 naive 0.2799 (+0.0139) → M2 vol-var 0.2781 (+0.0157, attempts swing w/ game script) → **M3 +YPA self-shrink 0.2771 (+0.0167, best, ship)** → M4 +CPOE YPA 0.2776 (+0.0162, SLIGHTLY WORSE) → market 0.2503 (gap 0.027, the SHARPEST market). **CPOE curve real+positive (YPA≈7.05+0.13·CPOE stable) but REDUNDANT with own recency YPA — opposite of aDOT-helping-receiving.** ★ REFINED RULE: the fundamentals conversion layer helps ONLY when the player's own conversion history is too noisy to trust (aDOT helped receiving YPR; CPOE didn't help pass YPA because QB own-YPA already encodes accuracy). Edge strongly negative (QB market very efficient). **★★ SUITE COMPLETE — 8 NFL props modeled, all beat hit-rate (+0.009..+0.021), all ~half-to-2/3 the way to the sharp close. SHIP LIST: receptions=M5b(NegBin+fund catch), rush_yds=M4(vol-var+ypc shrink), recv_yds=M4(aDOT YPR), pass_yds=M3(YPA shrink), rush_att/pass_att/completions=NegBin, pass_tds=Poisson.** **▶▶ 10-SEASON FOUNDATION + SWEEP DONE (Doug's design: train on deep history/actuals, test on odds era).** Prop LINES only exist 2023-2026 (Odds API historical props start 2023 — hard cap on obs). But we have ACTUALS for every player-game → ingested player_week 2012-2022 (FREE nflreadpy, 194,876 rows/11 seasons; cpoe 4%=QB-only=expected). Built **`nfl_props_accuracy.py`**: actuals-only distributional evaluator — grade the whole predicted DISTRIBUTION vs the actual via log-score + line-grid Brier (over/under swept across many candidate lines = real skill test; grading vs our own point = ~50% by construction, useless). **TRAIN components 2012-2022 (φ, aDOT→conv curve, league rates, vol-variance) → FREEZE → TEST 2023-2025 (accuracy + market ref).** **★ KEY RESULT: models hold across the 11-yr gap — disjoint-era gaps-to-sharp reproduce the 3-season probes almost exactly (recept +0.0175, rush_yds +0.017, recv_yds +0.019, pass_yds +0.029, rush_att +0.016, pass_att +0.020, compl +0.030, pass_tds +0.026). NON-STATIONARITY IS A NON-ISSUE — process relationships stable across a decade.** Model beats naive per-line base rate on actuals-only grid-Brier for all 8 props. **▶ HP SWEEP (fit 2012-2020 / validate 2021-2022, holdout 2023-26 untouched; obj=val log-score) → NOT marginal.** Winners HOLDOUT-CONFIRMED on 2023-2025: **QB props want SHORT half-life HL=2 (pass_yds/pass_att/completions) — big real gain (completions gap 0.0302→0.0207, pass_att 0.0204→0.0164, pass_yds 0.0285→0.0259); MIN_PRIOR=4 for 7/8 (partly compositional — abstains on <4-game-history players, a legit betting choice); K=40 heavy conv-shrink for yardage props.** **▶ ALSO ran nested-CV INSIDE the odds era (--sweep-modern, Doug's ask: select on 2 modern seasons/test on 3rd/rotate, components frozen 2012-2022) — the rigorous version. AGREES with the forward sweep and STRENGTHENS the headline.** **★★ QB-VOLUME HL=2 win DOUBLY-CONFIRMED — rock-stable across ALL folds in BOTH schemes: pass_att Δ+0.257, completions Δ+0.221, pass_yds Δ+0.161 logscore. Not overfitting — QB passing volume genuinely wants heavy recency (scheme/matchup/role shift fast).** pass_tds wants OPPOSITE = LONG memory (HL=8, rare/noisy). rush_att(+0.029)/rush_yds(+0.020) modest real gains, MP=4. **HONEST NULLS: recv_yds tuning HURT (−0.005, default wins); receptions negligible → both revert to default (no forced knob).** **★ FINAL CONFIG (nested-CV-confirmed, in `nfl_props_accuracy.SWEPT`): recept(4,3,20) rush_yds(6,4,40) recv_yds(4,3,12=default) pass_yds(2,4,40) rush_att(4,4,12) pass_att(2,4,12) compl(2,4,12) pass_tds(8,4,12).** Forward-sweep→2023-25 holdout ALSO confirmed gains carry (completions gap 0.030→0.021). Method fully validated two independent ways. **▶ CLV/TIMING BACKTEST (`nfl_props_clv.py`, early_4h vs closing, frozen model, 2023-25): NEGATIVE — no capturable timing edge in the −4h→close window.** Clean same-line price CLV ≈0 every prop (+0.0..+0.1%, noise); apparent rush_yds(+0.055)/recv_yds(+0.039) CLV-pts is a MARKET-WIDE DRIFT not model skill (mkt→model diag: rush_yds line moves UP whether model says OVER +0.197 or UNDER +0.221 — not directional); ROI-at-early-price negative across all props/buckets. **⇒ by 4h pre-kickoff the prop market is already efficient vs our model.** ★ ASTERISK: early_4h is NOT the opener (props open Tue/Wed; most sharpening happens before −4h). We have NO opener snapshots — mirror only has −4h + close. So this refutes "−4h line beatable" but CANNOT test "Tuesday OPENER beatable." The ONLY logger with a chance = capture the OPENER (once/market, cheap), which needs collecting data we don't have. Config-wiring is a real lift (agent-mapped): NO NFL player-week SERVING feed today (app serves NFL props from ESPN gamelogs, not nfl_data.player_week), and our models aren't an existing method code (A/C NFL; D/E/F MLB-only) → wiring = new method + serving plumbing. So don't wire until purpose is settled. **DECISION POINT (Doug): whether to build an opener-capturing cron logger (he's asking about it).** **▶▶ OPENER LADDER BACKFILL DONE + DURABLE IN AZURE (2026-09-10).** Opener probe found NFL props open ~96-120h out (full by ~-72h; "same-day" was MLB only). Built `nfl_ladder_backfill.py`: dense 6h ladder −6h→−114h (19 rungs; close/−4h already in warehouse for DK/FD/Pinnacle so NOT re-fetched), DK+FD+Pinnacle in one combined call (110cr at −114h opener incl. team h2h/spreads/totals, 80cr props-only elsewhere; Pinnacle rides free at single 'us' region). **Persists DIRECTLY to Azure as it fetches** (Doug's durability rule: NOTHING local is durable → Azure only system of record) via `db_store.capture_odds_snapshot` + `ingest_multibook_cache._per_book_lines`, source tags `ladder_006h..ladder_114h`, kind props/team; resume-aware (preloads durable (event,kind,snapshot_hour) set, skips free). Fetch parallel(10)/write serial(20-DTU). **Spent ~1.27M credits, 531K remaining** (pre-Sep-21 1M+ windfall → 20K/mo after). Data lands in `odds_snapshot`/`odds_line` (no schema change). ⚠ Streamlit floods stderr with ScriptRunContext/No-runtime noise (bypasses logging levels) → in-process stderr filter added. **▶▶ LADDER CLV + BOTTOM-UP + SELECTION ALL DONE (2026-09-11): NFL props efficient w.r.t. everything we can compute.** Analysis reads a one-time targeted Azure→local parquet extract (`nfl_ladder_clv.py --extract`; the full mirror --sync chokes pulling millions of ladder rows at 20 DTU + would bloat the app mirror — ladder stays OUT of the mirror, read Azure directly/scoped). **(1) LADDER CLV (`nfl_ladder_clv.py`, 3-season): NO outcome edge at ANY rung (real-impl flat −0.4% opener→close); CLV→close positive but TINY (+0.25% opener → 0 close, decays as Doug's thesis predicts but far under vig). ≥8% big-disagreement cut also flat-negative.** **(2) BOTTOM-UP opener win/loss search (discovery 2023-24 → validate 2025): only RUSHING props survived both periods — but the DRILL exposed it as the known public OVER-bias (a UNDER play), real-impl +ve all 3 seasons BUT positive ROI only in the −150..−110 JUICED band, FAIR-price ROI −3.74%; sharp confirmation makes it WORSE (not sharp-value). Same juiced-band artifact class as the earlier rush_yds scar. NOT an edge; barely needs the model.** **(3) SELECTION / learn-to-abstain (`nfl_bet_select.py`, Doug's reframe: product = 0-5 high-precision bets/game, VERY STRICT abstaining): COMPLETE NULL. Discriminative won-vs-lost gap ≈0 on EVERY a-priori feature — z_our(conf) +0.024, edge −0.001, conf +0.006, n_prior +0.05, cv/boom-bust +0.006. NO selection rule survives (+real-impl every season AND +ROI at fair prices); the tantalizing z≥1.5 subset (2024 +21.8% ROI!) is winner's-curse — fails 2023 + fair prices + tiny n.** **⇒ won/lost bets STATISTICALLY INDISTINGUISHABLE up front = market efficient w.r.t. all our model's info. NFL props: model accuracy GOOD (calibrated reference/abstain-defensively tool), but NO profitable selectable edge (envelope, timing, OR selection).** Reliable NEGATIVES (usable avoid-filters): pass_yds/pass_completions opener disagreements lose consistently; "sharp opposes our side" loses. **▶ LINE-SHOPPING (DK vs FD) NOW DIRECTLY SCANNED (`nfl_book_shop.py`) → NULL (fairly priced).** DK/FD post DIFFERENT lines 44.3% of snapshots, median a FULL 1.0pt (mean 1.66); ~1.45% odds-shop value at matched lines; lag asymmetry DK-catches-up-to-FD 17.1% vs 3.8% (FD leads, DK lags) — BUT none of it is profitable: best-of-both (model side) ROI −4.5..−5.8%; MIDDLING (over@low+under@high) −3.74% pooled with ZERO free/dead middles (gap-hit 2-20% too low vs vig; books price the gap so no free lunch — NOTE a middle is NOT a hedge/guaranteed, it's +vig-loss on miss); FADE-THE-LAGGARD both directions ROI-negative every season, fair-price −2.2%/−6.8%. **⇒ the 44% divergence is FAIR REPACKAGING + fairly-priced staleness, not exploitable. DK-vs-FD line shopping closed.** Pinnacle deprioritized (MLB: no sharper than DK). **▶ Doug's CROSS-BOOK rule tested (bet OVER at the LOWER line, trusting the higher book as 'informed'): DIRECTION IS INVERTED. over@low ROI −3.6/−5.7/−8.3% (real-impl decays to −ve). The MIRROR (UNDER @ higher line, trust the LOWER book) is the signal: real-impl +1.3/+1.9/+3.7% POSITIVE + STRENGTHENING all 3 seasons ⇒ the HIGHER line is public-over-shaded, the LOWER is sharp. Doug's instinct (divergence=info) RIGHT, sign inverted by over-bias. BUT sub-vig: fair-price ROI −2.07% (~2% edge < ~4.5% juice; only 2025 cleared it +1.2%). Closest model-free all-season-consistent directional signal found — beats the NUMBER, not the PRICE. best_ev (all + divergent-only) null; EV-threshold/required-price doesn't select winners.** **▶ GAP-SCALING (does the fade edge grow with disagreement size?): NO — not monotonic. Big gaps exist (up to 160% of line, real volume to ~20%) but no gap band is +ROI every season. 10-20% band tantalizing (fair +6.6%, 2025 +6.8%) but −3.3%/−3.9% in 2023/24 → fails; beyond 20% = noise (big gaps = high-uncertainty players + wide vig, not mispricing).** **★ FORWARD-WATCH (the one live thread): 2025 SPECIFICALLY flashes +ve on the fade side across TWO independent cuts (fade-shaded-higher-line +3.7%, 10-20%-gap +6.8%) while 2023-24 flat/neg → possible RECENT strengthening of public over-bias (post-legalization $). NOT bettable on 1 season; TRACK 2026: if fade-shaded-line AND 10-20%-gap both go +ROI again, it becomes real. **▶▶ HARNESS-THE-LINE (Doug's alt-line idea) FULLY TESTED → NEGATIVE.** Phase 1 (free, `nfl_line_relative.py`): QB passing props symmetric (no shape edge); rush_yds/recv_yds strongly right-skewed (+1.08/+1.09, mean>>median = fat right tail). Phase 2 (captured DK/FD alt ladders `nfl_alt_capture.py`, ~16.1K credits, 518K left → local `nfl_alt_data/`, NOT odds_line). Backtest (`nfl_alt_backtest.py`, 152K graded): DEEP OVERS clearly −EV, WORSE the deeper (off≥+60% recv_yds −27%, rush_yds −24%) — books OVER-juice the tail, not underprice it. Lone flicker rush_yds DK deep-UNDER off≤−30% +28.5% BUT only 2023(n=252)+2024(n=559), and DK offered ZERO rush_yds alt-unders in 2025 (market DISCONTINUED) → unvalidatable + unbettable. ⇒ DK/FD price alternate ladders efficiently; alt-line mispricing NOT exploitable. Idea closed. Key lesson: 'higher hit-rate on easier alt lines' is PRICED IN (fair-odds); tail overs are over-juiced. **▶ OPPORTUNITY FILTER (Doug's abstain-on-low-opportunity idea, `nfl_opportunity_filter.py`): WORKS for ACCURACY, not edge.** Bucketing prop bets by expected opportunity (targets/carries/attempts): HIGH-opp DRAMATICALLY tightens our Brier gap to market (rush_att 0.022→0.009, completions 0.035→0.010, pass_att 0.030→0.011, receptions 0.028→0.013) — our model is trustworthy on high-opp, role-stale garbage on low-opp. ⇒ APPLY a per-prop opportunity threshold to the fair-value report (abstain below it) — validated defensive win. BUT no betting edge: high-opp (accurate) = where the MARKET is sharpest (workhorses, most-bet) → real-impl ~0/ROI −5..−8%; the only +real-impl whiffs are LOW/MID-opp where we're LESS accurate (rush_attempts MID +3.9% all 3 seasons but ROI only +1.1%, likely 1-of-21-cells noise). THE WALL via opportunity: market efficient exactly where we're good; we're weak exactly where it might be soft. **▶▶ STRATEGIC PIVOT — BONUS/PROMO-DRIVEN +EV (Doug's real play, revealed 2026-09-15).** We won't beat the book on straights (accepted); the +EV mechanism = the book's OWN promos. A profit boost pays on the PAYOUT, so it flips high-probability −EV bets +EV (30% boost: coin-flip −110 → +9.1%; 3-leg 52% parlay → +23% vs −2.2% unboosted). Boosts target PARLAYS/SGPs (books assume longshots) → with ACCURATE high-P legs it flips decisively +EV. THIS is why calibrated prediction mattered (over-confident P = fake +EV). Requires: (a) legs ONLY from TRUSTWORTHY calibrated props (opportunity-threshold work: count props above thresholds + anytime-TD via RZ; abstain on yardage/pass_tds/completions), (b) SGP CORRELATION model (independent product is WRONG for same-game legs), (c) Kelly sizing (high variance). ⚠ Parlays alone COMPOUND the vig (never +EV without a boost or correlation edge); independent cross-game legs OK, SGP needs correlation. BUILT: `bonus.py` engine (Doug's schema + `min_legs` + `book`: bet_type[single/parlay/sgp/any]/boost_pct/min_odds_leg/min_odds_overall/min_legs/max_wager/min_wager → boosted EV + Kelly + constraints; `LIVE_BONUSES` = Doug's 3 real scenarios [25% any max$10 leg≥−200; 30% SGP ≥3legs leg≥−250; 50% parlay/SGP max$10 leg≥−300]; self-test verified incl. min_legs gate). **▶▶ BACKTEST DONE + POSITIVE — FIRST REAL +EV IN THE PROJECT (`nfl_bonus_backtest.py`, 2023-25 DK+FD, cross-game parlays from trustworthy count legs, favorite side, flat $1/ticket).** ★★ THE PROBABILITY MUST BE THE MARKET's DE-VIGGED PRICE, NOT OUR MODEL. Leg reliability exposed our MODEL P is OVER-CONFIDENT in the high-P tail we bet (calls 80%→wins 61%, 70%→57%; ECE 0.063) — aggregate Brier hid it (2/3 of legs in the harmless 0.5-0.6 bin); `--prob model` inflates predEV to fiction + the 25% 3-leg drifts NEGATIVE (optimizer's-curse selection compounds per leg). With `--prob market` (ECE 0.008-0.016, calibrated, hit%≈expHit%) predEV is HONEST + POSITIVE every held-out season, BOTH books: **50% parlay ~+20% predEV / +28-34% realized (worst season +18%); 30% SGP 3-leg ~+6% predEV/+16-19% realized; 25% any ~+5% predEV/+8-15% realized (thinnest — 3-leg marginal).** Realized > predEV because favorites hit slightly above devig-fair in this subset (small, don't lean on it). **⇒ the boost on a CALIBRATED (market-devig) favorite is +EV BY CONSTRUCTION (`mkt_P·(D−1)·boost`); no model edge needed — this is the honest core of the whole strategy. The MODEL's ONLY job = the opportunity/trustworthiness ABSTAIN (field legs only where the market number is meaningful: count props above thresholds; abstain yardage/pass_tds/completions). Bigger boost + fewer legs = safer (bias/vig compounds per leg).** **▶▶ SGP CORRELATION DONE (`nfl_sgp_correlation.py`, fit 2023-24 / validate 2025, Gaussian copula on latent stat X per leg, OVER iff X>t, tetrachoric rho per relationship category from co-over rates; joint via MC over correlated normals; exposes `joint_prob()`).** Correlations among TRUSTWORTHY VOLUME props are REAL but sign-MIXED: **QB volume↔his pass-catcher +0.175; RB committee −0.55; pass-vs-rush (game script) −0.375; opposing volume −0.275** (possession ~conserved → volume props TRADE OFF, unlike yardage/TDs which share the game total +); two same-team catchers ~0. **★ On aggregate 3-leg favorite tickets these ~CANCEL: realized co-hit 15.96% vs independent product 14.77% (copula 15.01%), and independence WINS overall log-loss ⇒ INDEPENDENCE is an adequate, slightly-CONSERVATIVE base joint** (the ~1pp surplus is the same small favorite-bias from the backtest, noise-level). **★★ The copula's robust ACTIONABLE value is COMPOSITION SELECTION, not precise pricing: positively-correlated same-side stacks co-hit FAR more (pos-stack 19.3% vs weak/opposing 14.5%; copula wins log-loss ONLY there, n=83) — independence UNDER-states them ~4pp ⇒ QB+his-catcher SGPs genuinely hit more than the naive product = a REAL SGP edge the book's generic haircut likely under-credits.** OPTIMIZER RULE: independence = safe base joint; PREFER +corr same-side stacks (QB + his WR/TE), AVOID same-team script-conflict legs (two RBs, pass+rush) same-side; use `joint_prob()` copula for EV when a correlated pair is present. **▶▶ OPTIMIZER STAGE A DONE (`nfl_bonus_optimizer.py`, standalone CLI, no app changes).** For each active bonus (bonus.LIVE_BONUSES) + book: pulls trustworthy high-opp legs, LEG P = book de-vig (calibrated), model = eligibility gate only. Emits (a) CROSS-GAME parlays/singles = fully-priced +EV plays with fractional-Kelly stakes (capped by max_wager), greedy NON-OVERLAPPING portfolio (`_diversify`); (b) SGP STACKS = per-game +corr same-side stacks (copula joint P, skips min-rho≤−0.30 script-conflicts) with the REQUIRED combined price for +EV (`need >=`) to check vs the book's live SGP builder. Correctly SKIPS cross-game for bet_type=='sgp' (SGP-only boosts need same-game). Offline source = local ladder extract for a historical --season/--week (verified 2024 wk6 DK: sensible QB+his-catcher stacks, diversified cross-game portfolio); LIVE = swap line source to Odds API board (same leg dict). **▶▶ STAGE B OPTIMIZER SECTION SHIPPED (approved 2026-09-15; design `notes/BONUS_STAGE_B_DESIGN_2026-09-15.md`).** (1) `nfl_opportunity_serving.py` = live eligibility gate: fetches the nflverse `stats_player`/`stats_player_week_{season}.parquet` release DIRECTLY via pandas (NO nflreadpy/polars → honors nfl_ingest's runtime rule; player_week is replaceable → cached w/ 6h TTL self-healing, NOT Azure). `expected_volume`/`passes_gate` recreate the backtest gate byte-for-byte (same VOL_STAT + SWEPT half-life; same-season-only when ≥MIN_PRIOR, prior-season spill only early; verified Lamb/St.Brown/Nabers/Wilson match offline exactly). (2) SGP rho FROZEN → `calibration/nfl_sgp_correlations.json` (sgp.freeze/load_frozen + `--freeze`). (3) `nfl_bonus_optimizer.legs_from_board()` maps the app's `parse_player_props` board → legs (gate + MARKET-devig P + the book's own price; week=99 sentinel=all completed weeks prior for upcoming); `evaluate_slate()` = structured output (cross/sgp + boost_pct/max_wager) shared by CLI+app. (4) `bonus_store.py` = durable active-bonus KV (app_settings table → Azure SQL, the system of record — NOT blob, blobs are dead; seeds from LIVE_BONUSES). (5) `app.py`: nav entry `💰 Bonuses` + route + `render_bonuses()` (bonus-manager CRUD, reuses the Value-Finder-cached board via `st.session_state['bonus_board']` = 0 marginal credits, renders cross-game +EV portfolio + SGP stacks with a builder-price input → exact boosted EV + ¼-Kelly). All py_compile OK; adapter+evaluate integration-tested on a mock board. Gate source = Azure serving feed was the plan but SIMPLIFIED to direct-nflverse-fetch (data replaceable, not the never-destroy class) — no owner-run DDL needed. Cache TTL already 5min (no change). **▶▶ PARLAY TRACKER SHIPPED (Doug: 'tracker next', built 2026-09-15).** TWO normalized tables `parlays`+`parlay_legs` (`db_store` Table defs + specs + NDJSON-store registry; composite leg key via upsert `scope`) — durable AZURE SQL (system of record; **blobs are DEAD** — the local NDJSON file is dev/test-only, guarded by recalibration._ensure_durable). `nullable boost_pct` added to `wagers` too (boosted straights: store RAW price + boost, grade `stake·(dec−1)·(1+boost)`). **⚠ OWNER-RUN DDL = `sql/parlay_tracker.sql`** (idempotent; also mirrored in canonical `sql/schema.sql`) — Doug must run it against Azure before the page persists. `parlay_store.py`: save/load/grade/settle_manual; per-leg grading reuses `recalibration.resolve_one_prop`; all-win=won / any-loss=lost / push-leg=void; boosted profit (unit-tested + SQLite round-trip verified). `app.py`: `🎰 Parlays` nav+route + `render_parlays()` (auto-grades on load; overview + leg-detail; manual settle; ANALYTICS: realized-win vs our-joint-P = live copula calibration, ROI by bonus/legcount/same-game/corr-content, leg hit-rate vs market-devig P, 'which leg broke it'). `render_bonuses` cross-game plays get a 'Log parlay' selectbox (multi-leg only; singles → wagers) + SGP expander 'Log this SGP' after the price input; **logging AUTO-CONSUMES the bonus** (`_consume_bonus` removes the matching (book,label,bet_type) from the active list + persists — a boost is single-use; re-add via the form); `nfl_bonus_optimizer.leg_to_store()` + legs now carry commence_time/game_date for grading. All py_compile OK; full log→settle→profit path verified. **REMAINING: (a) Doug runs `sql/parlay_tracker.sql` on Azure; (b) LIVE-TEST the app (nflverse fetch on Streamlit Cloud, Bonuses+Parlays pages) — I can't run Streamlit here; (c) optional: boosted-STRAIGHT logging UI in My Bets (boost_pct column exists), manual multi-leg parlay add (v1 minimal), capture anytime-TD odds for TD legs.** **PUSHED to origin/main through `2fa8b2c` (2026-09-17: MLB bonus arc complete — legs/gate/SGP wired) (2026-09-16: +sport field, any_parlay/sgp_sgpx bet types, MLB opportunity+threshold scripts, per-event analysis-failure file:line diagnostic) (adds: NFL no-history fallback, MLB props-only coherence-skip, editable parlay wager on log + Parlays-page edit/delete ticket; SGP-restriction memory note)** (Cal pushed on Doug's explicit go): resolve-speed parallel warm-up, use_container_width cleanup, NFL projection migration to our nflverse models, ESPN grading-parser fix, and the alt-line/safe-mode migration to our model. Earlier **pushed by Doug** through `ba2a6bb` (bonus system + Stage B optimizer section + parlay tracker + auto-consume) → deployed to Streamlit Cloud; only `ae320a9` (defensive board-stash guard) still local. Doug ran `sql/parlay_tracker.sql` on Azure (VERIFIED: parlays/parlay_legs tables + wagers.boost_pct all present). ⚠ APP IS STREAMLIT CLOUD (not local) — logs via Manage-app panel; can't read local parquet. ⚠ HONEST LIMIT: historical SGP COMBINED prices don't exist in our data → realized SGP ROI can't be backtested; the copula is validated on realized co-HIT rates, and SGP EV is computed LIVE from the book's SGP price × our copula joint P. Everything else NULL — NFL straight-prop edge hunt exhausted; models = calibrated fair-value + defensive-abstain layer + the bonus leg-eligibility filter.** **▶ DELIVERED (Doug: "forward-watch + wire the models"): (b) `sql/nfl_ladder_separate.sql` — owner-run copy→verify→delete migration to move the ~10M ladder rows out of odds_line into odds_snapshot_research/odds_line_research (de-bloat; the bloat is now timing out ALL Azure reads incl. mirror sync). (a) `nfl_fair_value.py` — LOCAL per-prop report (mirror + frozen models; Streamlit Cloud CAN'T read local parquets so cloud-wiring would need a new Azure nfl_player_week table + serving feed + method = big build for a no-edge tool → skipped). `nfl_fade_watch.py` — local forward-watch of the fade signal (2026, closing snapshot; needs 2026 mirror synced post-de-bloat). **★ KEY MODEL CHARACTERIZATION (from running the fair-value report): the model is STRUCTURALLY ROLE-STALE — over-projects players coming off big games now in reduced roles (Marquise Brown proj 51.5 vs line 24.5), under-projects players stepping INTO roles (Jaylin Lane 16.5 vs line 37.5). The market line encodes this-week role/health/matchup our recency model lacks = the exact info that makes it efficient-for-us. So the report is honestly an AGREEMENT-CONFIDENCE + BLIND-SPOT detector: ≈fair tags confirm a number is reasonable; big disagreements = 'go check the news' flag (our staleness), NEVER a bet.** OPTIONAL improvement: add a role/usage feature (recent snap-share from nfl_snap) to de-stale projections — sharpens the defensive tool but converges toward the market (no edge). SEQUENCE for Doug: run the SQL de-bloat → sync 2026 mirror → fade-watch + fair-value usable. **PUSHED to origin/main 2026-09-11 (`41ab63c`; the whole NFL-props arc + opener-ladder tooling + 2012-2022 nfl_player_week LFS parquets + the de-bloat SQL).** `nfl_ladder_data/` gitignored (re-creatable research cache; Azure = durable source). Opener LADDER data durable in Azure (odds_snapshot/odds_line, source=ladder_006h..114h) + local parquet nfl_ladder_data/ladder_lines__{season}.parquet.** **★★ COMPLETE EFFICIENCY MAP (2026-09-11): NFL props efficient w.r.t. EVERY backtest-able dimension — model prediction, features, opener/timing (CLV), reliability-selection/abstention, AND DK-vs-FD line shopping (divergence/middle/fade). Model accuracy is GOOD (calibrated fair-value + defensive-abstain tool) but there is NO profitable edge findable in historical data. THE ONE LIVING HYPOTHESIS = Doug's TRANSIENT sub-6h staleness (brief momentary mispricings that correct within minutes) — genuinely UNTESTABLE with our 6h-coarse ladder; needs LIVE minute-level polling to test/exploit. Operational engine for it = the REQUIRED-PRICE model (per bet, compute the line/odds needed for +EV given our model; live-alert if DK or FD offers it). Building/testing that = live infra + ongoing modest credits (20K/mo post-Sep-21) — Doug's call.** REMAINING: wire the 8 models + frozen config into real americanfootball_nfl.json (currently TOY placeholder — PRODUCTION change, get Doug's explicit ok first; for live, refit components on ALL 2012-2025 before deploy). **▶ EDGE-FIRST FLAT SCAN across all 10 markets DONE (`nfl_props_scan --by-line/--drill`, 2023-25): no robust edge.** Pervasive recreational OVER-bias confirmed (all overs −5 to −13%, t −7 to −11.5) but fading it doesn't clear vig at bettable prices. Best cell `rush_yds UNDER 30-45` (+3.42% pooled, +every season) DRILL-REFUTED: +ROI only in the −150..−110 JUICED band (+5.15% n=425), NEGATIVE at fair prices (−4.04% n=174) → following the book's juice, not adding info. **⇒ NFL props efficient-for-us, SAME as MLB. Every "beat the sharp close with a model/features/flat-fade" avenue is now exhausted across MLB+NFL.** ★ ONLY untested structurally-different avenue left = **transient soft-line pre-game LOGGER** (catch momentary staleness live; does NOT require out-predicting the close; needs live polling infra + modest Odds-API budget — CONFIRM spend first). Team model reliable-prediction-no-edge. → see NFL section below.
>
> **▶▶ (prior) MLB opportunity-first props summary (PARKED):** batter_hits loop CLOSED+LIVE+PUSHED (rounded-AB fix, refit/promote/reseed, ISOTONIC recal live; commit `63f290c`). **TB method "F" BUILT + gate-measured + committed (`fe3f1c7`, NOT pushed):** F = expected_AB × per-AB bases multinomial (AB/H/HR/TB components + league triple share + barrel/hard-hit nudge), convolved via `stats.bases_sum_at_least`. **GATE (76,706 real-line obs, `refit --tb-diag`): F BEATS naive A/C in every bucket but does NOT beat incumbent NegBin E on volume lines (E 0.2477 vs F 0.2498 pooled; E wins 0.5 & 1.5 buckets; F edges E only in tiny ≥2.5 n~191).** So F ships OFF, gate keeps E. Machinery VALIDATED (opportunity×conditional-dist works, plugs into the gate cleanly, template for RBIs/pitcher). **Next lever to close the ~0.002 gap = xSLG** (deferred data add: schema v6 + Savant re-fetch, FREE-not-credits/multi-hour — de-lucks F's per-AB level). **Divergence EDGE thesis (Doug's leading/lagging HH%) TESTED → NO EDGE** (`b9d9a69`, `refit --div-diag`, 178k obs, both level+spike defs, all 3 seasons): market-implied OVER is HIGHEST in the spiking bucket (0.539) → books already price quality-of-contact; Q4 edge −0.008/−0.010, negative every season. Mechanism real but already in the price. **PREDICTION gate ALSO run (`a24c580`, `refit --spike-diag`): NO incremental value** — hits β=0.00/Δ=0.0000, TB β=−0.05/Δ=+0.0002 (unconfirmed); our recency-weighted projection already captures recent form → HH%-spike redundant. **Thesis FULLY RESOLVED both ways (no edge, no incremental prediction); NOT wired.** Diagnostics kept as infra. **RBI runners-on PROBE also run (`f506e88`, `refit --rbi-diag`, 101k obs): NO incremental prediction** — lineup-OBP β=−0.05/Δ=−0.0001, hr_rate/exp_ab both < gate; incumbent A already embeds the batter's run-driving-in environment. Method-G NOT built. **⇒ THREE consecutive "already-captured" MLB-props negatives this session (TB method F beats naive not E; HH%-spike no edge/no prediction; RBI runners-on no prediction).** Pattern: our incumbent methods + recency-weighted projections already capture the accessible per-player signal; MLB props are efficient for us on PREDICTION too, not just edge. **STRATEGIC DECISION PENDING (Doug):** (a) pitcher-K probe (different structure — BF/leash opportunity; but temper expectations), (b) team-runs capstone (coherence/calibration play, NOT edge — big build), (c) redirect to the one untested EDGE avenue = transient/soft-line pre-game logger (NFL frontier B), or (d) NFL props. **Pitcher-K probe ALSO run (`eb990e4`, `refit --pk-diag`): opportunity thesis NO** (exp_bf collapses vs recalibrated C — over-confidence in disguise; 4th straight opportunity negative). **★ but found a REAL calibration win: live `pitcher_strikeouts` C is over-confident (cv Brier 0.266 > 0.25 baseline), serves RAW with NO recal overlay; recalibration → ~0.252 (+0.0126).** ▶ FIX via existing `refit --recalibrate` path (stage → Doug's promote); audit other un-recalibrated props too. **NEW STRATEGIC PICTURE:** opportunity-first arc conclusively tapped for MLB (4 probes negative); the actionable MLB work left is CALIBRATION HYGIENE (recalibrate pitcher_strikeouts +others), not new models. Bigger pivots still open: transient soft-line logger (only untested EDGE path), team-runs capstone (calibration-only), NFL props. **UNPUSHED: pitcher-K probe + this + earlier RBI commits (`f506e88`,`ac29ac9`,`eb990e4`,+memory).** Principle: KEEP an idea if it improves the model OOS, edge or not.
>
> **▶▶ RESUME (2026-09-09) — INDEPENDENT REVIEW IS BACK (GPT-6) AND FOUND REAL BUGS; several claims RETRACTED.** Reports in `notes/INDEPENDENT_REVIEW_2026-09-08.md` (correctness, 12 defects) + `notes/APPLICATION_IMPROVEMENT_STUDY_2026-09-08.md` (forward program, 12 work packages). **CLAIM CORRECTIONS (verified):** (1) **NO "proven ceiling"** — it's "no reliable incremental signal found YET"; live counterexample = `nfl_totals.py:92` defensive-EPA SIGN ERROR (should be `off_h + def_a`), fixing it 13.465→13.325 RMSE (I caught this bug class in phase-matched, missed it in totals). (2) **batter-K UNDER 1.5 does NOT replicate — it's 2024-ONLY** (`all(judged)` counts 1 season as "replicates"; 2025/26 have zero batter_K rows). Reclassify EXPLORATORY. (3) **NOT leakage-free**: NFL QB identity = majority passer IN the graded game (`nfl_qb_asof.py:147`); MLB real-line calib borrows a ±1-day neighbor game when explicit game_pk missing (`book_line_calibration.py:462`); "clean closing" NFL corpus has post-kickoff captures (`nfl_market_scan/_props_scan` don't check captured_at≤commence). **LIVE NFL MODEL IS BUGGY (my session bugs):** serving calls `nfl_model.predict` WITHOUT gid → rest term always 0 (`nfl_epa.py:430`); UTC-date slice drops night-game week→injury/QB-backup lookup (`app.py:3142`); "non-QB" injury feature INCLUDES QBs → double-counts (`nfl_injury_impact.py:164`). **VERDICT THAT HOLDS:** "no durable edge demonstrated" — market still wins after quote-time filtering (13.23 vs 12.73). **✅ CORRECTNESS PASS DONE (2026-09-09, NOT yet pushed).** Fixed + committed: serving parity (rest-drop + night-game week, `nfl_epa.build_matchup_features` canonicalizes game from spine), QB-in-injury double-count removed, QB identity leakage (fit uses projected starter), post-kickoff quote-time eligibility (both NFL loaders), MLB explicit-ID neighbor-borrow, **totals defensive-EPA sign** (13.46→13.32), **opponent-adjustment sign** (`nfl_power`; still doesn't beat raw). **Re-shipped LIVE honest model** = HFA(1.78)+w_epa(32.4)+w_qb(4.4), injuries/rest DROPPED (didn't help clean), sigma 13.19, fit on FULL 2016-2025 (2478 games); promoted. **P2 safety:** candidate `--diff` now deep-compares nested prop config; feature-cache keys on FEATURE_SCHEMA_VERSION + calibration fingerprint (`serving_fingerprint`). **Sign guards:** `test_nfl_signs.py` (5 tests). **Chronological eval:** `nfl_model --chrono` (expanding-window); over 9 folds QB adds ~nothing (13.286 vs 13.284) — the QB/injury "lifts" were LOSO+leakage. **Turnovers tested → no help** (already in EPA). **✅ REMAINING review items DONE (2026-09-09):** #9 props participation grading FIXED (`nfl_props_scan` uses snap_counts to grade played-zero players as a real 0 and VOID true non-participants — DK voids them, not auto-UNDER-wins; the receptions-0.5 phantom +14.76% candidate is gone; 759 played-zero graded + 301 voided); #11 `nfl_edge_calibration` now uses the FROZEN serving model (HFA+net+QB) + serving prob_shrink (spreads 0.10, ml 0.65) on quote-time-filtered odds — still NO required-edge X* (20%+ bucket −15.6%); #3 beat-the-close re-confirmed with clean inputs (projected identity + quote-time) — QB-change games ~+2-3% but t≈0.3, 2024 red every cut = no durable edge. **ALL review items addressed. PUSHED to `origin/main` 2026-09-09 (commit `76e140d`).**

## ▶ FORWARD PROGRAM (post-review, from the improvement study) — STARTED 2026-09-09
1. **✅ DK/FD offer preservation — DONE (props 5089136 + team/CLV 721b971).** PROPS: `odds_client.parse_player_props` carries `fd_over/under_*`; `props.py` `_best_exec` stakes at max-American of {DK,FD} → `dk_price`/`dk_book` = best executable + book. TEAM: `app.py` builds two views — EXECUTABLE (DK+FD) for the value analyzers (best_price/best_book line-shop DK/FD) + DK-ONLY for coherence + CLV baseline. `backfill_dk_clv` BOOK-AWARE (props+team): fetches DK+FD (cost-neutral — Odds-API bills per market×region), compares each wager to the close at the book it was placed (recorded `book`). Analysis-only books (Pinnacle) still feed only the fair prob. 142 tests pass (props/odds/CLV/wagers). ⚠ next `backfill_dk_clv` run re-fetches previously-cached events once (cache key now includes fanduel; cost-neutral per event).
2. **✅ MLB round(expected_ab) discontinuity — FIXED + LOOP CLOSED (454d46e + 314337f).** `_dist_p_over` uses an ADJACENT-COUNT MIXTURE over ⌊m⌋/⌈m⌉ (smooth in expected_ab; 3.49→3.51 moves 0.21pp, was 10.55pp). Re-fit + promoted LIVE: batter_hits `[(0.5,D,xBA1.0),(1.5,D,xBA1.0),(None,C)]`, combined cv 0.2366 (better than old ~0.2396). Platt reseeded → batter_hits DROPS OUT (clean, no stale key). ⚠ FLAG LESSON: `--real-lines` reads `--xstats-strength` (NOT `--dist-xstats-strength`); with the wrong flag xBA=0.0 and 0.5 wrongly flips D→C.
3. **✅ ISOTONIC recal for batter_hits — DONE, PROMOTED LIVE + PUSHED (63f290c).** Raw D had low-end miscalibration (ECE 0.035) Platt can't fix; isotonic does (ECE→0.006, collapses fake edges). Built `recalibration.apply_isotonic`; `props._apply_final_recalibration` applies a per-bucket `recal_iso` (precedence over Platt); `refit_calibration --recalibrate --save-recal` stages the winning isotonic. Promoted into live `baseball_mlb.json` — both D buckets carry a 46-knot map; fallback C none. Calibration-only (suppresses phantom +EV → correct abstention).
4. **◐ opportunity-first props arc — TB + RBI DONE (both no-go vs incumbent); pitcher-K/team-runs remain.** Design `notes/props_opportunity_model_design.md`. **TB (method "F", `fe3f1c7`):** beats naive A/C everywhere but LOSES to incumbent NegBin E on volume lines (pooled E 0.2477 vs F 0.2498) → ships OFF; xSLG is the only lever left (deferred). **RBI runners-on PROBE (`f506e88`):** lineup-OBP/HR-rate/exp-AB add NO incremental prediction over incumbent A → method-G NOT built. **Divergence/HH%-spike (`b9d9a69`/`a24c580`):** no edge, no incremental prediction. **PATTERN (3 negatives): MLB props efficient for us on PREDICTION too — incumbents + recency weighting already capture the per-player signal.** REMAINING arc (temper expectations): **pitcher-K** (expected BF × per-BF K-rate — BF/leash is a genuinely different opportunity var) → **CAPSTONE bottom-up team-runs** (coherence/calibration play, NOT edge). All diagnostics reusable (`--tb-diag/--div-diag/--spike-diag/--rbi-diag`). Ship OFF; activate only if it clears the OOS gate vs incumbent.
- **★★ GOVERNING PRINCIPLE (Doug 2026-09-10, ELEVATED):** The **product is MODEL ACCURACY, not beating the close.** The edge is realized LIVE by the transient soft-line logger (Frontier B) catching a book posting a number lagged off our accurate model — NOT by out-predicting the sharp close. So "beats the close?" is RETIRED as a kill criterion for model builds; **any change that improves OOS prediction/calibration SHIPS.** This promotes the earlier KEEP-IF-BETTER rule to THE governing rule: better prediction is the goal in its own right; edge is a separate, live-detection concern. → [[modeling-and-calibration]].
- **UNPUSHED:** 1 commit (`fe3f1c7` TB method F). Everything through the isotonic promote (`63f290c`) is on `origin/main`.

## Live-app fixes
- **[IN PROGRESS 2026-09-18] BONUS-DRIVEN ANALYSIS feature (Doug bets bonuses "big time" — his spec).** Reframes the 💰 Bonuses page from "parasite on the Value Finder" to "pick a bonus → it runs its own scoped analysis." Doug's asks: (1) smarter auto-naming, (2) bonus carries a MARKET SCOPE (sometimes a bonus is HR-only or TD-only), (3) select a bonus → run analysis scoped to its sport+markets with our value/edge filter REMOVED (pure book de-vig; boost is the edge), (4) leg-count selector, (5) user picks which GAMES/day the analysis runs on (bonuses are sometimes day/game-specific). Design decisions (Doug): fetch fresh odds for the bonus's markets (costs credits, show estimate + confirm); for non-modeled markets (anytime_td, home_runs) rely on "book posts odds ⇒ player active" (no separate active-check) BUT keep the opportunity-threshold gate for the MODELED props. **DONE + committed (NOT pushed): Phase 1 `0cc9703`** (Bonus.markets attribute + `bonus.display_name` auto-naming "DK · MLB · SGP 50% · HR" + bonus_store persistence + NEW test_bonus.py, 15 tests — the money engine had ZERO coverage). **Phase 2 `2ede5f8`+`c05021d`** (optimizer `_scope_legs` per bonus.markets + leg_count forced size in cross_game_plays/sgp_stacks/sgp_stacks_indep; evaluate_slate threads both, sgp_fn contract now (legs,bonus,leg_count); NEW test_bonus_optimizer.py 8 tests; UI: auto-names in manager, market-scope multiselect, leg-count control). All additive — existing bonuses (markets empty, leg_count auto) byte-identical. **▶ PHASE 3 NEXT (the big piece): the "Run analysis for this bonus" flow** — select bonus → pick games (day/game-specific) → credit estimate + confirm → fetch the bonus's markets via get_event_odds → de-vig → build legs INCL. non-modeled markets (⚠ anytime_td is often ONE-SIDED (yes only) → de-vig needs both sides or a vig'd-implied fallback; home_runs is 2-sided o/u 0.5) → optimize → display. Non-modeled markets can be SET on a bonus now but produce no legs until Phase 3 wires their leg source. **[PHASE 3 DONE 2026-09-18 `98ecaa5`+`af4086f`, NOT pushed]** `legs_from_scoped_board` (any market incl HR/TD via book de-vig + book_calibration; `side` favorite|over) + a "▶ Run analysis for a bonus" expander in render_bonuses: select bonus → pick games (get_upcoming_events, FREE) → side (auto: HR/TD→over, else favorite) + leg-count → credit estimate (#games×#markets) + confirm → get_event_odds per game → parse → SCOPED board in session_state → the +EV plays section renders it (legs_from_scoped_board, INDEPENDENCE SGP, just the run bonus) reusing the display+log. Props-only v1 (team boosts deferred, Doug OK'd). ALSO this session: `--workers N` parallel props backfill (`3bc6453`, budget-safe pre-plan→parallel-fetch→serial-write). ⚠ ALL of Phase 3 needs Doug LIVE-TEST (Cal can't run Streamlit): 💰 Bonuses → Run analysis for a bonus → MLB HR bonus + games → confirm credits → verify "to homer" legs + plays + log. **[PUSHED 2026-09-18 origin/main → `7865e00`: Phase 3 + scoped leg builder + parallel backfill. The whole bonus-driven-analysis feature is now live on Cloud.]** **[PUSHED 2026-09-18 origin/main → `f5059df`: Phases 1-2 + Frontier-B shelve note.]** **[BUGFIX 2026-09-18 `b4cc242`, PUSHED origin/main → `8be5b9f` — Doug's 3 live reports on the bonus/parlay generator.] (1) BOTH-BOOKS leak → FIXED:** `evaluate_slate` looped both DK+FD per bonus and emitted tickets for the wrong book; now iterates bonuses and builds only from `legs_by_book[bonus.book]`. **(2) MISSING YARDAGES/most bet types → FIXED by BROADENING the leg universe:** the universe was TRUSTWORTHY (3 modeled NFL count props) so pass/rush/rec yds, TDs, HR never appeared. Added `NFL_BONUS_MARKETS`/`MLB_BONUS_MARKETS` + `bonus_markets(sport_key)` in nfl_bonus_optimizer; the default (no-scope) leg universe is now this BROAD set (bonuses price off book de-vig ⇒ any captured market is eligible). Modeled count props still pass their opportunity gate; non-modeled (yardages/TDs/HR) price off book de-vig + book-calib shrink. NFL Value-Finder-reuse path now builds via `legs_from_scoped_board` (broad, favorite side) not the count-only `legs_from_board`. Copula RETIRED from the live bonus path — every SGP now independence (broad universe has no rho; independence was the validated adequate/slightly-conservative base). Fixed `_BONUS_MARKET_OPTIONS` "NFL: Anytime TD"→`player_anytime_td`. Compiles; test_bonus+test_bonus_optimizer+test_book_calibration green. **(3) anytime_td "0 book lines" after mirror rebuild → NOT a code bug, DATA finding:** Azure has 47,178 anytime_td rows but ALL are `bookmaker='pinnacle'` (Odds API historical never returned DK/FD anytime_td); the read filters bookmaker=draftkings per the DK/FD-only rule ⇒ book-calibration for anytime_td is BLOCKED (no DK/FD history), but the LIVE bonus flow still fetches DK/FD anytime_td fine. Mirror rebuild can't fix it (data doesn't exist). Strong prior it's calibrated anyway. **▶ Doug LIVE-TEST the broadened generator (verify yardages/TDs now appear + single-book only); then push authorization.** **[LIVE-TEST FINDINGS + FIXES 2026-09-18, PUSHED origin/main → `92a58f9` (Doug in push-to-test loop).]** (a) **Per-market leg-breakdown diagnostic** (`5b0fca9`, PUSHED): replaced the STALE "legs come only from count props / yardage not used here" caption with ✅built-N / ⚪on-board-0-legs / ✖not-fetched per market. Doug's slate: yardages DO build (pass 12, rush 37, rec 73) — the "missing yards" were just OUTCOMPETED in the top-K, NOT missing. (b) **Only ~3 parlays from 119 legs** = TOP_N=12 (only top-12 legs by P fed the builder) × non-overlapping `_diversify` (each leg used once → ~floor(12/3)=4 tickets) × +EV-only. Bumped **TOP_N 12→18**. Selection logic: leg P = book de-vig (calibrated, NOT model); sort by P, top-N, enumerate ≤MAX_PARLAY(3) cross-game combos, boosted EV = P·(D−1)·(1+b)−(1−P) independence joint, keep +EV, rank by EV, greedy non-overlap → TOP_K=8; favorite side (occ→over); ¼-Kelly capped by max_wager. (c) **"More likely to hit" knob** (Doug's ask): `min_leg_p` threaded evaluate_slate→cross_game_plays/sgp_stacks_indep; UI slider 0.50-0.85 drops legs below that book-devig hit prob (safer, pays less). (d) **Occurrence markets (anytime_td + batter_home_runs) now ALWAYS bet OVER** (new `_OCC` in optimizer; was taking the favorite=No side since <50% to score). **DECISION (Doug 2026-09-18): price one-sided TD/HR via PINNACLE-REFERENCE de-vig** — parse_player_props already de-vigs Pinnacle's two-sided line into over_implied (Pinnacle = ONLY book posting both sides for TD/HR; DK/FD post Yes-only), stake at DK/FD Yes price (Pinnacle stays analysis-only, honors DK/FD-only rule). Works through existing parse ONCE the market is FETCHED. **⚠ OPEN: Doug on the Phase-3 "Run analysis for a bonus" flow (NOT Value Finder) saw `✖ player_anytime_td: no odds fetched` — anytime_td is in the default broad universe so a no-scope bonus fetches it; if it STILL shows ✖ with TD in scope after this deploy, the live us-region feed isn't returning two-sided Pinnacle TD → add the region carrying Pinnacle to the Phase-3 fetch (regions cost credits). New ⚪-vs-✖ diagnostic disambiguates. HR book-calib "0 lines" = SEPARATE pending HR historical backfill, not the live path.** **[BOOK-CALIB PINNACLE ROUTING + legs=1 guard 2026-09-18, PUSHED origin/main → `ad87060` (+`1eb8286`).]** (a) **Occurrence-market book-calibration now reads PINNACLE** (Doug's ask "use the same devig from pinnacle for HR and TDs"): `warehouse.load_prop_lines` gained `bookmaker=` (default draftkings, threaded to the per-book mirror/db read which already supported 'pinnacle'); `book_line_calibration.OCCURRENCE_MARKETS`+`book_for_prop()`; `harvest_real_line_book_lines` AUTO-routes occurrence props→pinnacle, else DK. **VERIFIED: anytime_td harvests 11,908 two-sided Pinnacle lines (was 0 DK-filtered), 702/800 sampled join to nflverse actuals.** (b) **`refit_calibration --book-calib --recal-prop all`** loops every bettable market for the sport (from `_opt.bonus_markets`); no-data markets print "0 book lines" & skip. `_book_calib_all_props()`. (c) **batter_home_runs: Doug RAN the HR backfill (2026-09-18) — it landed under DRAFTKINGS (2023: 41,964 rows; 2024: 7,912), and DK posts HR TWO-SIDED (Over/Under 0.5, 20,982 O + 20,982 U). ★ FIXED `443c579`: HR was wrongly routed to Pinnacle (Cal's bad assumption — HR is NOT one-sided like TD) → renamed the routing set `OCCURRENCE_MARKETS`→`PINNACLE_REF_MARKETS=("player_anytime_td",)`; HR + everything except anytime_td reads DraftKings. HR now harvests 24,938 two-sided DK lines → book-calib works.** (2025/2026 HR = 0 in mirror; extend the backfill if more data wanted.) The live optimizer already priced HR fine off the DK/FD two-sided board (optimizer `_OCC` still forces the OVER side — separate concern, correct). anytime_td stays Pinnacle-only (genuinely one-sided at DK). **[BOOK-CALIB SWEEP RESULTS 2026-09-21, Doug ran on his machine.] ★ The book de-vig is CALIBRATED almost everywhere (validates the bonus foundation: book P≈true P ⇒ boosted EV honest). WINNERS: MLB batter_hits→platt (NOISE: Brier identical 0.2333, ECE 0.0093→0.0069 — SKIP), batter_total_bases/batter_rbis/pitcher_strikeouts/pitcher_outs→RAW (calibrated), pitcher_earned_runs→PLATT (real: ECE 0.0227→0.0100, BSS +0.0009, n=6471 — SAVE), batter_home_runs→PLATT (n=3378 2024-only: ECE 0.0297→0.0195, BSS +0.0050, a=0.769/b=-0.357 — SAVE, shrinks DOWN = protective for longshot HR legs). NFL player_anytime_td→PLATT (n=10766 Pinnacle: ECE 0.0229→0.0132, BSS +0.0020 — SAVE, the biggest TD-market win; Pinnacle TD de-vig slightly over-confident). ⇒ STAGE (--save-recal): pitcher_earned_runs, batter_home_runs, player_anytime_td. SKIP batter_hits + all raw. **NFL FULL SWEEP DONE (Doug ran 2026-09-21):** SAVE player_rush_yds→platt (n=4151, ECE 0.0301→0.0043, BSS +0.0038, a=1.084/b=-0.125) + player_anytime_td→platt (above). SAVE-WITH-WATCH player_rush_attempts→isotonic (n=2701, ECE 0.0444→0.0158, BSS +0.0060 but HEAVY remap mean|e|=0.056/868 rows>5% — isotonic overfit risk on modest n). RAW/skip: receptions, reception_yds, pass_tds, pass_yds (ECE already 0.0040), pass_attempts (platt INVERTED a=-0.294 disqualified), pass_completions (noise BSS +0.0001). **★ PATTERN: corrections cluster in LONGSHOT + RUSHING markets (HR, anytime_td, rush_yds, rush_attempts) — book over-prices the OVER, shrink pulls P DOWN = PROTECTIVE for bonus over-legs (kills fake +EV before parlay compounding). Passing/receiving already calibrated. ~11 of 16 measured markets = raw (book de-vig calibrated); validates bonus foundation.** FINAL STAGE LIST: ER+HR (MLB), rush_yds+anytime_td (NFL) high-conf, rush_attempts (NFL) watch; SKIP batter_hits+pass_completions (noise) + all raw. Workflow: `--recal-prop all --save-recal` then review git diff, drop noise maps, keep the 5. ★ HR grading fix (`2258d20`, add HR→PROP_STAT_MAP + WAREHOUSE_ONLY): grades 2024 (3,378 obs); 2023 HR (~20,995 lines, the backfill bulk) BLOCKED — warehouse has NO 2023 batter gamelogs; ingesting 2023 MLB gamelogs would ~7x the HR sample.** (d) **DOUG TO RUN (his machine, free/diagnostic):** `python refit_calibration.py --sport nfl --book-calib --recal-prop all` + `--sport mlb ...`; add `--save-recal` to stage winners into `calibration/book_calibration_<sport>.json` (review git diff → commit). winner=raw⇒calibrated/no-op; platt/isotonic⇒shrink beats raw OOS (watch 0.6-0.9 favorite band). (e) **legs=1 UX guard** (`1eb8286`): setting 1 leg on a parlay/SGP bonus correctly yields nothing (1-leg parlay invalid) — page now names the bonus + says use ≥2 legs or set type single/any. NOT a bug (verified singles work for any/single types). 66 tests green (warehouse/mirror/book_calib/bonus/optimizer). **[LOTTERY MODE 2026-09-18, PUSHED origin/main → `25ac1a6`.]** Doug wants up-to-10-leg "hail-mary/lottery" parlays. Split caps: `MAX_PARLAY=3` = AUTO-sweep cap (perf), new `MAX_FORCED_LEGS=10` = user-forced cap (cross_game_plays clamps). `require_positive_ev` (default True) added to cross_game_plays/evaluate_slate; UI "🎰 Lottery mode" checkbox flips it → builds highest-EV ticket even when −EV. Both leg-count inputs (Phase-3 expander + reuse) now allow ≤10. **★ WHY it needs a −EV escape hatch: a long parlay COMPOUNDS the vig faster than any boost offsets — break-even ~8-9 legs even at 50% (verified K=3 +23.9%, K=8 +7.7%, K=10 −0.4%); +EV-only correctly shows 0 at K=10, lottery shows the 1 ticket.** Cross-game K-leg needs K distinct games (empty-msg hints this + Lottery mode). Kelly stake→0 on −EV (Doug sets his own hail-mary wager). **★ KELLY POLICY (Doug asked "guaranteed edge → full-Kelly?" 2026-09-18): NO — keep FRACTIONAL.** The boost edge is +EV only if leg P (market de-vig) is calibrated — a statistical property over many bets, NOT a per-ticket guarantee (that's why we suppress over-confident props); +EV ≠ guaranteed profit (parlays are high-variance). Full-Kelly assumes EXACT P and is savage under estimation error + parlay variance + SGP correlation + simultaneous same-slate tickets (per-ticket Kelly ignores aggregate exposure). Current ¼-Kelly is correct; ½ is the aggressive ceiling; full-Kelly = ruin risk, keep OFF. max_wager caps often bind anyway. Phase 3 will expose the Kelly fraction as a UI knob (¼/⅓/½), full-Kelly gated/omitted.
- **[RESULT 2026-09-18] BOOK-CALIBRATION diagnostic (`--book-calib`, Doug's idea) → the sharp de-vig is CALIBRATED; bonus leg Ps are trustworthy.** New `refit_calibration.py --book-calib --recal-prop <prop>` measures the BOOK's own de-vig P vs realized (reuses the recal harness; "book" method in `_rc_run_bucket` = raw p is `mkt_over`). Fixed to need NO model cfg (`book_only=True` in `_cc_load_scored_rows`, commit `13ce1aa`) so NFL toy-cfg props measure with zero backfill. **RESULTS (Doug ran, closing-line corpus):** batter_hits n=103K reliability near-PERFECT (0.5-0.6→.557, 0.6-0.7→.637, 0.7-0.8→.695; ECE 0.009; platt "win" noise-level, Brier identical); batter_total_bases n=77K **winner=RAW** (no map beats the book); pitcher_earned_runs n=6.5K tiny over-bias (ECE 0.023→0.010, Brier 0.2469→0.2467, negligible + small n). ⇒ **NO shrink worth wiring for measured markets — a book-calib layer would be ~no-op. This VALIDATES the bonus foundation: boosted EV is honest because book P ≈ true P.** (Consistent with the efficient-market wall — here it works FOR us.) Diagnostic is measure-only (staging deferred; not needed since no correction found). **▶ PENDING: (a) Doug book-calibs NFL props (free now); (b) Doug pricing a closing-line backfill for HR/TD (`backfill_historical_odds.py --props batter_home_runs/--props player_anytime_td --snapshot close --warehouse --dry-run`) — those markets' odds were never captured, so they can't be book-calibrated until backfilled OR until Phase 3's fetch accrues them free. Strong prior (book calibrated everywhere measured) = HR/TD likely calibrated too. ⚠ backfill = CREDITS, confirm the dry-run estimate before firing.**
- **[2026-09-18] NFL prop grading → nflverse (fast) + book-calibration APPLICATION layer wired.** (#1 `ee1dea0`) `join_book_lines_to_actuals` graded NFL props via per-player ESPN gamelogs (slow, 404s, hangs — Doug's book-calib receptions run choked). We already HAD `nfl_data.player_week` (local parquet, 2012-2025, all stat cols) — just unwired. New `book_line_calibration._nfl_grade_book_lines`: game_date→(season,week) via nfl_schedule, then (norm name, season, week)→player_week row, read the prop stat (`_NFL_PROP_COL`; anytime_td = rush+rec TD≥1). NFL now grades from local parquet in seconds (verified: Kelce 3 rec / Mahomes 291 on 2024-09-05); MLB(warehouse)/NBA(ESPN) paths unchanged. Fixes NFL book-calib speed AND NFL grading generally (ESPN NFL gamelogs were buggy). ⚠ I WRONGLY claimed "no NFL warehouse" — corrected; the data existed. (#2 `1a68a19`) NEW `book_calibration.py` (load/save/apply per-market platt/isotonic; IDENTITY when no map). `--book-calib --save-recal` stages the winning shrink into `calibration/book_calibration_<sport>.json`; the bonus optimizer (legs_from_candidates MLB + legs_from_board NFL) applies it to each leg's over-prob before deriving P. No file yet = identity (byte-identical until a map is committed). Tests: `_nfl_grade` verified on real data; test_book_calibration 8. **▶ DOUG: (a) re-run NFL book-calib (fast now); (b) `--book-calib --save-recal --recal-prop pitcher_earned_runs` to stage the small ER over-bias shrink (ECE 0.023→0.010), review git diff + commit; HR/TD auto-apply once backfilled+measured. Book-calib commits `a484049`/`13ce1aa`. **[PUSHED 2026-09-18 origin/main → `542a10a`: the whole bonus arc + book-calib + NFL nflverse grading + backfill fixes. Doug RAN the ER book-calib save + committed `542a10a` = `calibration/book_calibration_baseball_mlb.json` {pitcher_earned_runs: platt a=0.950 b=-0.099 n=6471} — the ER over-bias shrink is now LIVE; the optimizer prices ER legs off the corrected P (~0.55→0.523).]**
- **[AUDIT+FIX 2026-09-17] Azure self-maintenance audit (Doug: "verify all tables keep themselves updated by using the app") — one real bug found + fixed.** Read-only freshness sweep (`c:/tmp/azure_freshness_audit*.py`, reusable). **HEALTHY/self-updating:** odds_snapshot (captured today), mlb_game finals (daily thru 9/16) + schedule (thru 9/27), mlb_batter/pitcher_game gamelogs (fetched_at 0.2d ago, 2026, ~2894/1159 rows for last-10d games — the "gamelog backfill" IS working), statcast_day/pitcher_asof_daily/prediction_log all current. **BUG (FIXED `5a2ce8c`):** `mlb_team_standings` frozen at 2026-08-13 — `ingest_maintenance` (hourly daemon refresh) did schedule+finals+handedness but NEVER `ingest_standings` (that only ran via manual CLI `--standings`). So get_team_standings/defense/offense served 5-week-stale records/run-env. Fix: ingest_maintenance now snapshots standings each run (idempotent, 1h-cached, fail-open). **NOT a bug — the coherence watermark 2026-08-30:** DK TEAM lines were captured daily thru Aug 30, then a GAP Aug 31–Sep 15 (team slates weren't completing during the phone-timeout window), resuming tiny Sep 16-17 (22/6 rows vs ~200/day). Those ~15 missing days can't be backfilled (never captured) but are negligible in a 6,884-triad mean; the offset self-heals forward as team slates run. ⚠ Doug can `python mlb_warehouse.py --standings` once to refresh standings NOW rather than wait for the next app-driven maintenance.
- **[PUSHED 2026-09-17] origin/main → `77afd53`** (Doug authorized "push"). Delivered the standings self-maintain fix (`5a2ce8c`) + pitcher_K recal diagnostic generalization (`1218aa3`) + memory. Cloud now self-updates standings on ordinary app use.
- **[RESOLVED 2026-09-18] pitcher_strikeouts SUPPRESSED — no per-player signal beats the base rate (COMMITTED `9fe837a`, NOT pushed).** C-vs-E investigation done via `--real-lines --dry-run --props pitcher_strikeouts` (n=6669): method C Brier **0.2652** beats per-player method A (0.274) but NEITHER C nor E beats a constant at the base rate (~0.2496 for over-rate 0.478) — and the bake-off kept C over E, so E doesn't help either. ⇒ pitcher_K has no per-player predictive signal beyond what the book prices (this is WHY the recal Platt collapsed to slope≈0). The over-confident raw C surfaced fake edges losing −9.1%. FIX: added `pitcher_strikeouts` to `value_gate.suppress` in calibration/baseball_mlb.json (with pitcher_outs, totals) — still computes/displays for tracking, never recommended; reversible; cleaner than shipping a degenerate constant-prob recal. Matches the pitcher_outs treatment + the "MLB props efficient for us on prediction too" verdict. The generalized recal diagnostic (`1218aa3`) + base-method Platt staging (`d2017f3`) remain as reusable tooling. batter_hits recal (isotonic) untouched. ⚠ The 2026-09-05 refit's choice of C over E for pitcher_K stands (C is less-bad); no method change needed since it's now suppressed. **[PUSHED 2026-09-18 origin/main → `e13637e`: suppression `9fe837a` + recal tooling `d2017f3`/`1218aa3` + memory.]**
- **[MIGRATED 2026-09-15] Value Finder NFL projections → OUR nflverse models (dropped ESPN for NFL).** Doug: "I thought we weren't using ESPN?" — right for our MODELS/bonus work (nflverse) + MLB (warehouse), but the app's Value Finder still projected NFL/NBA props from ESPN gamelogs. ESPN's NFL gamelog now returns ONE row with a `labels` array that has DUPLICATE keys (`YDS` at idx2=passingYards AND idx12=rushingYards; same for `TD`) → the parser's last-write-wins made QB `pass_yds` read RUSHING yards (Josh Allen projected 23 not ~250) + several props unmatched. FIX (migration, not ESPN-dependent): (1) `nfl_props_accuracy.freeze_models()` froze the 8 models' components (fit 2012-2022) → `calibration/nfl_prop_models.json`; (2) `nfl_opportunity_serving._KEEP` widened to all feature cols; (3) NEW `nfl_prop_serving.py` = build_obs (live nflverse features, mirrors `_obs_from_series`, SWEPT half-life + prior-season spill) + `project()` (proj mean/sd + p_over via `proj_mean_sd`/`p_over`, frozen comp); (4) `props._nfl_model_override()` replaces avg_stat/over_rate/curr_games for the 8 modeled NFL props in `analyze_player_props_value` (fail-open → ESPN fallback for anytime_td/interceptions we don't model). All 8 app NFL prop keys match our model keys. Verified live: Allen pass_yds 261 / Cook rush_yds 82 / St.Brown rec 7.8. [ALT-LINES MIGRATED 2026-09-15] Safe-mode/alt-line NFL now ALSO uses our model: `nfl_prop_serving.project` returns a `p_at(line)` closure (SHRINK_K re-pinned, order-safe); `props` safe-mode uses `nfl_ctx["sd"]` for the alt spread + `p_at(threshold_line)` for the per-threshold probability (ESPN empirical kept as an independent sanity-guard). combined_mult=1.0 for NFL (our projection is final). Verified monotonic (Allen pass_yds 0.85@175.5→0.32@299.5). **[ALSO FIXED 2026-09-15] ESPN parser root cause (fixes NFL GRADING).** `espn_client.get_athlete_gamelog`/`_parse_stat_row` now ALSO key each stat by ESPN's unambiguous machine `names` (passingYards/rushingYards/receptions/...), and `PROP_STAT_MAP` PREFERS those (+ added the previously-MISSING NFL entries: receptions, reception_yds, pass_tds, rush_attempts, pass_attempts, pass_completions). Verified: Allen pass_yds now reads passingYards [334,283,273,...] (was rushing [23,66,33]); rush_yds distinct. This fixes `resolve_one_prop` → NFL prediction_log grading + parlay-tracker leg grading (they don't use our models — they need the game's ACTUAL, which ESPN now labels correctly). The projection MIGRATION (above) is independent (our nflverse models). Spillover fix `ba2a6bb` now only benefits NBA (NFL projection = our models) but is harmless. ⚠ anytime_td grading still uses ambiguous 'TD' (needs rush+rec TD sum) — separate, not one of the 8.
- **[FIXED 2026-09-15] Value Finder "No results" early NFL season.** ROOT CAUSE (not the bonus work): ESPN `get_athlete_gamelog` returns CURRENT season only → NFL wk1-2 = ~1 game/player → `app.py` `games_sampled>=5` filter (line ~3805) dropped EVERY prop → all categories empty → "No results. Select markets or props…". Confirmed live (Josh Allen `[23.0]`, 1 game). FIX: `espn_client.get_player_stat_history` now SPILLS into prior season(s) when the current gamelog is thinner than `n` (anchored on most-recent game year, ≤2 look-backs, dedup by date; **no-op in-season** len>=n; MLB unaffected — warehouse-only). Verified: Allen/Cook now 20 games (2026 wk1 + 2025). Same chokepoint serves both the direct-ESPN and Cloud SQL (`gamelog_store`) paths. Also `ae320a9`: defensive try/except around the Value-Finder bonus_board stash.

- **[UX 2026-09-15] Bonuses page: no-plays now self-explains.** Root cause of "no parlays": the optimizer's eligible legs come ONLY from `TRUSTWORTHY` = receptions/rush_attempts/pass_attempts, but the DEFAULT NFL markets are anytime_td/rush_yds/pass_yds (ZERO overlap) → board has no eligible legs. **the NFL prop dropdown only offered anytime_td/rush_yds/pass_yds — Doug COULDN'T select the count props at all** (`PLAYER_PROPS_BY_SPORT['americanfootball_nfl']` was those 3). FIXED: expanded to all 8 modeled props (pass_yds/pass_tds/pass_attempts/pass_completions/rush_yds/rush_attempts/reception_yds/receptions) + anytime_td; added alt-line mappings for reception_yds/receptions/pass_tds (dropped uncertain attempts/completions alts to avoid wasted credits). `render_bonuses` also shows slate game-count + eligible-leg count and WARNS which count-prop markets to select; also flags 1-game slates (no cross-game parlays, SGP only). ⚠ NOTE on "no recommended STRAIGHT bets": EXPECTED — NFL props are efficient for us; our model doesn't beat the market, and it's over-confident on favorites (the very reason the bonus strategy uses MARKET-devig leg P). The small positive edges shown are sub-threshold / our own over-confidence, NOT real edges. Real +EV = bonuses on parlays, not straights. (Open design Q: the migrated NFL Value Finder now computes "edge" from our over-confident p_over → systematically fake positive edges on favorites; consider showing NFL projection as reference only, not an edge signal.) [FIXED 2026-09-15] NFL props were also dropped by the no_history early-return when ESPN could not resolve the player name ("N props skipped — no ESPN history"); now for NFL we fall back to OUR model there (synthesize a 1-value history so the loop + override produce the real projection) — recovers players ESPN misses but nflverse has.

- **★ STRATEGIC CAVEAT (Doug live 2026-09-15): books RESTRICT the +corr SGP combos.** FanDuel would not let Doug combine Jameson Williams receptions + Jared Goff pass (same-team QB↔catcher = our +0.175 pos-stack). Books block the very positively-correlated legs that carry the SGP edge (that's why they're +EV for us). ⇒ the pos-stack SGP edge is often UN-BUILDABLE at the book; the SGPs a book DOES allow are the weaker-corr ones. Cross-game parlays (bonuses 1&3) are unaffected (no correlation restriction, legs independent). Our app can't see book build-rules (not in the odds feed) → can't pre-filter. Possible UX: surface top-N SGP stack OPTIONS/game (not just the single best) so a blocked one has vetted alternatives. This tempers the SGP side of the bonus thesis; cross-game remains the clean lever.

- **[2026-09-16] BONUS bet_type vocabulary expanded (engine-level, both sports):** added `any_parlay` (any multi-leg, excludes singles) + `sgp_sgpx` (SGP or SGPx = same-game or SGPs combined across games) to `bonus.evaluate` type_ok + the app form. Optimizer composition routing via `_allows_cross`/`_allows_sgp` (cross: any/any_parlay/parlay/single/sgp_sgpx; sgp: any/any_parlay/sgp/sgp_sgpx). Bonus also gained a **`sport`** attribute (NFL/MLB) — form/table/store/per-slate filtering (`a1eae94`).
- **▶▶ MLB BONUS ARC STARTED (Doug 2026-09-16: extend boosted parlays to MLB, VALIDATED opportunity thresholds + BUILD MLB SGP correlation).** Sequence: (1) opportunity projection ✓ (2) threshold backtest [Doug runs] (3) MLB SGP correlation (4) MLB bonus ROI backtest (5) optimizer+app wiring. **STEP 1 DONE + VALIDATED: `mlb_opportunity_serving.py`** — recency-weighted expected VOLUME as-of a game from the StatsAPI warehouse (`mlb_warehouse.get_player_history`): batters→PA (plate_appearances), pitchers→OUTS (pitcher_outs values); `expected_opportunity(mlb_player_id, prop, as_of_date, season)`. Verified live vs warehouse: Judge 4.0 PA, Ohtani 4.6 PA, Skubal 19.4 outs, Wheeler 16.8 outs. ⚠ resolve_mlbam_id returns a (id, flag) TUPLE — unpack [0]. Recency `_RECENCY`={batter:(hl5,mp4), pitcher:(hl3,mp3)} placeholder — tune in the threshold backtest. **STEP 2 BUILT (`mlb_opportunity_threshold.py`) — DOUG RUNS.** Substrate: `book_line_calibration.harvest_real_line_book_lines("baseball_mlb", props, snapshot="closing")` + `join_book_lines_to_actuals(book_lines,"baseball","mlb")` (warehouse actuals + strictly-earlier in-season `prior_games`); opportunity via `mlb_opportunity_serving.opportunity_from_rows(prior_games, prop)` (leakage-safe, from prior_games). Metric = MARKET calibration gap |mean devig-implied − mean realized| by opportunity bucket (the bonus uses market P, so market calibration IS the criterion). Finds lowest opp T (per prop) with discovery cal-gap<=tol, confirmed held-out. CMD: `python mlb_opportunity_threshold.py --discovery 2023,2024,2025 --val 2026 --tol 0.03`. OUTPUT → the MLB `TRUSTWORTHY` thresholds. **★ STEP 2 RESULT (Doug ran 2026-09-16, 327K matched lines, discovery 2023-25 / held-out 2026, tol 0.03): the MLB MARKET IS CALIBRATED AT ALL OPPORTUNITY LEVELS** — GATE landed at the LOWEST bucket for every prop (cal-gap ≤ tol even at min opp). UNLIKE NFL (where high-opp tightened the gap), MLB market de-vig is trustworthy across the board ⇒ the MLB gate is a PARTICIPATION/QUALITY filter (real starter, leg resolves + not a longshot), NOT a calibration cutoff. Per-prop held-out cal-gap: batter_hits 0.006 ✅, batter_total_bases 0.004 ✅, batter_rbis 0.012 ✅, pitcher_strikeouts 0.023 ✅, pitcher_outs 0.010 ✅, pitcher_earned_runs 0.032 ⚠ marginal (ER noisy), **batter_strikeouts EXCLUDE** (2026 held-out n=0 — not offered/captured 2026 — + higher disc gap 0.02). Small residual public over-bias (batter over% ~2% below implied, same pattern as NFL). ⇒ MLB leg universe = 6 props (all but batter_strikeouts); thresholds are participation floors (proposed: batters PA≥3.4 = regular starter, pitchers outs≥15 = ~5 IP real start) + live lineup/probable confirmation. **STEP 3 BUILT (`mlb_sgp_correlation.py`) — DOUG RUNS.** MLB same-game copula (reuses nfl_sgp_correlation's bvn/ppf/tetrachoric primitives) with MLB categories: team_batters (same team, +runs), opp_batters, pitcher_vs_opp_bat (pitcher dominates → opp batters suppressed, −), pitcher_own_bat (~indep), opp_pitchers. Substrate = same book_line_calibration corpus grouped by game_pk (team_id from test_game). CMD: `python mlb_sgp_correlation.py --fit 2023,2024,2025 --val 2026 --freeze`. Freezes → `calibration/mlb_sgp_correlations.json`. **Also FIXED a pre-existing live bug (`0a5f43f`, props.py:1961): analyze crashed 'NoneType.get' when an ISOTONIC bucket map changed over_rate but Platt recal_cfg was None (hit MLB batter_hits) → guard `recal_cfg or {}`; found via the per-event file:line diagnostic.** **★ STEP 3 RESULT (Doug ran 2026-09-16, frozen → `calibration/mlb_sgp_correlations.json`): MLB same-game correlations REAL but WEAK, independence wins.** rho: team_batters +0.10 (same-team, team runs ✓), pitcher_vs_opp_bat −0.075 ✓, opp_pitchers −0.15 ✓, opp_batters −0.05, pitcher_own_bat −0.05 (|rho|≤0.15, much weaker than NFL). Validation (2026, 3-leg same-game favorites, n=101K): realized co-hit **41.2%** vs independence 43.1% vs copula 43.7%; **log-loss independence 0.6736 < copula 0.6741 → INDEPENDENCE better** (same as NFL — copula doesn't beat it at ticket level). MLB favorites co-hit slightly LESS than product (net-negative corr + ~2pp market favorite over-bias). ⇒ WIRING: MLB SGP uses INDEPENDENCE base joint (frozen rho = reference only; team_batters + = weak composition hint). **▶▶ STEP 5 DONE — MLB BONUS ARC COMPLETE (step 4 ROI backtest SKIPPED per Doug: market calibration already validated + boost mechanism proven on NFL + MLB uses market P throughout).** WIRING: `nfl_bonus_optimizer` gained `MLB_TRUSTWORTHY` (6 props), `legs_from_candidates(candidates, book)` (MLB legs from the app's analysis candidates — market-devig `over_implied`/100 + participation gate `_mlb_gate_ok`: lineup_status≠out AND batter top-6 order / pitcher probable), `sgp_stacks_indep` (INDEPENDENCE joint per step-3), `MLB_ABBR`; `evaluate_slate` now takes `sgp_fn` (NFL copula vs MLB independence). `props.py` candidate gained `fd_over_price`/`fd_under_price` (per-book MLB legs). `app.py`: Value Finder stashes `bonus_board['candidates']=all_props`; `render_bonuses` routes by sport (MLB → legs_from_candidates + independence SGP + MLB diagnostics; NFL → existing); Log flows tag `sport_key=board_sport` + `leg_to_store(..., sport)`. Verified end-to-end on mock candidates (bench batter gated, cross/SGP routing correct per bet_type). ⚠ MLB legs need the analyzed slate cached (Value Finder run w/ batter/pitcher props) + posted lineups (batters gate on batting order). PUSHED 2fa8b2c. Frozen `calibration/mlb_sgp_correlations.json` COMMITTED as a research artifact (NOT load-bearing — live MLB SGP uses `sgp_stacks_indep`).

- **[PERF 2026-09-17] MLB analysis was timing out phones → bulk-prime kills the per-player Azure storm (COMMITTED, NOT pushed).** Root cause (Doug: phone screen sleeps mid-analysis, cancels the run): a whole-slate MLB Value Finder issued ~320 per-player Azure round-trips on the 20-DTU Standard tier — one `mlb_warehouse.get_player_history` (`_game_log`) SELECT per (player, prop) (~240) + one `statcast_asof.get_rates` SELECT per batter (~80-100). Fix = Doug's own parquet idea via `st.cache_data` + the EXISTING bulk seam (`_game_log_bulk`): prefetch the whole season ONCE, serve every player from memory. `mlb_warehouse.bulk_history_maps()` (2 season reads, batter+pitcher) + `set_bulk_history`/`clear_bulk_history` prime a module map; `_game_log` serves from the prime (filter/as-of/sort/limit replicated in Python) BEFORE SQL — checked before `enabled()` so a primed cache needs no live connection; not primed / empty role map → falls through to per-player SQL (offline callers unaffected). `statcast_asof.bulk_rates()` (1 read per season/role/split) + `set_bulk_rates`/`clear_bulk_rates`; `get_rates` serves the partition from memory first. `app.py` Phase-2 MLB warm block primes both via `@st.cache_data(ttl=600)` helpers (`_mlb_bulk_history_maps`/`_mlb_bulk_batter_rates`, daily key), gated on `espn_client._mlb_warehouse_hist_enabled()` + `db_store.enabled()`, fail-open. ⚠ needs `import db_store` inside the block (app.py binds it only as `_db_boot`). Net ~320→~3 round-trips/slate. In-memory logic unit-verified (sort/doubleheader-tiebreak/as-of/limit/unknown-athlete→[]/miss→None). warehouse+statcast suites: same 7 PRE-EXISTING live-Azure-env failures (tests assume isolated SQLite; unrelated to this change), no new regressions. ⚠ if the warehouse-hist flag is OFF on Cloud, MLB history comes from ESPN HTTP not these tables → the prime is a no-op (verify `ODI_MLB_WAREHOUSE_HIST` is set in Streamlit secrets to get the speedup). LIVE-TEST on Cloud (Cal can't run Streamlit): run an MLB slate analysis, confirm it returns fast.

- **[PERF 2026-09-17] "Loading team data…" stall = coherence offset, NOW cached process-globally (COMMITTED `836909a`, NOT pushed).** Doug reported the bulk-prime speedup (b4cd2fc) made NO difference — an MLB spreads+props slate on 3 games still hung at "Loading team data…" (5%). Root cause was a DIFFERENT, EARLIER bottleneck he never got past: for a team-market slate (`markets_str` truthy), `app.py` fits the coherence run-line offset via `coherence_flags.compute_offset` → `r2_data.load_team_triad(sport, ["2024","2025","2026"])` = **3 FULL-season DK-team-triad SELECTs on cold 20-DTU Azure**. It was cached only in `st.session_state` (PER SESSION), so every fresh Cloud session — incl. a phone screen-off dropping the websocket → NEW session — re-paid the whole 3-season read (the per-player props bulk-prime happens LATER in the per-game loop and was never reached). FIX: new `@st.cache_data(ttl=3600)` helper `_mlb_coherence_offset(sport_key, day)` caches it PROCESS-GLOBALLY (first slate/day pays once, every reconnect free); None-when-unfit semantics preserved (skip coherence, never forward-log uncalibrated flags off raw 0.0). ⚠ RESIDUAL: the VERY FIRST run of the day still pays the 3-season read once (unavoidable w/o freezing the offset to a committed JSON like mlb_sgp_correlations.json — offered to Doug as a follow-up if the first run is still too slow). LIVE-TEST on Cloud: run a team-market MLB slate twice; 2nd+ (and any reconnect) should skip the "Loading team data…" stall.

- **[2026-09-17] MLB flag cutover COMPLETE — 9 ODI_MLB_* gates now default ON, escape hatch kept (COMMITTED `2107a1a`, NOT pushed).** Doug: "remove the necessity of these flags" (all "1" in prod); chose "Default ON, keep escape hatch". Flipped every gate helper `in ("1","true","on","yes")` → `not in ("0","false","off","no")` (unset/empty/unrecognized ⇒ ON, only an explicit disable-word ⇒ OFF; behavior-preserving since prod flags all "1"). Helpers: espn_client `_mlb_warehouse_hist_enabled`/`_mlb_warehouse_team_enabled`/`mlb_warehouse_gate_status._on`; props `_identity_enforced` (circuit breaker `_ENFORCE_FAILOPEN_FRACTION`=0.5 still fails OPEN regardless); mlb_starters `_mlb_warehouse_offense_enabled`/`_mlb_additive_runs`/`_totals`/`_ml_enabled`. DEAD flags removed: **ODI_MLB_WAREHOUSE_CALIB** (deleted `_MLB_WAREHOUSE_CALIB_ENV`+`_warehouse_calib_enabled` from book_line_calibration — MLB real-line grading was already unconditionally warehouse; dropped "calib" key from gate_status + app.py display + app.py boot secret-promotion list); **ODI_MLB_STAMP_RESOLVER** (already retired, no reader). Tests updated for default-ON (`_flag_off`/`_team_off`→"0", enforce `""`→"0", savant-path tests explicitly disable ODI_MLB_WAREHOUSE_OFFENSE="0", truth tables ""/unrecognized⇒ON). Affected suites green except the 2 PRE-EXISTING live-Azure `test_p2_finish` precompute failures (baseline-confirmed via stash). **▶ Doug CAN NOW delete `ODI_MLB_WAREHOUSE_CALIB` + `ODI_MLB_STAMP_RESOLVER` from Streamlit secrets outright, and optionally the other 7 (default ON now).**

- **[PUSHED 2026-09-17] origin/main → `0a88a9a`** (Doug authorized "push"). Delivered `836909a` coherence-offset perf fix + `2107a1a` MLB flag cutover + `0a88a9a` memory (remote was already at `b4cd2fc`). Streamlit Cloud will redeploy on this; NEXT: Doug live-tests a team-market MLB slate (2nd+ run should skip the "Loading team data…" stall) + can now prune the dead secrets. **[LIVE-TESTED OK 2026-09-17 — Doug confirmed the coherence fix + bonus/parlay pages work on Cloud.]**

- **[PERF 2026-09-17] First-Analyze maintenance now runs in a BACKGROUND DAEMON THREAD — kills the ~45s-2min cold-start block (COMMITTED `87ac8e6`, NOT pushed).** Doug picked this next (DTU hot-path item #1, the confirmed BLOCKER from the 2026-09-02 audit wf_5093c6d3). Root cause: `props.analyze_player_props_value` (app.py:3796, run across up to 16 concurrent event-worker threads) → `recalibration.maybe_auto_refit` → `maintain_sport` INLINE = StatsAPI ingest + statcast freshness + ~900-1200 SERIAL per-row Azure round-trips (outcome resolution) on 20-DTU, blocking the very slate requested; the hourly gate could also be RACED by the 16 workers → multiple full maintenance runs. FIX (`recalibration.py`): `maybe_auto_refit` flips the hourly gate atomically under a NEW `_auto_maintenance_lock` + `_auto_maintenance_running` in-flight set, then spawns `maintain_sport` in ONE daemon thread → Analyze returns immediately. `maintain_sport` already ran off-ScriptRunContext (pool worker) so DB write/durability is unchanged — just detached from the request lifetime; refit params land for the NEXT Analyze (already true for every non-first Analyze). ⚠ KEPT the in-request trigger — it's the de-facto scheduler; removing it w/o a real scheduler would silently freeze calibration ([[modeling-and-calibration]] note). Verified: 16 concurrent callers return ~4ms (was inline-blocking), collapse to exactly 1 background run, hourly gate suppresses within-window re-runs + re-runs after the window. `maintain_sport` still synchronous → its direct tests (test_p4_grading/test_recalibration_durability/test_recal_blend, 37 green) unaffected. **DTU hot-path list now substantially COMPLETE — see the perf bullets below.**

- **[PERF 2026-09-17] Team-market reads collapsed via two bulk-primes — ~4N single/small SELECTs/slate → 2 bulk reads (COMMITTED `8cbc85b` standings + `9ee949b` team-games, NOT pushed).** The live team-market path (`espn_client.mlb_warehouse_team_stats`, run per team in the event pool) read `get_team_standings` (1 single-row SELECT/team) + `get_team_games`→`_team_final_games` (1 recent-games SELECT/team) = ~4 SELECTs/game on 20-DTU (the `warehouse_mirror` fast-path is Cloud-unavailable, so Cloud always hit SQL). Added two bulk-prime seams mirroring the b4cd2fc gamelog/rates pattern: `mlb_warehouse.bulk_standings_map` (all teams' latest-snapshot block, ONE query, same read as get_team_defense) + `bulk_team_games_map` (whole season's final games, ONE query, each game bucketed under BOTH home+away team_id). `get_team_standings`/`_team_final_games` serve from the primed module map on the LIVE path (as_of_date=None) before the SQL; as-of/backtest reads fall through untouched. ⚠ CRITICAL: the team-games serve returns FRESH dict COPIES — `mlb_warehouse_team_stats` rekeys team names IN PLACE and a game is shared between its two teams in the prime, so without the copy analyzing one team would corrupt the opponent's view of the same game (caught + regression-tested). `app.py` primes both once per slate (cached helpers `_mlb_bulk_standings`/`_mlb_bulk_team_games`, daily key) in the MLB warm block, gated on the TEAM flag + SQL, fail-open. Tests: 3 new (bulk map + serve-from-prime + copy-isolation); full test_mlb_warehouse still only the 7 known pre-existing live-Azure failures. **Combined with the daemon-thread fix, an MLB team+prop slate now issues a tiny bounded set of bulk reads instead of the per-player/per-team storm + the ~45s-2min inline maintenance block.**

- **[PERF 2026-09-17] DTU hot-path list finished — 3 more wins + the rest covered/low-value (COMMITTED `689368e` + `67e85aa`, NOT pushed).** (#8, `689368e`) `find_game_pk_by_commence` ran once PER PROP-PLAYER (~30/game) with identical game-level args → added a 300s memo keyed on (home,away,commence,tol); caches HITS ONLY so entity_resolver's same-day schedule gap-fill+retry still finds a late-ingested game (test setUp clears the module memo). (#6, `689368e`) `_fetch_team_schedule` called `get_team_games` with NO season → unbounded all-history scan that also bypassed the season-keyed bulk-games prime; scoped to current season (now hits the prime + matches the primary team-stats path). (#4/#5, `67e85aa`) `build_matchup_features` built the season `_game_lineup_index` (full-season box-score PA scan) on every live game only to return None (live lineup edge is always inert — no box score for a future game); now skipped when as_of_date is None (byte-identical inert None), backtests unchanged. **COVERED/SKIPPED after review:** #12 pitcher-CV gate reads via `_game_log` → already served by the b4cd2fc gamelog bulk-prime; #13 player_id_map prime-at-boot SKIPPED (P6 reordered SFBB BELOW the statsapi roster resolver, so player_id_map is a minority FALLBACK — lazy build is correct, priming would do wasted work); #9 short-TTL memo + `resolve_actuals_bulk` = LOW VALUE now (the big repeated reads are all bulk-primed; remaining are 1 read/Analyze, and maintenance is off the request path). ⚠ LIVE-TEST on Cloud: run an MLB team+prop slate (fast now); the first slate/day still pays the one-time cached bulk reads + season index builds, every later run/reconnect is memory-served.

- **[PUSHED 2026-09-17] origin/main → `f89afe3`** (Doug authorized "push"). Delivered the whole DTU perf arc: `87ac8e6` maintenance daemon-thread, `8cbc85b` bulk standings, `9ee949b` bulk team-games, `689368e` game_pk memo + team-schedule scoping, `67e85aa` live lineup-index skip (+ memory). Streamlit Cloud redeploys on this. NEXT: Doug live-tests an MLB team+prop slate for the end-to-end speedup.

- **[PERF 2026-09-17] Coherence offset now DURABLE + SELF-MAINTAINING in Azure — kills the last cold-Azure read on the first slate/day, zero manual upkeep (COMMITTED `8b5cec9`, NOT pushed; supersedes the committed-JSON idea `a4af72b` which was never generated/pushed).** Doug: even after the process-global cache (836909a), the first team-market slate of the day still stalled on the one 3-season Azure read — "makes the other enhancements pointless if we're still waiting forever." First tried a committed JSON (a4af72b); Doug's better idea: use a WRITABLE Azure table so the app maintains it itself, no commits/manual runs. **A mean is exactly incrementally updatable** — `new = (S_old+S_new)/(n_old+n_new)` — so we store its SUFFICIENT STATISTICS (sum, n, watermark) in the existing `app_settings` KV (one row/sport, `setting_key=coherence_offset_<sport>`, ~110-char JSON < the String(256) col; **ZERO new DDL**). `coherence_offset_store.current_offset(sport, today)`: no row → BOOTSTRAP once from a full-season read + persist; else read ONLY triads after the stored watermark (through yesterday), fold into the running mean, WRITE the advanced watermark back → daily read stays tiny forever, durable across restarts. `app._mlb_coherence_offset` calls it (cached daily), falls back to live full `compute_offset` only if the store fails. `coherence_flags.compute_offset` refactored onto shared `_triad_offset_stats`/`compute_offset_stats` (full & incremental can't drift); `--freeze-offset` now (re)seeds the Azure baseline (OPTIONAL — app auto-bootstraps); `r2_data.load_team_triad_range` = the date-scoped incremental read. ⚠ watermark advances strictly-past → a late-completed triad for a passed date is skipped until a re-seed (negligible over thousands). 8 new tests (KV round-trip, exact fold, watermark advance, no-op-when-current, bootstrap, fail-open) + coherence suites green. **▶ NO owner action REQUIRED** (first slate auto-bootstraps once, ~one 3-season read ever, then tiny daily deltas). OPTIONAL pre-seed to avoid even that one bootstrap read: `python coherence_flags.py --freeze-offset`.

- **[PUSHED 2026-09-17] origin/main → `70bb6ff`** (Doug authorized "push"). Delivered the self-maintaining Azure coherence-offset store (`8b5cec9` + the superseded JSON commits `a4af72b`/`6561be0` + memory). Streamlit Cloud redeploys; first team-market slate auto-bootstraps the app_settings row once, then tiny daily deltas. NEXT: Doug live-tests — the "Loading team data…" step should be gone after the one-time bootstrap.

## NFL — OPEN / next (team model is done; these are the live threads)
- **▶ NEXT FRONTIER A — NFL PROPS via the modeling machinery.** The heavy MLB stats work was PROPS-side (methods A-E, xStats blends, per-prop calibration); NFL props were only crudely flat-side scanned (`nfl_props_scan.py` → recreational OVER-bias, vig-eaten; the one apparent survivor `receptions OVER 0.5` was a DNP-survivorship artifact, killed). Untapped: real per-prop projection + calibration off the `nfl_player_week`/`nfl_snap` layers. ⚠ `calibration/americanfootball_nfl.json` props section is a TOY (`player_pass_yds` ≡ `player_rush_yds`, byte-identical — copy-paste placeholder) — rebuild from a real player-week layer with OOS splits.
- **⛔ FRONTIER B — transient-soft-line logger: SHELVED (Doug 2026-09-18, infra-blocked).** The thesis (catch sub-hour soft/transient numbers via a high-frequency pre-game DK/FD poller) was the ONE untested edge path, but it needs a SCHEDULER to poll every few minutes — and **Streamlit Cloud can't run scheduled/background jobs** (the app only runs while a session is open; the maintenance daemon we built piggybacks on user activity, not a real cron). So the logger is not buildable on the current infra. Would require external scheduling we've decided against running (GitHub Actions cron / Azure Functions timer / a cheap VM cron → write to Azure → app reads) — revisit ONLY if Doug wants to stand up that infra. The related (a) market-shrunk break-even lens already found NO reliable required-edge X* (`nfl_edge_calibration.py`). ⇒ with the logger shelved and the edge-hunt exhausted across MLB+NFL, the product is MODEL ACCURACY + the bonus/promo +EV play (both live).
- **▶ PARKED lead — anchoring bias (Week-1 only).** Paper (Patterson/Shank/Fodor, SSRN 2025): fade the preseason-SB-anchored favorite ATS in Week 1. `nfl_anchoring.py` built; 2026 SB odds fetched (`calibration/nfl_preseason_sb_odds.json`). Week-1-only ≈ 16 games/yr → too thin to prove on 3-4 seasons; lean on the paper's 20-yr prior + forward-track. Best literature-backed lead we have.

## Open review findings
- **[Separate improvement study completed, 2026-09-08; implementation not applied]** See [`notes/APPLICATION_IMPROVEMENT_STUDY_2026-09-08.md`](../notes/APPLICATION_IMPROVEMENT_STUDY_2026-09-08.md), its standalone HTML and roadmap CSV. Doug prioritized predictive accuracy and reliable betting decisions. New evidence: rounded expected AB produces discontinuous MLB hit probabilities; opportunity mixtures improve an isolated empirical-rate 0.5-line screen in both 2024/25, requiring full D+xBA validation. Fixed alternative NFL probability models do not convincingly beat the projected-QB chronological baseline or market. DK/FD matched offers expose execution improvements; single-bet correlation rules, greedy optimality claims, SGP surrogate pricing and batch-only exposure caps need work. Twelve ranked work packages cover all application areas; NFL props opportunity/participation modeling is a leading next build. Six offline measurements completed; focused tests 161/164 pass (two mirror-dependent SQL mocks, one stale bucket expectation). No paid API calls, production model/data changes or push.
- **[Independent review completed, 2026-09-08; fixes not applied]** See [`notes/INDEPENDENT_REVIEW_2026-09-08.md`](../notes/INDEPENDENT_REVIEW_2026-09-08.md) and its read-only reproduction helper. The earlier blanket leakage/ceiling/replication claims above are challenged by observed defects: NFL majority-passer identity uses game outcomes; closing quotes include post-kickoff captures; serving drops rest and miskeys night-game injury weeks; totals has a defensive-EPA sign error; MLB's +1.26% batter-K survivor is 2024-only; MLB calibration can borrow a different game's actual; candidate diffs omit nested prop changes. The market still beats the NFL model after quote-time filtering. Retain “no durable edge demonstrated,” not a proven ceiling. No production code/calibration/data changes or paid API calls in this review.
- **[OPEN, medium] NFL depth-chart 2025+ schema drift** — nflverse reshaped `load_depth_charts` to a dt-snapshot format (no week). `nfl_ingest.ingest_depth` is now schema-tolerant (stamps season, keeps union cols) but 2025+ is snapshot-based; a consumer that needs a 2025+ depth backup must pick the latest `dt` before the game date. Not blocking (the live QB projection uses recent-starter, not depth; depth is only the fallback). → [[data-and-architecture]].
- **[OPEN, medium] warehouse_mirror live-season staleness** (`warehouse_mirror.py`): `ensure()` fast-path returns instantly when all files `_valid` but never re-syncs the CURRENT season → backtests can read stale current-year data. MITIGATION: `--refresh-mirror`. FIX: exclude the live season from the `_is_valid` fast-path.
- **[RESOLVED] snap_counts layer** — ingested; `nfl_injury_impact` now uses snap share for all-position regular-detection.

## Ongoing monitors (MLB — parked/efficient, accumulate + watch)
- **⛔ Coherence run-line — PAUSED (dead on clean data).** Devigged bands + clean corpus → replication FAIL; the +11% was a raw-band artifact. Live `coherence_flags` cv_max=1.0 gate bets a −EV population → do not run. → [[edges-and-backtests]].
- **`batter_strikeouts UNDER 1.5` — the one clean-data MLB survivor** (+1.26% t=1.69, replicates; inverted public over-bias). Bet flat at DK/FD only when UNDER ≥ ~−110; small stakes, do NOT scale. → [[edges-and-backtests]].
- **⚠ cv_floor (earned_runs E + CV≥1.3) — SUSPECT.** Never re-validated on the clean corpus; re-run before any stake. → [[edges-and-backtests]].

## MLB — DONE + parked (tombstones; detail in domain files)
- **All MLB edges dissolved on the clean corpus (2026-09-05).** Steps 1-4 done: CLV, props recalibrated (batter_hits D+xBA(1.0)), team refit (well-calibrated, no edge), market-structure (coherence/under/f5 all DEAD). Lone survivor batter_K UNDER 1.5. → [[edges-and-backtests]] + [[modeling-and-calibration]].
- **Precise 5M-credit backfill + warehouse + mirror = DONE (2026-09-03).** Uniform −12h/−4h/close windows; backtests read the mirror 0-DTU. → [[data-and-architecture]].

## Parked calibration ideas (MLB, deferred while NFL active) → [[modeling-and-calibration]]
- value_gate auto-tune (advisory + `--promote`, never live-auto); EV floor 4%→3% for DK/FD; pitcher props seed Platt + exit synthetic-line fits; online Platt Blob-mode hardening (SQL-only today).
