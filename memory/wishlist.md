---
name: wishlist
description: "Parking lot for owner ideas raised mid-work (deferred, not-yet-scoped); check when picking the next initiative"
metadata: 
  node_type: memory
  type: project
  originSessionId: f7a9060a-d003-424e-b845-98201e6c954e
  modified: 2026-08-27T19:54:54.729Z
---

**CONVENTION (owner, 2026-08-15):** when the owner interjects a NEW idea while other work is in progress, ADD IT HERE rather than derailing — *unless* it's immediately relevant to the work being done. Revisit this list when choosing the next initiative.

## Open ideas

- **Precompute + STORE per-(prop, method) uncertainty from A-E method DISAGREEMENT, for the bet selector to weigh** (owner, 2026-08-24). Compute the cross-method dispersion (spread of the A/B/C/D/E probabilities on the same obs) ONCE per prop/method, persist it (calibration file or a small table), and have `select_top_bets` read it as a confidence weight / abstain input alongside EV. New vs the entries below only in the PRECOMPUTE-AND-STORE + selector-wiring emphasis. Reuses the BANKED, source-agnostic primitive `kelly_fraction_uncertain`/`prob_interval_low` (pricing_common, commit 0c4fbf3 — just needs a prob_low, which the method spread can supply). ⚠ RECONCILE WITH THE EMPIRICAL INVERSION: `--consensus-diag` (2026-08-15) found on batter_hits that DISAGREEMENT / WIDE dispersion was MORE profitable (split +11% vs agree −3%; wide +5% vs tight −3.7%) → a naive "downweight high-dispersion" gate would cull the profitable bets. So MEASURE the ROI/Brier lift of any dispersion weighting on backtest; don't assume high-uncertainty = bad. Same layer as the per-player-uncertainty-gate + bet-disqualification (#1 model-uncertainty) + method-consensus entries below — FOLD TOGETHER when building; this note pins the "store it for the selector" shape. See [[fresh-calibration-tooling]] (uncertainty-Kelly banked for props), [[kelly-bet-sizing]], [[baseballpredictions-mining]] (uncertainty-propagation spine).

- **Serve full-season props from an as-of AGGREGATE table (skip the per-prediction rollup)** (owner, 2026-08-19). Now that STEP-1 locked FULL-season windows for hits/batter-K/pitcher-K, the projection for those is just the player's season-to-date rate — so a materialized as-of table (per player per date) could serve it with ONE row read instead of fetching ~150 game rows + rolling up live. ⚠ NOT a straight drop-in: the props projection also needs the per-game DISTRIBUTION (method A/C/E over-rate at the line — a mean alone can't give P(hits>1.5)), the reliability filter (per-game), and any per-game venue/opp/park weighting. So the clean version = an as-of table that PRE-COMPUTES the distribution stats (mean, variance, over-rate at standard lines / ECDF, count) per player-date, which the live path reads instead of rolling up = feature-store pattern for props. Infra partly exists (mlb_warehouse asof_batter_ops / pitcher_asof_daily). DEFER until AFTER step-2 recalibration + step-3 adjustments settle WHICH method (and thus which pre-computed stats) each prop needs. Also: only helps FULL-window props; ER (20) + any per-game-adjusted prop still needs game rows. ★ RELATED FINDING (2026-08-19): the refit engine already fits methods on FULL-season projections while live served at recent_n=20 — a latent fit≠serve mismatch that the STEP-1 full-window change CLOSES for hits/batter-K/pitcher-K (step 2 mainly needs to fit ER at 20). See [[assumed-defaults-register]], [[tier-a-savant-pitcher-work]] (as-of stores), the feature-store cache work.

- **Team-market accuracy carryover — apply the props recency/feature findings to the RUNS model** (owner idea flurry, 2026-08-19). STEP-1 proved MLB player-prop projections should use the FULL SEASON (equal weight, decay OFF) for hits/batter-K/pitcher-K (ER=20); the team/runs model predicts the same kind of thing (team run production) but its recency is UN-audited. Owner: "shouldn't we incorporate these into the runs process, it's essentially the same as predicting player props?" — yes. Cluster to test in STEP 3 (team-market re-validation), all graded on REAL closing lines:
  1. **Team-market recency sweep** — team-form lookback `recent_n=20` (app.py) + `RECENCY_HALF_LIFE` mlb=7 (stats.py) + the additive model's `n_starts=10`/`blend_k=200` (additive_runs.py) are ALL never-swept. The props finding predicts full-season team offense likely beats a 20-game/hl7 window. Build the analogue of `--recency-sweep` for team markets.
  2. **Lineup vs ACTUAL starting pitcher (BvP) as a weighted input** — blend the specific lineup-vs-this-pitcher history with the general full-season offense rating, WEIGHTED by BvP sample size (empirical-Bayes: shrink toward general when thin). ⚠ raw BvP is tiny-sample/noisy (often <10 PA); sabermetric consensus is it adds little beyond PLATOON/HANDEDNESS (the robust high-sample core) — so build the platoon/hand split first, add the pitcher-specific tail only via sample-weighted shrinkage. The pitcher's GENERAL quality is already an additive-model input; this adds the matchup-specific layer.
  3. **Pitcher home/away splits** — ALREADY partially in as the venue-match weight (`VENUE_MATCH_WEIGHTS` mlb (1.40,0.60), a soft same-venue tilt — NOT a hard split that would halve the sample). Its STRENGTH is an unswept default → sweep it (more history to work with now under full-season windows).
  Team markets are our WEAKEST area (model Brier > market everywhere), so this is plausibly the highest-value carryover. See [[assumed-defaults-register]], [[team-market-audit]], [[feature-edge-roadmap]].

- **Per-PLAYER projection-uncertainty gate / sizing ("don't bet unpredictable players")** (owner, 2026-08-19, said half-joking during the recency sweep but it's a real lever). ★ MECHANISM (owner 2026-08-19): run a SHORT-window projection alongside the locked FULL-season one — their DIVERGENCE is a direct "player in flux" signal (trade / mechanical change / injury return / genuine hot-or-cold streak). Big gap → abstain or size down; tight agreement → high confidence. Cheap: reuses the exact same projection process at two windows, no new data. Pairs with the full-season projection (stability) as its natural complement. Instead of forcing per-player predictability into the recent_n WINDOW (can't — window is per-PROP, one setting for all players of that prop; and a long window self-caps for thin-sample players so it mainly mis-serves role-changers/decliners), make predictability a SELECTION/SIZING layer: only bet (or bet smaller) when a player's projection confidence is high enough. Concretely = the Beta-Binomial `posterior_sd` + cross-method dispersion feeding an abstain/interval-Kelly gate — the uncertainty-propagation SPINE mined from [[baseballpredictions-mining]] and the kelly_stake_uncertain primitive already BUILT (see [[kelly-bet-sizing]], not yet wired live). ⚠ COUNTERINTUITIVE CATCH to test, not assume: edge lives where the MARKET is wrong, and the market is LEAST efficient on volatile/uncertain players (stars are efficiently priced) — so the highest-edge bets may BE the unpredictable ones; a naive "only bet predictable players" gate could cull the population where the money actually is. So measure ROI/Brier lift of the uncertainty gate on backtest, don't just switch it on. This is a step-3/4 (selection/sizing) item; largely the same layer as the "Bet DISQUALIFICATION" idea below (criterion #1 model-uncertainty gates) — fold them together when building. See [[assumed-defaults-register]] (recency sweep found longer windows partly a survivorship artifact of the stable-star pool → the real fix for volatile players is this gate, not the window).

- **Evaluate table PARTITIONING on the large warehouse tables** (Doug, 2026-08-18). Determine empirically whether partitioning the biggest Azure SQL tables would help. Candidates + natural partition keys: **statcast_pitch** (~3.1M rows, the biggest — partition by game_date or a season computed column), **odds_line / odds_snapshot** (large — by snapshot/commence date), **mlb_batter_game / mlb_pitcher_game** (by season_bucket), **pitcher_asof_daily** (~117k, by season_bucket), **prediction_log** (by date). Hypothesized benefits: (1) partition ELIMINATION on the season/date-bounded scans the backtests hammer (the offense GROUP BY, per-pitcher as-of, game-fact reads); (2) cheap season-scoped MAINTENANCE — build_season already delete+reinserts a whole season_bucket, which a SWITCH/TRUNCATE PARTITION would make near-instant + non-logging; (3) possibly faster ingest. ⚠ CAVEATS to weigh: **Azure SQL Database has a single filegroup**, so partitioning gives elimination + manageability but NOT the multi-filegroup I/O parallelism you'd get on-prem — so the read win is mostly what a good date/season-leading index already provides. We JUST added covering indexes (ix_statcast_pitch_offense/_pitcher, ix_mlb_*_gamepk, ix_mlb_game_season_status, cba9205/a30a1ea) that likely capture most of the query benefit already — so measure the MARGINAL gain over those before committing. Also: this session proved the recent "slow query" pain was CLIENT-side (pymssql/thread-pool connection stalls; the full-season GROUP BY is 4.6s server-side) NOT big-scan/tier-bound — so partitioning would NOT have helped that; its value is genuinely-large scans + season maintenance, not the connection issue. APPROACH: pick 1-2 tables (statcast_pitch first), capture query plans + timings for the hot queries, prototype a partition function/scheme (partition-aligned indexes required for SWITCH), re-measure. Effort: MODERATE; DB-infra, offline, reversible. See [[refit-statcast-cache-gotcha]] (statcast_pitch in SQL), [[tier-a-savant-pitcher-work]] (the indexes + the client-side-not-tier perf diagnosis), [[blob-to-sql-migration]] (all state on Azure SQL).

- **Props refit MEMORY footprint — the pooled sweep hit ~10GB RAM** (Doug, 2026-08-19, during the 3-season pooled refit). ROOT CAUSE (quantified): `_build_props_sweep_grid()` = **576 variants**; `run_player_props_backtest` builds `results[variant][prop]` holding a full `calib_obs` tuple list PER variant, and multi-season pooling (`_merge_props_results`, commit 812773b) now CONCATENATES all 3 seasons' obs into that structure simultaneously → 576 × 7 props × ~20k pooled batter tuples ≈ the 10GB. In-memory accumulation, NOT a leak (frees after the run) and NOT a correctness issue. Levers, ranked: (1) ✅ **DONE — PRUNE the refit grid (commit 9e4dd6f, unpushed):** `--focused-grid` (opt-in; full 576 stays default) = ~37 variants — full res on half_life×venue×shrink (the axes that win props) + opp/def/rest probed once. Built by SELECTING a name-subset of the full grid so presets/labels are byte-identical → focused winners directly comparable, baseline gate unchanged. ~15× less CPU (esp. the per-variant NegBin E-fit) + peak RAM. Drops knob-INTERACTION cells → use the full grid for a FINAL run. FocusedGridTests added. USE for iterating: add `--focused-grid` to the refit command. (2) **Spill per-season calib_obs to disk + stream the selection** (literally Doug's "cache some data") — caps RAM at ~1 season; moderate refactor; STILL OPEN if the focused grid isn't enough. (3) **Compact obs layout** — (name,line,actual,date) identical across variants, only projected/empirical_over differ → store shared fields once for ~2-3× cut; STILL OPEN. Also: caching the WAREHOUSE FETCHES is a separate axis — speeds RE-RUNS, does NOT lower the RAM peak. Root cause was method-E's NegBin MLE × 576 variants × deep pooled obs, single-threaded (GIL) so the 24-core machine only used ~1 core. Recurs for STEP-3 team-market pooling. See [[assumed-defaults-register]] (STEP 2), [[feature-edge-roadmap]].

- **Retire migration flags once finalized (delete gate + old branch, not "leave ON")** (Doug, 2026-08-18). Once a cutover flag is validated in prod and we'd never revert, keeping it is a net LIABILITY: the OFF branch rots + drifts, the "byte-identical when off" guarantee decays, a fresh env / missing secret / new deploy target silently regresses to stale behavior, and two live paths double the bug+test surface. "Retire" MUST mean a cleanup COMMIT that makes the new path unconditional and DELETES the flag read + the dead old branch + drops it from app.py's promotion tuple and espn_client.mlb_warehouse_gate_status — NOT leaving it permanently ON (worst of both: dead branch kept forever AND now depends on an env var being set everywhere). Lifecycle: inert(OFF) → flip ON in prod → burn-in → delete. Grounded per-flag verdict (2026-08-18 grep): **ODI_MLB_WAREHOUSE_CALIB → retire NOW** (already vestigial — calib join is unconditionally warehouse-only, gate fn is dead code, yet mlb_warehouse_gate_status STILL reports it = misleading operator signal; pure zero-behavior-change cleanup). **ODI_MLB_WAREHOUSE_HIST + _TEAM → retire with the ESPN teardown** (flipped+verified prod; the ESPN branches they gate are being deleted in P4b anyway — fold in). **ODI_MLB_ADDITIVE_RUNS → NOT yet** (unvalidated; it IS the A/B toggle for the #1d backtest; retire only after it passes+promotes+burns in — and even retired, KEEP the multiplicative FALLBACK: retiring just deletes the `if _enabled()` guard so additive is always attempted with multiplicative on None). **ODI_MLB_ENFORCE_IDENTITY → KEEP** (NOT a migration toggle — a live risk VALVE: fail-closed identity enforcement + circuit breaker; legit reason to leave off until the ~2.6% unresolved rate shrinks). **ODI_MLB_WAREHOUSE_OFFENSE → retire after warehouse-bullpen lands + validated live** (new 2026-08-18, commit 297ff23; migrates the challenger's team offense off live Savant HTTP onto statcast_pitch — a migration flag like the others; parity 30/30 max 0.0006). **ODI_MLB_STAMP_RESOLVER → keep until Commit C lands** (deferred). **ODI_MLB_BOXSCORE_WORKERS → not a gate** (concurrency int knob; belongs in the future SQL param store, not retirement). See [[mlb-statsapi-rearchitecture]] (gate inventory), [[tier-a-savant-pitcher-work]] (#1d flag), [[commit-c-plan]] (STAMP_RESOLVER), [[unified-tunable-param-store]] idea above (BOXSCORE_WORKERS).

- **As-of Statcast xwOBA for the spreads challenger (backtest faithfulness)** (owner, 2026-08-16). The expected-runs SPREADS challenger uses the starter's Savant xwOBA for its staff-suppression term. LIVE that's present, but the backtest runs in AS-OF mode which deliberately OMITS Savant xwOBA to stay leakage-safe → the challenger used to not fire in the backtest at all. STOPGAP shipped 2026-08-16: `mlb_starters._expected_staff` falls back to the as-of ERA-based `run_suppression` when xwOBA is absent, so the challenger now fires in the backtest — but with an ERA PROXY for the starter, not the true xwOBA production uses. So the backtest spreads model is DIRECTIONALLY faithful, NOT byte-exact to production (this is why the "-5% disqualify spreads" verdict was overturned to "marginal" — but the exact production-spreads ROI is still unmeasured). REAL FIX: build an as-of cumulative pitcher xwOBA aggregation (season-to-date through the day before each game) from the Statcast SQL warehouse (`statcast_pitch`/`statcast_day`, see [[refit-statcast-cache-gotcha]]), mirror the existing as-of pitcher/batter index pattern (mlb_warehouse `asof_pitcher_stats`/`asof_batter_ops`), and feed it into `get_pitcher_quality(as_of_date=...)` so the backtest challenger uses the SAME xwOBA basis as live. Then re-run the spreads unleash/combo sweeps to get the true production-faithful spreads ROI + settle disqualify-vs-keep honestly. Effort: MODERATE (one more as-of warehouse index + wiring; offline-only, no live risk). Blocks: a fully-trustworthy spreads verdict. See [[team-market-audit]] (spreads faithfulness caveat), [[refit-statcast-cache-gotcha]] (raw Statcast pitch cache now in Azure SQL).

- **Datamine market UPSETS — what makes the book wrong?** (owner, 2026-08-16). Research the games where the result went AGAINST the closing line (favorite lost outright, big underdog covered, total blew past the line) and hunt for FACTORS that correlate with the market being wrong — candidate edge sources the market underweights. Approach: label each warehouse game by "upset magnitude" (e.g., closing de-vigged implied prob of the actual winner, or actual margin/total vs the closing line) → analyze/train a simple model on candidate features (park, weather, bullpen fatigue/recent workload, today's-lineup deviation from typical, platoon/hand mismatch, umpire, travel/rest, day-after-night, get-away day, September callups, weather-run-environment) → surface the top upset-predictive factors. Read-only over the warehouse (mlb_game + odds_snapshot/odds_line + park_factors/weather_factors + mlb_batter/pitcher_game). Deliverable: ranked factors that predict market misses → feed the model as features OR a bet-selection/disqualification signal. ⚠ Reframe of the whole team-market finding: we CAN'T beat the close with current features (model Brier > market everywhere), so the only path to real edge is finding what the market systematically MISPRICES — this study is that search, done directly. Strong complement to [[team-market-audit]] (D-phase inputs: lineup/weather/park/bullpen are the prime suspects) and the bet-disqualification idea below.

- **Bet DISQUALIFICATION / risk-reward filtering** (owner, 2026-08-15). We've invested heavily in ACCURACY + calibration (making the probabilities right), but have NOT systematically identified what lets us THROW AWAY bets carrying too much uncertainty or a bad risk/reward ratio. Goal: a disqualification layer that culls bad-risk wagers before they're recommended. Candidate criteria to explore: (1) MODEL-UNCERTAINTY gates — wide cross-method dispersion (A/B/C spread) or model-vs-market disagreement magnitude → skip (ties into the [[wishlist]] consensus idea: dispersion as an uncertainty signal); (2) RISK/REWARD floors — minimum edge-after-vig, minimum Kelly fraction (too-small stake = not worth the variance), maximum downside/odds (heavy favorites where a loss craters ROI); (3) NO-CALIBRATION disqualify — props with no real calibration fit price on a raw over-rate (⚠ TB/RBI, see [[mlb-statsapi-rearchitecture]]) → gate them out until seeded; (4) CLV/line-movement — disqualify when the line has already moved against us since the projection (ties into the line-timing idea above); (5) low-sample / thin-history players or markets; (6) correlation-aware culling (same-game legs). Deliverable: a ranked set of disqualification rules with measured ROI/Brier impact (backtest each rule's lift), layered ON TOP of the existing EV/value_gate selection. Distinct from bet-SELECTION (which picks the best of the offered value) — this is about REFUSING marginal/high-variance bets entirely. See [[accuracy-improvement-roadmap]], [[kelly-bet-sizing]] (sizing already vig-aware/fractional; caps exist but no disqualification logic), [[accuracy-followups-executed]] (EV floor 4%→3% + value_gate auto-tune already noted).

- **Line-timing / early-betting CLV-decay analysis** (2026-08-15). Determine whether betting EARLY (the line/odds moving after the bet is placed) is hurting or helping — i.e. should slates be analyzed closer to game time instead of early? Wants a concrete "wait vs bet-now" answer. Data already on hand should suffice: `prediction_log` + `wagers` (placed price/time) + `odds_snapshot`/`odds_line` (the price path) + the DK-vs-DK CLV backfill (`backfill_dk_clv.py`, executed_price vs close). If in-hand data is too thin, the Odds API historical endpoint could fill the price path. Deliverable: per-market (and maybe per-lead-time-bucket) realized CLV + ROI as a function of how early the bet was placed → a recommendation on optimal analysis/bet timing. See [[clv-closing-line-capture]] (CLV machinery), [[app-fixes-followups]].

- **⏸ PROPS backfill — TOOL BUILT, DEFERRED until post-refit (owner, 2026-08-15).** `topup_props_odds.py` is written + dry-run-verified (warehouse-driven gap diff of missing prop-games → per-game Odds-API fetch of only each game's MISSING markets, NEWEST-FIRST so the corpus deepens backward, budget-capped `--max-credits`, write-once). NOT yet test/review-hardened and NOT run. **Owner decision: WAIT for the full backtest + `refit_calibration --real-lines` first** — the per-prop ROI/Brier may show some props aren't worth OFFERING at all, so don't pay to backfill doomed props; decide the `--markets` set from the refit, then backfill only survivors. ⚠ KEY NUANCE (owner, 2026-08-15): the synthetic-line refit measures ACCURACY (calibration) for every prop incl. TB/RBI (we have the actuals), but NOT real-market PROFITABILITY (needs real closing lines we lack for TB/RBI). Owner SUSPECTS `batter_total_bases` may out-earn `batter_hits` — currently untestable. So TB/RBI are EVALUATE-VIA-SAMPLE, not skip: after the refit seeds their calib, pull a cheap real-line SAMPLE (`topup_props_odds.py --markets batter_total_bases,batter_rbis --max-credits ~6000` → ~300 recent games @ ~20cr) and run `backtest --mode props-odds` to get a real ROI read on TB vs hits BEFORE committing the full backfill; a proven prop gets promoted into the ongoing set (and a "core" one that doesn't earn its keep gets demoted). Real backlog (dry-run): 8,616 prop-eligible games 2023-05-03→present, only 365 with any prop odds → effectively ALL need props (`batter_strikeouts` was NEVER historically captured). Full cost by market count: 3 core ~255k cr, 5 core ~421k cr, 7 all ~590k cr. At 20k/mo (minus keep-up ~2,430 new games/yr): 7 mkts ~9yrs (infeasible), 5 ~3.6yrs, 4 ~2.4yrs, 3 ~1.5yrs. 100k-boost math: 100k ≈ 1 season newest-first (2,200 games), 200k (2 boost months) ≈ 2026+2025 across 5 markets (~51%), full 5-mkt backlog ~4.2 months of 100k. RECOMMENDED hybrid: 2 boost months → recent 2 seasons, then 20k/mo grinds 2024/2023. Default DEFAULT_MARKETS = the 5 calibration-backed core (skips TB/RBI = `batter_total_bases`,`batter_rbis`, no calib). ⚠ reserve monthly credits for LIVE daily ops (set `--max-credits` below the full plan). Harden (offline gap-diff tests + 4-lens adversarial review like the team tools) + finalize market set AFTER the refit. See [[accuracy-improvement-roadmap]], [[mlb-statsapi-rearchitecture]] (TB/RBI no-calib live risk).

- **Odds backfill (historical real lines) — original note** (2026-08-15). `backfill_historical_odds.py` pulls 2023-present real book lines (Odds API credits). RELEVANT-NOW candidate: real book lines currently only span ~5 weeks (2026-07-10→now), so `--real-lines`/`diagnose_roi`/`--consensus-diag`/`prob_shrink`/`market_prior_k` are all limited to that window — a backfill is the direct fix + the way to confirm/kill the batter_hits consensus inversion below. Plumbing CONFIRMED: `--warehouse` writes the fetched payloads into the Azure odds warehouse via `warehouse.capture_event_odds` (backfill_historical_odds.py:336-344) — exactly what the refit harvest reads by default. Cost: props are per-GAME/expensive (tool's own example caps ~19k credits; balance ~18k) → ALWAYS `--dry-run` first to price the scope; `--max-credits` caps + fits prop games to budget; props historical only after 2023-05-03; schedule enumeration still via ESPN (offline, fine). Owner previously deferred this (~11k team-only). Decide via dry-run cost; a smaller window (2025+2026) is a cheaper middle path. **UPDATE 2026-08-15:** priced team-only (30cr/snapshot, daily): 2024 ~6.24k / 2025 ~6.36k / 2026-to-date ~4.2k. **TEAM-ONLY 2025-full + 2026-full BACKFILLED to the Azure warehouse (~10.5k cr, balance was 17,146)** to strengthen prob_shrink (was gated by a 5-week window) + cross-season team ROI. **PROPS backfill stays wishlisted** — dense = infeasible (~70cr/GAME → ~118k/season; the whole budget buys only ~192 prop games), and the props corpus grows FREE via live capture; revisit only for a targeted cross-season consensus spot-check. ⚠ FIXED a real bug: `backfill_historical_odds --warehouse` never promoted SQL secrets → silently wrote nothing to Azure offline; now promotes in main() (mirrors backtest.py). Also: `odds_client.get_remaining_credits()` returns None even when the key is valid (direct /v4/sports probe works) — the `--reserve` guard is inert as a result; `--max-credits` is the real cap. Minor, worth a look.

- **Unified tunable-param store (retire hard-coded weight constants)** (owner, 2026-08-17). Move the remaining TUNABLE model constants out of module code into the same candidate-staged param store the calibration already uses, so every knob re-tunes via `--diff`/`--promote` instead of a code edit + redeploy. MOTIVATION: the split is inconsistent + friction-y — `prob_shrink`/`starter_adjustment`/`expected_runs_challenger`/`value_gate` live in `calibration/baseball_mlb.json` (one-command refit), but `DEFAULT_PYTHAG_WEIGHT=0.35` / `DEFAULT_LINEUP_WEIGHT` / `DEFAULT_PYTHAG_RESIDUAL_WEIGHT` are CODE constants in `analysis.py` (require code+deploy to change — hit directly during the 2026-08-17 pythag re-sweep). RECOMMENDED SHAPE: (1) STEP ONE = fold those code constants into the EXISTING calibration-candidate store (already has staging/promote/diff/fit-timestamp/audit) — smallest change, kills the friction, unifies the workflow; a sweep can then stage a pythag candidate like any other refit. (2) JSON-file vs SQL-table is a SEPARATE decision — the file store is proven + git-versioned; SQL matches the rest of the stack (everything else migrated to Azure SQL per [[blob-to-sql-migration]]) and would enable live-edit/UI + multi-writer. Lean: keep the candidate→promote MECHANISM, SQL-back it only if a live-editing/multi-writer need is real, not as a rewrite for its own sake. GUARDRAILS: (a) always keep the hard-coded value as a FAIL-OPEN default so a store/SQL outage never breaks pricing (calibration_loader already does miss→code-default); (b) scope to TUNABLE weights/thresholds ONLY — NOT structural constants (feature keys, clamp bounds, model architecture, the ODI_MLB_* flags) — those staying in code is a feature. Note the app is Alpha/single-user so the "no redeploy" benefit is modest; the real win is CONSISTENCY + auditability + sweeps writing candidates directly. See [[calibration-candidate-workflow]], [[accuracy-improvement-roadmap]].

- **Method-consensus / agreement bet-selection layer** (2026-08-15). ⚠ **DIAGNOSTIC RAN 2026-08-15 — hypothesis NOT supported; INVERTED on the deep prop.** `--consensus-diag` over the ~5-week real-line window: on **batter_hits (n_test 2806, ~950 bets — the only trustworthy n)** AGREE −3.1% ROI vs SPLIT +11.0%, and TIGHT dispersion −3.7% vs WIDE +5.0% — i.e. requiring agreement would KEEP the worse bets and DROP the better ones; DISAGREEMENT was the more profitable regime. Other props thin+mixed (n 150–300, buckets <60): earned_runs/TB lean agree-slightly-better, rbis/outs/strikeouts opposite/noise; most buckets −ROI regardless. So DON'T build the agreement filter as designed. The counterintuitive "structurally-different methods (D/xBA, E) disagreeing with A/B/C flags a mispricing" is hypothesis-generating but ONE 5-week window at consensus prices — REVISIT with more real-line history (see odds-backfill above) before acting. The idea itself, for reference: NOT single-method A-vs-B-vs-C selection — a two-tier consensus: (1) combine the residual family {A empirical, B Gaussian, C ECDF} over the same projection into one estimate + use their DISPERSION as a model-uncertainty signal; (2) form a cross-family consensus with the structurally-independent methods (D xBA, E NegBin) + the MARKET; (3) MOST valuably, use cross-model AGREEMENT to gate/size BETS (fire only when models agree on the value side / disperse tightly) — a bet-SELECTION edge, which targets the ROI pain (calibration is already ~fine). Owner's literal "AND a,b,c on direction" is weak (A/B/C share the projection so they rarely disagree on SIGN — only near the coin-flip line you skip anyway); the value is magnitude-dispersion + cross-family + market. **GATE ON EVIDENCE FIRST:** the new `refit_calibration.py --consensus-diag` (built 2026-08-15) measures exactly this — per prop, ROI/hit when all methods AGREE on the value side vs SPLIT, + A/B/C dispersion tercile. Build the layer only if AGREE clearly beats SPLIT in that read (run it after the full warehouse-native backtest). Never verified as planned/built before — it's a genuinely new initiative. See [[accuracy-improvement-roadmap]], [[accuracy-followups-executed]].

- **Stuff+ / GBM pitch-quality model (owner interest 2026-08-27, PARK until after R2).** Owner wants to explore Stuff+ (pitch "nastiness" scored from PHYSICS → aggregated per pitcher) + gradient-boosted models (XGBoost/LightGBM) generally. ★ CONCRETE GATING FINDING (verified 2026-08-27): the Statcast pipeline (`savant_history.py` → Azure SQL `statcast_pitch`) currently ingests only CONTACT/outcome columns (xwoba, xba, launch_speed/angle, description, bb_type) — NOT the pitch-PHYSICS columns Stuff+ needs (release_speed, release_spin_rate, pfx_x/pfx_z movement, release_extension, release_pos). Savant returns them in the SAME CSV, so STEP 1 = extend the schema + ingest to add them + `delta_run_exp` (the per-pitch run-value target). Then: per-pitch GBM (physics → delta_run_exp/whiff) → aggregate to a pitcher Stuff+ → feed pitcher_strikeouts/earned_runs/F5. ★ This IS the workload where a GPU actually helps (millions of pitches × physics features, matrix in VRAM) — unlike the backtest/calibration (I/O-round-trip-bound, GPU-useless; the speed lever there is bulk-read-once-compute-in-RAM). ⚠ EFFICIENT-MARKET CAVEAT (hard-won): the sharp F5 line may already summarize the pitcher's full effect (that's R2's F5 thesis) → Stuff+ must prove it generates signal the sharp DOESN'T price, or flags where DK lags the sharp on pitcher markets — NOT "model the pitcher better" (the market already does). SEQUENCING: after R2 (R2 names WHERE the market is exploitable → focuses the build). Natural extension of [[tier-a-savant-pitcher-work]] (pitcher_asof exists). See [[feature-edge-roadmap]] (Stuff+ was a low-pri "defer to sharp-F5" item), [[refit-statcast-cache-gotcha]].

- **Monte Carlo game/PA simulation ("simulation theory") (owner interest 2026-08-27, PARK — partly just interesting, which is a fine reason).** Simulate games PA-by-PA (or pitch-by-pitch) from component rates to derive full outcome distributions for ANY market (props, ML/RL/total, F5, segments) off ONE coherent engine, instead of separate parametric (Poisson/NegBin) closed-forms per market. SYNERGIES: (a) it's exactly the "PA-state-machine simulator that prices everything off one distribution" already noted as the engine for the CROSS-MARKET COHERENCE edge above (baseballpredictions #17) — a sim flags the book's self-contradictions by construction; (b) it captures CORRELATIONS parametric models can't (big-inning clustering, F5-vs-full-game, same-game-parlay legs); (c) Stuff+/GBM outputs could FEED the sim's per-matchup input rates. ⚠ SAME EFFICIENT-MARKET CAVEAT: a sim won't beat the market's MEAN either — its value is coherent multi-market pricing + correlation/parlay edges + niche/soft markets + variance signals, not raw prediction. SEQUENCING: after R2, likely with/after Stuff+ (which feeds it). See the coherence entry above, [[baseballpredictions-mining]] #17/#23, [[market-upset-study]].

- **FanDuel batter-prop capture (follow-up to the precise backfill, 2026-09-01).** The precise-backfill raw payloads + parquet contain FULL FanDuel batter props (batter_hits/rbis/total_bases), but `odds_client.parse_player_props` → `warehouse._emit_prop_lines` DROPS every FD batter prop on the way into `odds_line` (keeps DK + Pinnacle + FD pitcher_strikeouts) — a pre-existing warehouse limitation ([[preferences]]: "FanDuel has NO batter props captured"). Diagnosed 2026-09-01: raw FD batter_hits ~1804 outcomes/30-games → 0 emitted, vs DK/Pinnacle raw==emitted. Likely cause: FD posts MULTIPLE lines per player (e.g. hits 0.5 AND 1.5) and parse_player_props' one-line-per-player selection discards FD's structure. FIX = teach parse_player_props to pick/keep FD's lines (choose the standard line, or store multiple). ENABLES FD PROP LINE-SHOPPING in the warehouse (FD is an executable book). NOT lost meanwhile — raw FD props are preserved losslessly in `odds_backfill/parquet/` + `cache/hist_event_odds/`, so this can be built + re-emitted anytime WITHOUT re-spending credits. Phase 3 reload deliberately proceeds with existing prop behavior (team/F5 is the goal + where the edges live). See [[data-and-architecture]], precise-backfill in [[edges-and-backtests]].
